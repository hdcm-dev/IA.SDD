# Actuación 007 — Informe Comisión V2, Interfaz y contrato con el consumidor

| Campo | Valor |
|---|---|
| Tipo | `informe` |
| Fecha | 2026-09-13 |
| Autor | Comisión V2, Interfaz y contrato con el consumidor |
| Corrige | — |

## 1. Mandato y método

**Pregunta que respondo** (carta `ev-07/05-V2-contrato-consumidor.md`): qué es exactamente el contrato entre el bundle y .NET —operaciones de entrada, eventos de salida, esquemas, errores, versionado, identidad de instancia—, cómo se declara de los dos lados sin que un archivo dependa del otro, cómo se detecta que se rompió, y qué de todo eso tienen que ejercitar las dos PoC. Distingo contrato de **compilación** y de **integración** con el vocabulario de `PRODUCT-INTAKE-template.md` §14. No opino sobre ubicación de archivos (V1), construcción (V3) ni ciclo de vida del circuito (AH-2) salvo en lo que el contrato tenga que declarar.

**Base:** `IA.SDD` `a501857` (13.17); precedente `<F1>` `9aabe5c` (actuación 002, cabecera).

**Qué leí, en este orden** (rutas absolutas abreviadas: `EXP` = `<workspace>/IA/SDD/IA.SDD/Expedientes/0002-Conocimiento-Bundle-JS-En-Solucion-Dotnet`; `SDD` = `<workspace>/IA/SDD/IA.SDD/SDD/Devs`; `PIN` = `<repo-privado-F1>/src/<F1>.<Componente>`; `DOC05` = `<repo-privado-F1>/SDD/Docs/05-Arquitectura-Tecnica`):

| # | Ruta | Para qué |
|---|---|---|
| 1 | `EXP/evidencia/ev-07-cartas-despachadas/00-encargo-comun.md` | Reglas y forma del informe |
| 2 | `EXP/evidencia/ev-07-cartas-despachadas/05-V2-contrato-consumidor.md` | Mandato |
| 3 | `EXP/evidencia/ev-05-presentacion-original.md` | Qué pide el Product Owner sobre el contrato (l.17, l.36-39) |
| 4 | `<workspace>/IA/SDD/IA.SDD.Documentacion/PROMPTs/SDD/Catalogado/02-Extraccion-Concepto-Espcificacion-Integracion-Proyecto-Bundle-JS/INPUTs/Bundle-JS-En-Solucion-Blazor.md` | Documento de entrada, §2.3, §8.3, §8.4 |
| 5 | `EXP/actuaciones/001-*.md`, `002-*.md` | Contrato de entrada de la mesa |
| 6 | `EXP/evidencia/ev-02-citas.out`, `ev-03-precedente.out` | Citas ya levantadas |
| 7 | `PIN/Contrato.cs`, `PIN/ts/contrato.ts`, `PIN/ts/<componente>.ts`, `PIN/DiagramaDeCabecera.razor`, `PIN/<F1>.<Componente>.csproj`, `PIN/package.json`, `PIN/tsconfig.json`, `PIN/.gitignore`, `PIN/ts/frontera.test.ts` (cabecera) | El contrato real de los dos lados y su puente |
| 8 | `DOC05/Contratos-Abstractions-<F1>-<Componente>.md`, `DOC05/Adrs/ADR-00042-*.md`, `DOC05/Adrs/ADR-00043-*.md` | Cómo el precedente documentó el contrato |
| 9 | `<repo-privado-F1>/SDD/Maquetas/<F1>-Web/README.md` (l.52, l.159-165) y `assets/vendor/<componente>.js`, `<componente>.d.ts` | La otra superficie de consumo del precedente |
| 10 | `SDD/Rules/Rules-Base-Conocimiento.md` (completo) | Formato del documento a producir, §0.1, §4.2, §4.4, §4.5, §6.2 |
| 11 | `SDD/Intake/PRODUCT-INTAKE-template.md` §13-§14 (l.304-422); `SDD/Intake/PRODUCT-MANIFEST-template.md` l.88-101, l.140-150, l.208-224; `SDD/Rules/Intake-Rules.md` l.123-161 | Clases de contrato y de arista |
| 12 | `SDD/Rules/Rules-Arquitectura-Tecnica.md` §2.1-§2.2 (l.72-96), §3.1 (l.112), §4.5 (l.218-226), l.277 | Qué es normativo sobre `contratos-<area>.md` |
| 13 | `SDD/Rules/Rules-Examples.md` §0.1 (l.36-47), §4.6 (l.331-360); `SDD/Rules/Rules-Calidad-Y-Pruebas.md` l.18, l.35; `SDD/Rules/Deriva-Rules.md` §2.2 (l.91-104) | Dónde el framework ya pide contratos verificables |
| 14 | `<workspace>/IA/SDD/IA.SDD/Conocimiento/Index-Knowledge.md`; `Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md` §3 (l.158-227) | Referencia de forma y hermano posible |

**Qué no leí y por qué:** ningún otro informe de `actuaciones/` (regla 1: a ciegas; al momento de escribir sólo existen 001 y 002). `Knowledge-Conformacion-Pull-Request-Manual.md`: es `canonico` y de proceso, no aporta forma para un contrato entre dos entornos. El resto de los `.ts` del precedente (dibujo, normalizador, recorrido, ficha, geometría) sólo por `grep`: son implementación interna, no contrato. No corrí `dotnet build` ni `npm run build`: no hacía falta para responder la pregunta y el encargo no lo pide.

**Comandos ejecutados (E1)**, desde `PIN` y `<F1>`:

```text
$ grep -rn -i "camel\|JsonPropertyName\|JsonSerializer\|PropertyNamingPolicy" . --include=*.cs --include=*.razor --include=*.ts
(sin resultados)
$ grep -rn -i "global\|iife\|d\.ts\|declaration" package.json tsconfig*.json <F1>.<Componente>.csproj
(sin resultados)
$ ls wwwroot/
<componente>.css  <componente>.js
$ grep -n "version" ts/*.ts | grep -v test
ts/contrato.ts:114:  version: 1;
$ diff <(grep -E "^\s*(export|  [a-zA-Z?]+:|  \| )" ts/contrato.ts) <(grep -E "^\s*(export|  [a-zA-Z?]+:|  \| )" ../../SDD/Maquetas/<F1>-Web/assets/vendor/<componente>.d.ts)
36,38d35   <   cobertura?: CoberturaDelMapa;  <   export interface CoberturaDelMapa {  <   posicionesAlcanzadas: number[];
57,58c54   <   estado: EstadoDePunto | null;  <   motivoSinDato: string | null;   ---   >   estado: EstadoDePunto;
65a62,71   > export declare function montar( ... montarConInterop( ... render( ... seleccionar( ... destruir(
exit=1
$ head -c 300 SDD/Maquetas/<F1>-Web/assets/vendor/<componente>.js
/* <componente>.js — <F1>.<Componente>   Prototipo del componente. Es la forma que tendria la salida transpilada de la fuente TypeScript: sin dependencias, un global, DOM y SVG a mano. ...
$ grep -ln "<Componente>" tests/**/*.cs ; grep -n -i "json\|serializ" tests/<F1>.Web.Pruebas/VistaDeLaCabeceraPruebas.cs
(cuatro archivos de pruebas del anfitrión; ninguno serializa el contrato)
```

---

## 2. Relevamiento para el documento `Bundle-JS`

Convenciones de la tabla: «adaptador» es lo que el Product Owner llama `main.js` y el precedente resuelve como el punto de entrada del módulo (`<componente>.ts`); «bundle» es el resto del código empaquetado; «RCL» es la biblioteca de clases Razor; «app» es la aplicación Blazor que la referencia. Los nombres del precedente se citan por ruta, nunca como contenido a transmitir.

| Id | Sección destino | Qué tiene que decir | Ancla (nivel + cita) | Riesgo si falta |
|---|---|---|---|---|
| RV2-01 | §0 | Qué costuras cubre el contrato: (a) bundle↔adaptador, interna al proyecto JS; (b) adaptador↔RCL, la costura entre lenguajes; (c) RCL↔app, en C#. Qué queda afuera: cualquier contrato del bundle con servicios de red o APIs de terceros —el bundle caracterizado no hace peticiones ni conoce la red— y el contrato visual (CSS, tokens), que es de la 03 | E2 `DOC05/ADR-00043` l.40 «no hace peticiones, no accede a almacenamiento y no conoce la red»; E2 `DOC05/Contratos-Abstractions-*` §4 l.75 «Los colores no son parte de este esquema»; E4 actuación 002 §2 `fuera_de_alcance` | Sin el «qué queda afuera» de §0 el agente extiende el contrato a la red y a la presentación (`Rules-Base-Conocimiento.md` §4.2: «§0 y §8 no se negocian») |
| RV2-02 | §1 | Identidad del contrato: **contrato de componente de presentación sin borde de red**, con tres caras: operaciones de entrada (funciones exportadas del adaptador), una o más salidas (acuses) y esquemas de datos JSON. No es OpenAPI ni AsyncAPI | E2 `DOC05/Contratos-Abstractions-*` §2 l.34-43 «No hay OpenAPI ni AsyncAPI: no hay borde de red»; §3 l.45-61 (cinco operaciones y una salida) | El agente busca un formato de contrato de red y no lo encuentra; o documenta la costura como API REST |
| RV2-03 | §1 | Clase del contrato en el vocabulario del framework: **de compilación** en las dos costuras de código. Bundle→RCL es arista de clase **insumo de construcción** («la única clase posible entre proyectos de ecosistemas distintos»), con un único generador; RCL→app es **referencia de proyecto**. La cara de ejecución (JSON que cruza el circuito) no es contrato de integración en el sentido de §14, porque no une dos unidades de entrega desplegadas: es la materialización en tiempo de ejecución de un contrato de compilación, y el documento tiene que decirlo con esas palabras | E2 `SDD/Intake/PRODUCT-INTAKE-template.md` §14 l.403-409; E2 `SDD/Rules/Intake-Rules.md` l.142-145; E2 `SDD/Intake/PRODUCT-MANIFEST-template.md` l.144-150 y l.210-217 | Confundir las clases «produce un orden de build que ningún comando puede ejecutar» (`PRODUCT-MANIFEST-template.md` l.209); ver HV2-06 |
| RV2-04 | §2 | Las declaraciones del contrato viven **una vez de cada lado**, en el mismo proyecto de código, en carpetas hermanas, y **ningún archivo importa al otro**: el lado .NET declara sus tipos cerrados «sin depender de este archivo» | E2 `PIN/Contrato.cs` l.3-6; E2 `PIN/ts/contrato.ts` l.6-8; E2 `DOC05/ADR-00042` §2 punto 3 | Un lado generado desde el otro sin declararlo, o un archivo compartido que ata los dos toolchains |
| RV2-05 | §2 | Del lado JS el contrato tiene **dos piezas** y las dos son contrato: los esquemas (archivo de contrato) y la **superficie de API del adaptador** (funciones exportadas del punto de entrada). En el precedente la segunda no está declarada en el archivo de contrato: sólo existe como implementación | E2 `PIN/ts/contrato.ts` l.160-171 declara `OpcionesDeMontaje` e `Instancia<Componente>` pero ninguna función de módulo; E2 `PIN/ts/<componente>.ts` l.281-318 exporta `montar`, `montarConInterop`, `render`, `seleccionar`, `destruir` | El agente declara los DTO y deja la API sin contrato; la PoC HTML invoca funciones que no figuran en ningún lado citable |
| RV2-06 | §2 | El adaptador es **lo único que conoce a .NET**: recibe una referencia opaca con `invokeMethodAsync` y el nombre del método; el bundle es agnóstico del anfitrión y se monta también sin .NET. Es lo que el Product Owner describe como «registrar callback blazor en la clase de interfaz en main.js» | E2 `EXP/evidencia/ev-05` l.17; E2 `PIN/ts/<componente>.ts` l.296-306 (`montarConInterop` envuelve a `montar`); E2 `PIN/ts/frontera.test.ts` l.1-11 (la frontera se verifica, no sólo se escribe) | Si el bundle conoce a .NET, la PoC HTML deja de ser posible sin imitar Blazor |
| RV2-07 | §3 | Operaciones de entrada mínimas del contrato y su forma: **montar** (contenedor + opciones → instancia), **render** (una vista completa; «todo cambio es un render completo, no hay mutadores parciales»), **seleccionar** (orden del anfitrión, sin acuse), **destruir**. Todas reciben el contenedor como identidad | E2 `PIN/ts/contrato.ts` l.166-171; E2 `PIN/ts/<componente>.ts` l.281-318; E2 `DOC05/Contratos-Abstractions-*` §3 l.47-54 | Sin el conjunto mínimo, cada bundle inventa su ciclo y la RCL no tiene forma estable que invocar |
| RV2-08 | §3 | **Identidad de instancia**: la instancia se indexa por el **elemento contenedor** del lado del navegador; .NET nunca la sostiene, trabaja siempre contra la referencia al elemento. Montar sobre un contenedor ya montado destruye la instancia anterior | E2 `PIN/ts/<componente>.ts` l.40-44 («Blazor no puede sostener la instancia, así que la sostiene el módulo») y l.285; E2 `PIN/DiagramaDeCabecera.razor` l.21, l.59, l.69, l.88 | Instancias huérfanas al re-renderizar; dos componentes sobre el mismo contenedor |
| RV2-09 | §3 | **Eventos de salida**: un acuse por activación deliberada, emitido por **dos vías** —la función registrada al montar y un `CustomEvent` burbujeante con nombre prefijado— con **discriminador de tipo** en el mensaje. Recorrer no emite: cada acuse es un viaje por el circuito | E2 `PIN/ts/<componente>.ts` l.143-155; E2 `PIN/ts/contrato.ts` l.133-154 (`tipo: "punto-activado"`); E2 `DOC05/Contratos-Abstractions-*` §3 l.57-61 | La PoC HTML sólo puede escuchar la vía DOM; sin las dos vías las PoC no ejercitan lo mismo |
| RV2-10 | §3 | **Registro del callback .NET**: el nombre del método invocable se escribe **literal de los dos lados** —atributo `[JSInvokable("...")]` en la RCL y parámetro con valor por omisión en el adaptador— «porque si alguno cambia, el acuse deja de llegar y sólo se nota en ejecución» | E2 `PIN/DiagramaDeCabecera.razor` l.73-77; E2 `PIN/ts/<componente>.ts` l.299 | Nombre derivado por convención en un lado y cadena en el otro: se rompe sin error de compilación |
| RV2-11 | §3 | **Esquemas de datos**: uniones de literales de cadena del lado JS ↔ constantes `string` en clases estáticas del lado .NET, **nunca `enum`** («cruzan a JSON sin ceremonia y el conjunto lo fija el contrato y no el sistema de tipos»); records inmutables; los estados que el bundle deriva y los que recibe se distinguen en el propio tipo | E2 `PIN/Contrato.cs` l.8-10, l.20-33; E2 `PIN/ts/contrato.ts` l.6-8, l.46-56 | Un `enum` .NET cruza como número; un cambio de orden lo rompe en silencio |
| RV2-12 | §3 | **Regla de nombres a través de la costura**: propiedades PascalCase del lado .NET ↔ camelCase del lado JS. El documento debe declarar **de qué depende** esa correspondencia —la política de nombres del serializador del interop de Blazor, que por defecto emite camelCase— y **dónde se fija** en el proyecto, porque no está escrita en ningún archivo del precedente | E1 `grep` de `camel|JsonPropertyName|JsonSerializer|PropertyNamingPolicy` sobre `PIN`: sin resultados; E2 `PIN/Contrato.cs` l.48-54 vs `PIN/ts/contrato.ts` l.23-33; hecho de stack: documentación oficial de ASP.NET Core Blazor, «JavaScript interop, serialización JSON» | Ver HV2-03 |
| RV2-13 | §3 | **Ausencia y `null`**: una sola regla, declarada. Hoy el lado JS declara campos opcionales como ausentes (`?:`, con `exactOptionalPropertyTypes`) y el lado .NET los declara `= null`, que el serializador emite como `null`; la implementación lo tolera con `??`, el tipo no lo admite | E2 `PIN/ts/contrato.ts` l.32, l.90, l.118; E2 `PIN/tsconfig.json` l.9; E2 `PIN/Contrato.cs` l.54, l.94, l.112; E2 `PIN/ts/<componente>.ts` l.100, l.234 (`?? null`); E3: ver HV2-02 | Ver HV2-02 |
| RV2-14 | §3 | **Errores**: dos familias. Errores de dato: el bundle «no valida y no falla: declara» (dibuja el estado ausente con su motivo, nunca un valor plausible). Errores de anfitrión (contenedor inválido, posición inexistente): tienen código de la familia declarada en la 03, pero **hoy no tienen canal hacia el anfitrión**: van a consola. El documento tiene que exigir que el contrato **declare el canal de error hacia el anfitrión o declare su ausencia** | E2 `DOC05/Contratos-Abstractions-*` §5 l.77-88; E2 `PIN/ts/<componente>.ts` l.113-123 («se declara en la consola porque el componente no tiene canal de error propio hacia el anfitrión»); E2 `SDD/Rules/Rules-Arquitectura-Tecnica.md` §4.5 punto 5 (l.224) | Un anfitrión que no puede saber que se equivocó; el diagnóstico que el Product Owner pide (`ev-05` l.25) no tiene de dónde leer |
| RV2-15 | §3 | **Versionado**: el sobre de la vista lleva un campo de versión de contrato; la política mientras adaptador y RCL compilan juntos es «se versiona con el anfitrión, sin matriz de compatibilidad»; el documento tiene que nombrar el **disparador** que la cambia (publicar la RCL como paquete o el bundle como paquete npm) y qué hace el bundle ante una versión que no conoce | E2 `PIN/ts/contrato.ts` l.114; E2 `PIN/Contrato.cs` l.114; E2 `DOC05/Contratos-Abstractions-*` §6 l.92-96; E2 `SDD/Rules/Rules-Arquitectura-Tecnica.md` l.277 («Contrato sin versionado: cambios silenciosos rompen consumidores»); E2 `ev-05` l.38 (NuGet futuro, fuera de alcance) | Ver HV2-04 |
| RV2-16 | §3 | Lo que el contrato declara del **ciclo de vida** (sólo eso; el circuito es de AH-2): `destruir` es obligatorio, idempotente y libera escuchas; la liberación es de los dos lados y en orden —primero la instancia del navegador, después la referencia .NET—; y el contrato tolera que el navegador ya no esté | E2 `PIN/ts/<componente>.ts` l.128-141, l.285; E2 `PIN/DiagramaDeCabecera.razor` l.79-98 | Retención de escuchas y referencias vivas: es la medición que el precedente hace en su batería (`csproj` l.81-84) |
| RV2-17 | §3 | El contrato transporta **textos accesibles como entrada del anfitrión** en las opciones de montaje («el estado nunca se comunica sólo por color»); los colores no cruzan: se exponen como variables con valor por defecto | E2 `PIN/ts/contrato.ts` l.160-164; E2 `DOC05/Contratos-Abstractions-*` §4 l.70, l.75 | Un bundle con textos fijos en un idioma; ver solicitud de convocatoria SC-02 |
| RV2-18 | §4 | Bifurcación resuelta: **declaración doble a mano** frente a **generar un lado desde el otro** o **esquema compartido**. El precedente eligió la doble declaración y su criterio fue que el tipado cubra los dos lados; el costo, que la coincidencia se sostiene por disciplina. El documento presenta la bifurcación con el criterio y dice que la elección **exige** el mecanismo de detección de RV2-25 | E2 `DOC05/ADR-00042` §4 l.53-54 (alternativa «JavaScript a mano» descartada por perder el tipado del contrato); E2 `PIN/Contrato.cs` l.5-6 («el que se olvide se nota al serializar») | Ver HV2-01 |
| RV2-19 | §4 | Bifurcación resuelta: **render completo** frente a mutadores parciales. Criterio: una sola operación de entrada de datos, sin estado parcial que sincronizar; costo: cada cambio viaja entero | E2 `PIN/ts/contrato.ts` l.167; E2 `PIN/ts/<componente>.ts` l.73-105 | Ver HV2-05 para la contradicción con la 05 del precedente |
| RV2-20 | §4 | Bifurcación resuelta: **el acuse siempre se emite**, también sobre un punto sin dato, con el estado declarado ausente (`null`) y su motivo, nunca con «el más parecido» | E2 `PIN/ts/<componente>.ts` l.208-214; E2 `PIN/Contrato.cs` l.133-140; E2 `PIN/ts/contrato.ts` l.137-148 | Un contrato que calla ante el caso borde deja a la persona sin respuesta y al diagnóstico sin señal |
| RV2-21 | §4 | Bifurcación resuelta: **la instancia la sostiene el navegador, no .NET** (identidad por contenedor). Alternativa descartada: devolver un `IJSObjectReference` por instancia y sostenerlo del lado servidor | E2 `PIN/ts/<componente>.ts` l.40-44; E2 `PIN/DiagramaDeCabecera.razor` l.25-26 (sólo se sostienen el módulo y la referencia .NET) | Ver RV2-08 |
| RV2-22 | §5 | Esqueleto mínimo del **par de declaraciones**, ofuscado: un tipo cerrado (unión de literales ↔ clase de constantes), un DTO de entrada con un campo opcional, un sobre con `version`, un mensaje de salida con discriminador `tipo`, y las opciones de montaje con el callback. Los dos archivos, lado a lado, con el comentario de cabecera que explica por qué no se importan | E2 `PIN/Contrato.cs` l.1-10, l.44-61, l.108-115, l.141-154; E2 `PIN/ts/contrato.ts` l.15-40, l.113-119, l.133-154, l.160-164; E2 `SDD/Rules/Rules-Base-Conocimiento.md` §4.2 fila §5 y §6.2 (techo 600) | Sin esqueleto, un documento `propio` no cumple su fin (`Rules-Base-Conocimiento.md` §6.2: «hay que darle identidad, estructura, contrato y esqueletos») |
| RV2-23 | §5 | Esqueleto mínimo del **puente**: del lado JS, la función que envuelve al montaje agnóstico con `refDotNet.invokeMethodAsync(nombre, acuse)`; del lado Razor, `import` del módulo, `DotNetObjectReference.Create(this)`, `[JSInvokable("<nombre>")]` y la liberación en `DisposeAsync` | E2 `PIN/ts/<componente>.ts` l.291-306; E2 `PIN/DiagramaDeCabecera.razor` l.52-98 | Ver RV2-10 |
| RV2-24 | §5 | Esqueleto de **fixture JSON compartido**: el mismo documento de vista que Blazor serializa es el que la PoC HTML carga; el mismo acuse que el bundle emite es el que la PoC Blazor recibe. Es el artefacto que hace que las dos PoC ejerciten **el mismo contrato desde lados distintos**, sin partir el sample en dos versiones | E2 `SDD/Rules/Rules-Examples.md` §0.1 l.47 («prohibido partir el sample en una versión ilustrativa y otra verificable»); E2 `DOC05/ADR-00043` §8 l.78 («Diferencia de comportamiento entre las dos superficies: ninguna — la batería corre contra las dos») | Dos PoC que pasan con contratos distintos |
| RV2-25 | §6 | Criterio: existe una **verificación ejecutable de la correspondencia** entre las dos declaraciones —serializar desde .NET con las mismas opciones del interop y validar contra el tipo JS o contra el fixture de RV2-24— con criterio de aceptación evaluable. Hoy el precedente **no la tiene** | E1 `grep` de `json|serializ` sobre las pruebas del anfitrión: sin resultados; E2 `SDD/Rules/Rules-Examples.md` §4.6 l.331-345 (`criterio_aceptacion`: «aserción evaluable, no prosa»); E2 `SDD/Rules/Rules-Calidad-Y-Pruebas.md` l.35 («contract tests por interfaz» para `library`) | Ver HV2-01 |
| RV2-26 | §6 | Criterio: la PoC HTML consume **el mismo artefacto** que la RCL carga —el archivo que emite la construcción—, nunca una copia «de forma equivalente» ni un prototipo | E1 `head` de `assets/vendor/<componente>.js`: «Prototipo del componente. Es la forma que tendría la salida transpilada»; E2 `SDD/Maquetas/<F1>-Web/README.md` l.163 («forma equivalente a la salida `<componente>.global.js`»); E2 `DOC05/ADR-00043` §8 l.80 («Copias manuales del bundle en el árbol: cero») | Ver HV2-07 |
| RV2-27 | §6 | Criterio: el nombre del método invocable y los nombres de las operaciones aparecen **literalmente** de los dos lados y son localizables por búsqueda de texto; el documento da el comando | E2 `PIN/DiagramaDeCabecera.razor` l.76 y `PIN/ts/<componente>.ts` l.299 comparten la cadena literal | Ver RV2-10 |
| RV2-28 | §6 | Criterio: el campo de versión del sobre **se lee** del lado JS y el comportamiento ante una versión desconocida está declarado y probado | E1 `grep "version" ts/*.ts` sin tests: sólo la declaración `contrato.ts:114`; ninguna lectura en `<componente>.ts` | Ver HV2-04 |
| RV2-29 | §6 | Criterio: **no existe una tercera copia** del contrato en el árbol (`.d.ts` «de consulta», prototipo, copia en una maqueta o sample); si un consumidor necesita las declaraciones, las **genera** la misma construcción | E1 `diff` entre `ts/contrato.ts` y `assets/vendor/<componente>.d.ts`: difieren en `cobertura`, en `estado: … \| null` y en `motivoSinDato`; E2 `PIN/ts/contrato.ts` l.4 declara ese `.d.ts` como «referencia normativa»; E2 `PIN/tsconfig.json` l.10 (`noEmit`, sin `declaration`) | Ver HV2-05 |
| RV2-30 | §7 | Anti-patrón: **confiar en «compila junto» como garantía de la costura**. C# y TypeScript no compilan uno contra el otro; la coincidencia sólo se nota al serializar | E2 `PIN/Contrato.cs` l.5-6 frente a `DOC05/ADR-00042` §5 punto 2 y `DOC05/Contratos-Abstractions-*` §1 l.30 y §6 l.94 | Ver HV2-01 |
| RV2-31 | §7 | Anti-patrón: **`enum` de .NET cruzando a JSON** en lugar de cadenas de conjunto cerrado | E2 `PIN/Contrato.cs` l.8-10 | Ver RV2-11 |
| RV2-32 | §7 | Anti-patrón: **tercera copia del contrato** como «referencia normativa» que ninguna construcción produce | E1 `diff` de RV2-29 | Deriva medida en el propio precedente |
| RV2-33 | §7 | Anti-patrón: **emitir hacia .NET en cada interacción** (recorrido, hover): sólo cruzan la costura los eventos deliberados, porque cada uno es un viaje por el circuito | E2 `DOC05/Contratos-Abstractions-*` §3 l.59-61 («recorrer cuarenta puntos con las flechas serían cuarenta viajes»); E2 `PIN/ts/<componente>.ts` l.169-181 | Un contrato que funciona en la PoC HTML y se degrada en la PoC Blazor |
| RV2-34 | §7 | Anti-patrón: **canal de error por consola** como único canal hacia el anfitrión | E2 `PIN/ts/<componente>.ts` l.113-123 | Ver RV2-14 |
| RV2-35 | §8 | Qué es normativo y el documento **no redefine**: las siete secciones obligatorias de `contratos-<area>.md` (alcance, formato, operaciones, esquemas, errores, versionado, trazabilidad) y su obligatoriedad por D8 (`library` → `Contratos-Abstractions.md`; `web-monolith` → «solo si expone API externa»). El documento dice **qué contiene** el contrato del bundle, no dónde lo documenta el framework destino ni bajo qué categoría | E2 `SDD/Rules/Rules-Arquitectura-Tecnica.md` §4.5 l.218-226, §2.2 l.86-89, §3.1 l.112; E2 `SDD/Rules/Rules-Base-Conocimiento.md` §0.1 l.51-56 y §4.5 l.289 («conocimiento disfrazado de regla»); E4 R2 de actuación 002 §2 | El documento dicta estructura al framework destino, que es la restricción de fondo del Product Owner (`ev-05` l.35, l.41) |
| RV2-36 | §8 | Las **clases de contrato** (§14: integración, compilación) y las **clases de arista** (Intake-Rules §4: referencia de proyecto, insumo de construcción) son del framework: el documento las **cita** para clasificar la costura y no las reformula. Declara como hueco que ninguna de las dos clases nombra la cara de ejecución de una costura entre lenguajes dentro de una misma unidad de entrega | E2 `SDD/Intake/PRODUCT-INTAKE-template.md` §14 l.403-409; E2 `SDD/Rules/Intake-Rules.md` l.142-145; ausencia verificada por lectura de §14 y §2.2 de `Rules-Arquitectura-Tecnica.md` | Ver HV2-06 |
| RV2-37 | §9 | Hermano a citar, no repetir: `Template-Blazor-Interactive-Server-SDD-Default` §3.2 ya fija «comunicación hijo a padre: `EventCallback`» y «`IAsyncDisposable` en todo componente que…»; el documento `Bundle-JS` remite a eso para el lado RCL↔app y sólo agrega lo que cruza a JS. Precedente citado ofuscado | E2 `Conocimiento/Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md` l.189, l.191; E2 `SDD/Rules/Rules-Base-Conocimiento.md` §4.4 «Un hecho, un lugar»; E4 R4 de actuación 002 §2 | Duplicación que deriva; nombres de cliente en un repositorio público |

---

## 3. Hallazgos

| Id | Ubicación | Afirmación | Evidencia (nivel + ancla) | Severidad | Confianza | Impacto si no se corrige | Dirección propuesta |
|---|---|---|---|---|---|---|---|
| HV2-01 | `DOC05/ADR-00042` §5 punto 2; `DOC05/Contratos-Abstractions-<F1>-<Componente>.md` §1 l.30 y §6 l.94; `EXP/…/INPUTs/Bundle-JS-En-Solucion-Blazor.md` §8.3 l.251-253 (lo hereda al llamarlo «contrato de compilación» sin decir qué lo verifica) | El precedente afirma que la costura entre C# y TypeScript **«no compila si los tipos dejaron de coincidir»**. Es falso: los dos lados no compilan uno contra el otro; `tsconfig.json` tiene `noEmit` y no emite declaraciones, el `csproj` no consume ninguna, y ninguna prueba serializa el contrato. El propio `Contrato.cs` lo reconoce: «el que se olvide se nota al serializar» | E2 `PIN/Contrato.cs` l.5-6; E2 `PIN/tsconfig.json` l.10; E1 `grep` de `JsonSerializer|camel|…` sobre `PIN` y de `json|serializ` sobre `tests/<F1>.Web.Pruebas/*.cs`: sin resultados; E2 `DOC05/ADR-00042` l.61; E2 `Contratos-Abstractions-*` l.30, l.94 | S1 | 0,95 | El documento transmitiría a otros frameworks que la doble declaración está guardada por el compilador. Los agentes que «diagnostican fallas de contrato» (`ev-05` l.25) buscarían un error de build que nunca va a aparecer; la falla sólo se ve en ejecución y sin mensaje | El documento declara en §3 y §7 que la doble declaración es **una convención sin guardia mecánica**, y en §6 exige una verificación ejecutable de correspondencia (RV2-25): fixture JSON producido por el serializador del interop y validado por el lado JS, o equivalente, con `criterio_aceptacion` evaluable. El precedente **no se modifica** (R1, R5): se cita como forma, con la deuda declarada |
| HV2-02 | `PIN/ts/contrato.ts` l.32, l.90, l.118 (`?:`) con `PIN/tsconfig.json` l.9 (`exactOptionalPropertyTypes`) frente a `PIN/Contrato.cs` l.54, l.94, l.112 (`= null`) | Las dos declaraciones **no son isomorfas en ausencia y `null`**: el lado JS declara «ausente» y no admite `null`; el lado .NET declara `null` y el serializador del interop lo emite como `null` por defecto. La implementación lo tolera con `?? null` (`<componente>.ts` l.100, l.234) y con un chequeo de veracidad (`normalizador.ts` l.124), pero el tipo declarado miente. **Contraejemplo (E3):** una PoC HTML escrita contra `contrato.ts` construye `{ posicion: 1, … }` sin `linea` y pasa; un consumidor JS que siga el tipo y escriba `if (p.linea !== undefined)` recibe de Blazor `"linea": null` y trata `null` como presente | E2 citadas; E3 construido; hecho de stack: documentación oficial de ASP.NET Core Blazor, «JavaScript interop, serialización JSON» (System.Text.Json, nulos incluidos salvo configuración) | S2 | 0,85 | Las dos PoC ejercitan **formas distintas del mismo campo** y las dos pasan; el defecto aparece en el primer consumidor que confíe en el tipo | El documento fija **una** regla y la escribe en §3: o el lado JS declara `T \| null` en todo campo que .NET pueda emitir nulo, o la RCL configura el serializador para omitir nulos; y el fixture de RV2-24 incluye los dos casos (campo ausente y campo nulo) |
| HV2-03 | `PIN/Contrato.cs` l.48-54 (PascalCase) frente a `PIN/ts/contrato.ts` l.23-33 (camelCase); ningún archivo del precedente declara la política | La correspondencia de nombres a través de la costura **depende de la política por defecto del serializador del interop** y no está escrita en ningún lado del precedente ni del documento de entrada. Un proyecto que registre opciones propias de serialización, o que use atributos de nombre en un solo lado, rompe la costura sin cambiar ninguna declaración | E1 `grep -rn -i "camel\|JsonPropertyName\|JsonSerializer\|PropertyNamingPolicy"` sobre `PIN`: sin resultados; E2 citadas; hecho de stack: documentación oficial de ASP.NET Core Blazor, «JavaScript interop, serialización JSON» | S2 | 0,9 | Otro framework con convenciones distintas (nombres explícitos, PascalCase en JSON) aplica el documento y las PoC fallan en el primer campo | §3 del documento declara la regla de nombres, de qué mecanismo depende y dónde se fija (RV2-12); §6 la verifica con el fixture |
| HV2-04 | `PIN/ts/contrato.ts` l.114; `PIN/Contrato.cs` l.114; `PIN/ts/<componente>.ts` (sin lectura de `version`); `DOC05/Contratos-Abstractions-*` §6 | El campo `version` del sobre **está declarado y nunca se lee**: ningún archivo del módulo lo consulta, ningún test lo ejercita con un valor distinto. La política «se versiona con el producto, sin matriz» es correcta mientras adaptador y RCL compilan juntos, pero el original prevé convertir la RCL en NuGet (`ev-05` l.38) y el documento va a otros frameworks: **el disparador que cambia la política no está nombrado** | E1 `grep -n "version" ts/*.ts \| grep -v test` → sólo `contrato.ts:114`; E2 `Contratos-Abstractions-*` l.92-96; E2 `SDD/Rules/Rules-Arquitectura-Tecnica.md` l.225 y l.277 | S3 | 0,9 | Un campo de versión decorativo enseña a versionar sin versionar; cuando la RCL o el bundle se publiquen aparte, no hay comportamiento ante versión desconocida | §3: el módulo lee `version` y declara (no dibuja, informa) ante una que no conoce; §4: política «con el anfitrión» y su disparador de cambio; §6: criterio RV2-28 |
| HV2-05 | `DOC05/Contratos-Abstractions-<F1>-<Componente>.md` §3 fila 3 y §5 fila 4; `PIN/ts/contrato.ts` l.4 y l.167; `SDD/Maquetas/<F1>-Web/assets/vendor/<componente>.d.ts` | El contrato del precedente está declarado en **cuatro lugares que no coinciden**: (a) la 05 lista una operación «Actualizar la clasificación… no rehace el dibujo» que el código no expone (sólo `montar`, `render`, `seleccionar`, `destruir`) y que `contrato.ts` niega («no hay mutadores parciales»); (b) la 05 dice «contenedor ausente → excepción» y el código hace no-op silencioso (`instancias.get(contenedor)?.render`); (c) `contrato.ts` nombra como «referencia normativa» un `<componente>.d.ts` que ninguna construcción produce y cuya única copia, en la maqueta, difiere de él; (d) la API del módulo sólo está en la implementación | E2 `Contratos-Abstractions-*` l.51, l.86; E2 `PIN/ts/<componente>.ts` l.308-318; E2 `PIN/ts/contrato.ts` l.4, l.167; E1 `diff` (sección 1 de este informe): `cobertura`, `estado \| null`, `motivoSinDato` y las cinco funciones; E2 `PIN/tsconfig.json` (sin `declaration`) | S2 | 0,9 | Si el documento toma la 05 del precedente como forma del contrato, transmite operaciones que no existen y un `.d.ts` normativo que nadie genera. Viola «un hecho, un lugar» (`Rules-Base-Conocimiento.md` §4.4) | El documento fija en §2 la **fuente de verdad**: las dos declaraciones en código; la 05 del framework destino **enumera y referencia**, no redeclara (`Contratos-Abstractions-*` l.43 ya lo dice: «la fuente de verdad es el código»); toda copia adicional se genera en la misma construcción o no existe (RV2-29, RV2-32) |
| HV2-06 | `SDD/Intake/PRODUCT-INTAKE-template.md` §14 l.403-409; `SDD/Rules/Rules-Arquitectura-Tecnica.md` §2.2 l.87 (`web-monolith`: contratos «solo si expone API externa»); documento de entrada §8.3 l.251-253 y §8.4 l.288-289; carta V2 («integración (RCL↔app)») | **El framework no tiene clase para la costura que se rompe en ejecución.** Por §14, bundle→RCL y RCL→app son contratos **de compilación** (arista «insumo de construcción» la primera, referencia de proyecto la segunda); la cara JSON sobre el circuito no une unidades de entrega, así que no es «de integración». Consecuencias: (i) en el caso A del documento de entrada (`web-monolith`) el contrato JS **no tiene documento obligatorio** en la 05; (ii) el documento de entrada §8.4 dice «entre el workspace npm y la RCL: consumo de artefacto publicado, no arista», y 13.17 dice lo contrario: es arista de compilación de clase insumo de construcción, «la única posible entre ecosistemas distintos», y el paquete «pertenece a la solución de código de su consumidor»; (iii) la carta llama «de integración» a RCL↔app, que en el sentido de §14 no lo es | E2 citadas; E2 `SDD/Rules/Intake-Rules.md` l.142-145, l.154-155; E2 `SDD/Intake/PRODUCT-MANIFEST-template.md` l.100, l.144-150 | S2 | 0,85 | El documento clasificaría mal la costura y, con ella, dónde se documenta y qué la verifica; un agente en caso A no produciría contrato alguno porque su D8 no lo exige | §1 y §8 del documento: la costura es contrato de compilación de clase insumo de construcción con una cara de ejecución que el documento describe como contenido (las siete secciones de §4.5) sin fijarle categoría ni archivo; el hueco de clase se declara en §8 como tal. La corrección del §8.4 del documento de entrada la clasifico como hallazgo de V1 en cuanto a estructura; acá sólo su efecto sobre la clase de contrato |
| HV2-07 | `DOC05/ADR-00043` §2 y §7 («dos superficies… generadas en la misma compilación»; «no hay copia manual»); `PIN/package.json` l.37 (`--format=esm` únicamente); `PIN/wwwroot/` (sólo `<componente>.js` y `<componente>.css`); `SDD/Maquetas/<F1>-Web/assets/vendor/<componente>.js` l.1-6 y `README.md` l.163 | La «PoC HTML» del precedente (la maqueta) **no consume el artefacto que Blazor carga**: la construcción emite una sola superficie (módulo ES) y lo que la maqueta carga es un **prototipo escrito a mano el 2026-08-10**, «forma equivalente» a una salida global que no existe. El propio `diff` del `.d.ts` que lo acompaña muestra que ya divergió. Es exactamente la falla que el documento de entrada §8.3 describe: «si divergen, la PoC HTML deja de probar lo que usa Blazor» | E1 `grep -i "global\|iife"` sobre `package.json`, `tsconfig*.json`, `csproj`: sin resultados; E1 `ls wwwroot/`; E1 `head -c 300 assets/vendor/<componente>.js`; E1 `diff` de RV2-29; E2 `ADR-00043` l.35-38, l.69-71, l.80 | S1 | 0,95 | Si el documento presenta la forma del precedente como «PoC HTML», enseña una PoC que valida un componente que no es el que corre. El objetivo del Product Owner (una PoC «útil para los agentes que luego van a implementar… dentro de la librería», `ev-05` l.37) se pierde | §6 criterio RV2-26 y §7: la PoC HTML importa **el mismo archivo** emitido por la construcción (módulo ES vía `<script type="module">`, servido por un servidor estático) o, si se exige una segunda superficie, ésta sale de la misma construcción y una prueba corre contra las dos (métrica de `ADR-00043` §8). Copias «equivalentes», prohibidas. La decisión entre módulo servido y global desde archivo depende de PPO-01 |

---

## 4. Lo que revisé y está bien

| # | Qué | Ancla |
|---|---|---|
| OK-01 | **La doble declaración con tipos cerrados como cadenas y su motivo escrito en los dos archivos.** Uniones de literales del lado JS, clases estáticas de constantes `string` del lado .NET, records inmutables, y el porqué («cruzan a JSON sin ceremonia… el conjunto lo fija el contrato y no el sistema de tipos») en la cabecera de cada archivo. Es la forma que el documento tiene que transmitir, con la salvedad de HV2-01 | E2 `PIN/Contrato.cs` l.3-10; `PIN/ts/contrato.ts` l.1-9 |
| OK-02 | **El nombre del callback .NET es literal de los dos lados y explica por qué.** `[JSInvokable("AlActivarPunto")]` en la RCL y `nombreDelMetodo = "AlActivarPunto"` como parámetro con valor por omisión en el adaptador, con el comentario «si alguno cambia, el acuse deja de llegar y sólo se nota en ejecución». Es la única guardia real de la costura y está bien puesta | E2 `PIN/DiagramaDeCabecera.razor` l.73-77; `PIN/ts/<componente>.ts` l.296-306 |
| OK-03 | **La identidad de instancia por contenedor y el adaptador como único punto que conoce a .NET.** El módulo sostiene las instancias en un `WeakMap` por elemento, .NET trabaja siempre contra la referencia al elemento, `montar` destruye lo anterior, y `montarConInterop` es una envoltura de `montar` que sólo agrega el puente. La frontera está además verificada por una batería que busca los nombres prohibidos | E2 `PIN/ts/<componente>.ts` l.40-44, l.281-306; `PIN/ts/frontera.test.ts` l.1-11 |

---

## 5. Solicitudes de convocatoria

| Id | Rol faltante | Señal, con ubicación | Qué no puedo afirmar sin él |
|---|---|---|---|
| SC-01 | **Seguridad** (actuación 002 §4.2 lo dejó para el refutador) | `ev-05` l.17 nombra integrar «apis javascript externas (como es el caso de googlemaps)»; el precedente no tiene ese caso (`ADR-00043` l.40: no conoce la red) y su contrato de montaje sólo transporta callback y textos (`contrato.ts` l.160-164) | Si el contrato de montaje puede o no transportar credenciales de terceros hacia el navegador, y en qué forma. Sin ese rol, el documento sólo puede declarar en §0 que las integraciones de red del bundle quedan afuera |
| SC-02 | **Accesibilidad / uso** (actuación 002 §4.2, nota de reconvocatoria en ciclo 2) | El contrato del precedente transporta **textos accesibles** como entrada del anfitrión (`contrato.ts` l.160-164; `Contratos-Abstractions-*` l.70) y la 05 fija «el estado nunca se comunica sólo por color» | Si esos textos son contrato obligatorio del bundle (y por lo tanto contenido de §3 del documento) o convención del producto de origen. La condición de reconvocatoria de la providencia se cumple: el contrato fija algo de foco y teclado |

---

## 6. Preguntas al Product Owner

| Id | Origen (`C`) | Pregunta cerrada |
|---|---|---|
| PPO-01 | `C`: el original dice que la PoC HTML invoca el bundle «como lo haría Blazor, sin .NET» (`ev-05` l.37) pero no dice **desde dónde se abre**. Los navegadores no cargan módulos ES desde el origen de archivo local (estándar de industria: módulos ES y política de origen de los navegadores; se nombra, no se enlaza). Del modo de apertura depende si hace falta una segunda superficie de consumo (HV2-07) y qué ejercita la PoC HTML | ¿La PoC HTML debe (a) **abrirse desde el sistema de archivos sin servidor**, lo que obliga a emitir una segunda superficie (global) desde la misma construcción y a probar las dos, o (b) **servirse por un servidor estático mínimo**, lo que permite que importe **el mismo módulo ES** que la RCL carga y no exista segunda superficie? |
| PPO-02 | `C`: el original habla de «main.js» y «bundle javascript» como **dos artefactos** (`ev-05` l.36); el precedente emite **uno** (el módulo, cuyo punto de entrada cumple el rol de adaptador). No puedo afirmar si «dos artefactos» es un requisito del contrato o una descripción | Para el contrato, ¿el adaptador (`main.js`) debe ser (a) **un archivo emitido aparte** del bundle, con su propia superficie de importación desde la RCL, o (b) **el punto de entrada del único bundle**, exportando la superficie de API y el puente a .NET? |

Sigue: consolidación · presidente de mesa
