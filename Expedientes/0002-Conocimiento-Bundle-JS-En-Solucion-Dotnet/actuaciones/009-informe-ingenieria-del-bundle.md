# Actuación 009 — Informe Agente ad hoc AH-1, Ingeniería del bundle JavaScript/TypeScript

| Campo | Valor |
|---|---|
| Tipo | `informe` |
| Fecha | 2026-09-13 |
| Autor | Comisión AH-1, Ingeniería del bundle JavaScript/TypeScript |
| Corrige | — |

Base: `IA.SDD` `a501857` (13.17) · `<F1>` `9aabe5c`. Abreviaturas de ruta usadas abajo: `PM/` = `<repo-privado-F1>/src/<F1>.<Componente>/`; `<F1>-SDD/` = `<repo-privado-F1>/SDD/`; `Rules/` = `<workspace>/IA/SDD/IA.SDD/SDD/Devs/Rules/`; `INPUT` = `<workspace>/IA/SDD/IA.SDD.Documentacion/PROMPTs/SDD/Catalogado/02-Extraccion-Concepto-Espcificacion-Integracion-Proyecto-Bundle-JS/INPUTs/Bundle-JS-En-Solucion-Blazor.md`; `EXP/` = `<workspace>/IA/SDD/IA.SDD/Expedientes/0002-Conocimiento-Bundle-JS-En-Solucion-Dotnet/`.

## 1. Mandato y método

**Mandato** (`EXP/evidencia/ev-07-cartas-despachadas/07-AH1-ingenieria-de-bundle.md`): layout del proyecto JS/TS, empaquetador, formatos de salida, gestión de instancias por contenedor, ciclo de vida montar/render/destruir, pruebas con el runner del runtime; y aclarar qué designa «main.js» en el pedido. Fuera: contrato de datos (V2), cadena MSBuild (V3), API de interoperabilidad de Blazor (AH-2).

**Qué leí, en orden:**

| # | Ruta | Para qué |
|---|---|---|
| 1 | `EXP/evidencia/ev-07-cartas-despachadas/00-encargo-comun.md` | Reglas y forma |
| 2 | `EXP/evidencia/ev-07-cartas-despachadas/07-AH1-ingenieria-de-bundle.md` | Carta de mandato |
| 3 | `EXP/evidencia/ev-05-presentacion-original.md` | Qué pide el Product Owner, literal |
| 4 | `INPUT` (v0.2) | Análisis previo |
| 5 | `EXP/actuaciones/001-*.md`, `002-*.md`; `EXP/evidencia/ev-02-citas.out`, `ev-03-precedente.out` | Contrato de entrada y evidencia levantada |
| 6 | `Rules/Rules-Base-Conocimiento.md` (completo) | Formato del documento a producir, §4.2 para las secciones destino |
| 7 | `PM/ts/<componente>.ts`, `PM/ts/contrato.ts`, `PM/DiagramaDeCabecera.razor`, `PM/<F1>.<Componente>.csproj`, `PM/package.json`, `PM/tsconfig.json`, `PM/tsconfig.pruebas.json`, `PM/.gitignore`, `PM/ts/componente.test.ts`, `PM/ts/frontera.test.ts`, `PM/ts/retencion.test.ts` | Precedente: forma del proyecto, superficie, ciclo de vida, pruebas sin anfitrión |
| 8 | `<F1>-SDD/Docs/05-Arquitectura-Tecnica/Adrs/ADR-00042-*.md`, `ADR-00043-*.md`, `<F1>-SDD/Docs/05-Arquitectura-Tecnica/Contratos-Abstractions-<F1>-<Componente>.md`, `<F1>-SDD/Maquetas/<F1>-Web/README.md` (§2 y filas 159-165), `<F1>-SDD/Maquetas/<F1>-Web/assets/vendor/<componente>.js` (cabecera) | Decisiones del precedente y cómo lo consume su maqueta |
| 9 | `Rules/Rules-Examples.md` §0, §0.1, §0.2, §3.6, §4.5; `Rules/Rules-Devops.md` §4.9; `Rules/Intake-Rules.md` §4 (l.123-163); `Rules/Maqueta-Rules.md` §4.1; `SDD/Devs/Guides/Coherencia-Proyecto-De-Otro-Ecosistema.md`; `PRODUCT-MANIFEST-template.md` §1.2 | Qué dice el framework 13.16/13.17 sobre proyectos de otro ecosistema y sobre el «anfitrión mínimo» |
| 10 | `Conocimiento/Index-Knowledge.md`, cabecera y §0 de `Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md` | Forma de referencia |

**Corridas E1** (copia del precedente en el scratchpad de sesión, sin tocar el original): `npm ci` (5 paquetes), `npm run build` (emite un solo archivo, `wwwroot/<componente>.js`, 9.977 bytes, 0 `import`, 17 exportaciones), `npm test` (83/83 con el `.csproj` presente; 82/83 sin él: `frontera.test.ts` lo lee), y `esbuild ts/<componente>.ts --bundle --format=iife --global-name=<Componente>Global` sobre la misma fuente, que emite el global sin cambiar el código (E3). Comandos y salidas en §3.

**Qué no leí y por qué:** ningún otro informe de `EXP/actuaciones/` (regla 1; al despachar sólo existían 001 y 002). `Rules-Calidad-Y-Pruebas.md` sólo lo barrí por `grep` (no nombra ecosistema, runner ni ubicación de pruebas de otro ecosistema: cero ocurrencias de «ecosistema», «runner», «sin anfitrión»). `Master-Prompt.md` no lo abrí: la cadena de generación no es mi competencia. `Knowledge-Conformacion-Pull-Request-Manual.md` no lo abrí: la forma la tomé del otro documento de referencia y del archivo de reglas.

## 2. Relevamiento para el documento `Bundle-JS`

Sección destino según `Rules-Base-Conocimiento.md` §4.2. Nombres del precedente citados sólo como ancla; el documento los ofusca.

| Id | Sección | Qué tiene que decir | Ancla (nivel + cita) | Riesgo si falta |
|---|---|---|---|---|
| RAH1-01 | §0 | El artefacto caracterizado es **el proyecto de código del bundle**: fuentes, cadena de herramientas, punto de entrada, salidas y pruebas propias. Afuera, con su lugar: la forma de los DTO (contrato, V2), la cadena de construcción del anfitrión .NET (V3), la API de interop y la RCL (AH-2), y **dónde se ubican PoC y pruebas en el destino** (lo fija el intake del destino, `PRODUCT-INTAKE-template.md` §16). | E4 002 §2 `restricciones_duras` R2; E2 `Rules/Rules-Base-Conocimiento.md` l.248 («qué queda explícitamente afuera, y en qué categoría vive») | Sin frontera, el documento dicta estructura de solución .NET, que es la restricción R2 |
| RAH1-02 | §0 | **Vocabulario fijado** para las cuatro cosas que el pedido mezcla bajo «main.js» y «bundle»: (a) *punto de entrada*: el archivo fuente que el empaquetador toma como raíz; (b) *bundle* o *módulo*: el artefacto construido, uno por superficie de salida; (c) *superficie de interop*: las funciones del módulo que el anfitrión Blazor invoca y el adaptador que traduce la referencia .NET a un callback; (d) *guion anfitrión* de la PoC HTML: el script de la página que **imita al consumidor** y no es artefacto del bundle. Ver HAH1-01. | E2 `EXP/evidencia/ev-05-presentacion-original.md` l.17 («llaman a través de métodos de un main.js a un bundle javascript ... registrar callback blazor en la clase de interfaz en main.js»); E2 `INPUT` l.254 («El `main.js` de la PoC HTML tiene que imitar las invocaciones que hace la RCL»); E2 `PM/ts/<componente>.ts` l.2 («punto de entrada del componente») y l.296-306 (`montarConInterop`: el adaptador vive dentro del mismo módulo); E2 `Rules/Rules-Examples.md` l.220-221 («doble del consumidor») | Dos agentes leen «main.js» y construyen cosas distintas: uno un segundo artefacto del bundle, otro el script de la PoC |
| RAH1-03 | §1 | Identidad: proyecto de código del ecosistema npm, fuente TypeScript, **sin dependencias de ejecución** (sólo de construcción), `private`, tipo módulo. El runtime de JavaScript es **requisito de construcción y no de ejecución** del producto. | E2 `PM/package.json` l.3-5 (`"private": true`, `"type": "module"`), l.11-15 (tres devDependencies, cero dependencies); E2 `PM/<F1>.<Componente>.csproj` l.26-29 («requisito de construccion y no de ejecucion»); E1 `npm ci` → «added 5 packages» | Un agente agrega dependencias de ejecución al bundle y el artefacto deja de ser autocontenido |
| RAH1-04 | §2 | Layout mínimo del proyecto: carpeta de fuentes con las pruebas **al lado de cada módulo** (`x.ts` / `x.test.ts`), carpeta de estilos versionada, `package.json` + archivo de bloqueo, dos `tsconfig` (uno `noEmit` para tipar el bundle, otro que emite a una carpeta no versionada para el runner), carpeta de salida **no versionada**. El documento presenta esta ubicación de pruebas como decisión del precedente (excepción declarada en su intake), no como norma. | E2 `EXP/evidencia/ev-03-precedente.out` l.2-27 (árbol); E2 `PM/.gitignore` (`node_modules/`, `wwwroot/<componente>.js`, `wwwroot/<componente>.css`, `build/`); E2 `PM/tsconfig.pruebas.json` l.2 («Este archivo es el unico que emite, y solo hacia `build/`, que no se versiona ni se empaqueta»); E2 ADR-00042 §7 l.77 («Las pruebas de este proyecto viven junto a la fuente y no en `/tests/`: es una excepción ... declarada en §16 del intake») | Sin el par de `tsconfig`, o se empaqueta el código de pruebas o el runner no tiene JavaScript que correr |
| RAH1-05 | §2 | **Un solo punto de entrada** y qué exporta: la superficie pública del contrato y nada más. El precedente reexporta todos sus módulos internos (`export * from`), con lo que la superficie construida (17 nombres) excede el contrato (5 operaciones). Ver HAH1-04. | E2 `PM/ts/<componente>.ts` l.32-37 (`export type * from "./contrato.js"; export * from "./geometria.js"; ...`); E1 `grep -o "export{[^}]*}" wwwroot/<componente>.js \| tr ',' '\n' \| wc -l` → `17`; E2 `<F1>-SDD/.../Contratos-Abstractions-<F1>-<Componente>.md` §3 (cinco operaciones) | El anfitrión empieza a invocar internos; el contrato deja de ser el que se prueba |
| RAH1-06 | §2 | Qué es cada archivo emitido: el módulo JavaScript, la hoja de estilos (se copia, no se empaqueta) y —si se decide— la superficie global. **Ningún artefacto emitido se versiona ni se mantiene a mano**, incluida cualquier declaración de tipos: el precedente cita un `.d.ts` «normativo» que ningún paso construye. | E2 ADR-00042 §2 l.41 («El bundle no se versiona: se construye en cada compilación»); E2 `PM/ts/contrato.ts` l.4-5 («Se corresponde con `<componente>.d.ts`, que es la referencia normativa»); E1 `grep -n "declaration\|\.d\.ts" package.json tsconfig*.json` → sin ocurrencias; E2 `<F1>-SDD/Maquetas/<F1>-Web/README.md` l.165 (`<componente>.d.ts` ... 2026-08-10) | Dos fuentes del contrato (`contrato.ts` y un `.d.ts` a mano) que derivan en silencio |
| RAH1-07 | §3 | **Registro de instancias por contenedor.** El módulo guarda la instancia en un mapa débil indexado por el elemento; el anfitrión nunca recibe ni sostiene la instancia y toda función pública toma el contenedor como primer argumento. | E2 `PM/ts/<componente>.ts` l.39-44 («Las instancias se indexan por contenedor y no se guardan del lado .NET»), l.308-318 (`render(contenedor, ...)`, `seleccionar(contenedor, ...)`, `destruir(contenedor)`) | El anfitrión intenta guardar un objeto JS del lado servidor, que Blazor no puede sostener |
| RAH1-08 | §3 | **Ciclo de vida de tres verbos y sus invariantes**: `montar` es idempotente sobre el mismo contenedor (destruye la instancia anterior antes de crear otra); `render` es siempre un render completo, sin mutadores parciales; `destruir` desregistra cada escucha que registró, vacía el contenedor, quita la clase propia y borra la entrada del registro. | E2 `PM/ts/<componente>.ts` l.281-289 (`destruir(contenedor)` antes de `new`), l.128-141 (`destruir()`); E2 `PM/ts/contrato.ts` l.167 («Todo cambio es un render completo. No hay mutadores parciales»); E1 `npm test` → `# pass 83`, incluida `retencion.test.ts` (cien ciclos, registrados == desregistrados) | Escuchas vivos tras cada navegación de Blazor: retención creciente que sólo se ve en producción |
| RAH1-09 | §3 | **El módulo no alcanza el `document` global**: toma `ownerDocument` del contenedor. Es la convención que hace posible probar sin navegador. | E2 `PM/ts/<componente>.ts` l.245-250 («Alcanzar el `document` global ata el componente a que exista uno, y lo rompe fuera de la ventana principal ... Lo levanto la medicion de retencion ... que corre sin DOM»); E2 `PM/ts/dibujo.ts` l.124 | Las pruebas necesitan un DOM simulado completo, o directamente un navegador |
| RAH1-10 | §3 | **Frontera hacia adentro**: ningún `import` sale del propio módulo salvo los de node en las pruebas; sin peticiones de red, sin almacenamiento, sin conocer al anfitrión. Se verifica con una prueba, no por inspección. | E2 ADR-00043 §2 l.40; E2 `PM/ts/frontera.test.ts` l.66-77 (`origen.startsWith("./") \|\| origen.startsWith("node:")`); E1 bundle emitido con `grep -c "^import" wwwroot/<componente>.js` → `0` | El bundle arrastra una dependencia o llama a la red, y la PoC HTML «sin servidor» deja de ser posible |
| RAH1-11 | §3 | **Salida hacia afuera por dos canales del mismo evento**: el callback pasado al montar y un `CustomEvent` que burbujea desde el contenedor. El adaptador de interop es un `montar` con un callback que invoca la referencia .NET; el guion anfitrión de la PoC HTML puede usar cualquiera de los dos canales. | E2 `PM/ts/<componente>.ts` l.143-155 (`emitir`: `alActivar?.(acuse)` y `dispatchEvent(new CustomEvent(...{bubbles: true}))`), l.296-306 (`montarConInterop`) | La PoC HTML sólo puede imitar a Blazor si existe un canal que no dependa de la referencia .NET |
| RAH1-12 | §3 | **Estilos fuera del bundle**: hoja CSS versionada en el proyecto, con variables con prefijo propio y valores por defecto, copiada al estático en la construcción; el anfitrión remapea las variables y **nunca edita la copia**. | E2 `PM/<F1>.<Componente>.csproj` l.68-78 (`CopiarHojaDeEstilos`: «`wwwroot/` es salida de construccion y no fuente»); E2 `<F1>-SDD/Maquetas/<F1>-Web/README.md` l.139 («Nunca `assets/vendor/<componente>.css`: es una copia generada y editarla la desincroniza») | Estilos incrustados en el JS que el anfitrión no puede tematizar sin recompilar |
| RAH1-13 | §3 | **Convención de código para lo que cruza a JSON**: uniones de literales y no `enum`; propiedades opcionales con semántica de «no opino» declarada. (El contenido del contrato es de V2; acá va sólo la convención de escritura del lado TypeScript.) | E2 `PM/ts/contrato.ts` l.7-8 («Los enums son uniones de literales y no `enum`: cruzan a JSON sin ceremonia»); E2 `PM/ts/<componente>.ts` l.91-93 («`null` significa «no opino» y no «olvidate»») | Un `enum` numérico de TypeScript llega como entero a .NET y el tipo cerrado del otro lado no lo reconoce |
| RAH1-14 | §3 | **Pruebas sin anfitrión con el runner del runtime**: un segundo `tsconfig` emite JavaScript a una carpeta no versionada y el runner nativo del runtime lo corre; el contenedor de prueba es un doble mínimo que **guarda los escuchas para dispararlos** y **cuenta** registros y desregistros. Sin biblioteca de pruebas, sin DOM simulado. | E2 `PM/package.json` l.8 (`"test": "tsc -p tsconfig.pruebas.json && node --test build/*.test.js"`); E2 `PM/ts/componente.test.ts` l.15-35 (clase `Contenedor`); E2 `PM/ts/retencion.test.ts` l.18-42 (`ContenedorMedido`); E1 `npm test` → `# tests 83 / # pass 83 / # fail 0` | Las pruebas del bundle sólo corren con navegador y no distinguen un fallo del componente de uno del anfitrión |
| RAH1-15 | §3 | **Qué puede alcanzar una prueba del bundle**: sólo archivos del propio proyecto de código. El precedente incluye una prueba estructural que lee el archivo de proyecto .NET; el documento lo declara como decisión con costo (la batería deja de correr si se mueve la fuente TypeScript sola). Ver HAH1-05. | E2 `PM/ts/frontera.test.ts` l.46-54 (`readFileSync(join(PROYECTO, "<F1>.<Componente>.csproj"))`); E1 copia de `ts/`, `css/`, `package*.json`, `tsconfig*.json` sin `.csproj` → `# pass 82 / # fail 1`, `ENOENT ... <F1>.<Componente>.csproj`; con el `.csproj` → `83/83` | Una batería «sin anfitrión» que en realidad depende del anfitrión |
| RAH1-16 | §4 | Decisión: **empaquetador minimalista** y no de propósito general; alternativas consideradas con el argumento «JavaScript a mano, sin transpilar» tratado como legítimo (cero dependencias contra pérdida del tipado del contrato). | E2 ADR-00042 §2 l.43 y §4 l.51-56 | El agente elige un empaquetador con complementos y cargadores «sin contrapartida para un componente sin dependencias de ejecución» |
| RAH1-17 | §4 | Decisión: **tipado estricto como compuerta previa al empaquetado**: `tsc --noEmit` antes de empaquetar; `strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `moduleResolution: bundler`; versiones **exactas** de las herramientas de construcción e instalación reproducible contra el archivo de bloqueo. | E2 `PM/package.json` l.7 (`tsc --noEmit && esbuild ...`), l.12-14 (`"esbuild": "0.25.12"`, `"typescript": "5.9.3"` sin acento circunflejo); E2 `PM/tsconfig.json` l.3-11; E2 ADR-00042 §7 l.73-74 | Un tipo que dejó de coincidir se descubre en ejecución y no al construir |
| RAH1-18 | §4 | Decisión: **dos superficies de consumo desde el mismo código y en la misma compilación** —módulo ES para quien importa; global clásico para quien carga con etiqueta de script desde el sistema de archivos—, con su forma de construcción concreta (una segunda invocación del empaquetador sobre el mismo punto de entrada). El documento la presenta como decisión a tomar por el destino con sus alternativas, **no como algo que el precedente demostró**: ver HAH1-02. | E2 ADR-00043 §2 l.35-38 y §4 l.48-53; E3 `npx esbuild ts/<componente>.ts --bundle --format=iife --global-name=<Componente>Global --minify --outfile=wwwroot/<componente>.global.js` → «Done», archivo de 10.467 bytes que empieza con `var <Componente>Global=(()=>{` | Una PoC HTML que sólo puede abrirse servida, o una copia a mano del bundle para abrirla desde archivo |
| RAH1-19 | §5 | Esqueletos mínimos: `package.json` (scripts `build` y `test`, tres devDependencies exactas), par de `tsconfig`, punto de entrada con registro por contenedor y los tres verbos más el adaptador de interop, un `x.test.ts` con el doble de contenedor, `.gitignore` de salidas. | E2 `PM/package.json`, `PM/tsconfig.json`, `PM/tsconfig.pruebas.json`, `PM/ts/<componente>.ts` l.39-44 y l.281-318, `PM/ts/retencion.test.ts` l.18-42, `PM/.gitignore` | Sin esqueleto, cada agente reinventa el par de `tsconfig` y el doble de contenedor, que es donde más se falla |
| RAH1-20 | §6 | Criterios verificables: (1) desde limpio, `npm ci && npm run build` produce **todas** las superficies declaradas en una sola corrida; (2) el módulo emitido tiene cero `import`; (3) la lista de exportaciones del módulo coincide con la superficie del contrato; (4) `npm test` pasa sin navegador y sin la cadena .NET; (5) la prueba de retención cierra en cero sobre N ciclos; (6) ningún artefacto de salida está versionado. | E1 salidas de §1 (build: un archivo; `import`: 0; exportaciones: 17; test: 83/83); E2 ADR-00042 §8 l.83-87 y ADR-00043 §8 l.78-81 | El conocimiento «se aplicó» sin que nadie pueda decir cómo se comprueba |
| RAH1-21 | §7 | Anti-patrones, cada uno con su motivo: copia a mano del bundle en otro árbol; declaración de tipos mantenida a mano; `export *` de módulos internos desde el punto de entrada; alcanzar `document`/`window` globales; dos implementaciones del mismo componente para dos consumidores; prueba del bundle que lee archivos del proyecto de otro ecosistema; artefacto de salida versionado. | E2 `<F1>-SDD/Maquetas/<F1>-Web/README.md` l.159 («Lo copiado no se edita ... nadie se entera hasta que la maqueta valida un componente que ya no es el que corre»); E2 ADR-00043 §4 l.53 («Dos implementaciones de lo mismo divergen»); resto: anclas de RAH1-05, -06, -09, -15 | Son los modos de falla que el propio precedente documenta o exhibe (HAH1-02, -04, -05) |
| RAH1-22 | §8 | Frontera con las reglas: dónde viven las pruebas y los samples del destino (`PRODUCT-INTAKE-template.md` §16, `Rules-Examples.md` §3.6); la verificación del sample **nunca enganchada a la construcción de la solución**; el modo de construcción donde falta la cadena de JavaScript (`Rules-Devops.md` §4.9 punto 4); la clase de arista `insumo de construcción` (`Intake-Rules.md` §4). El documento las **nombra y no las reescribe**. | E2 `Rules/Rules-Examples.md` l.199-203; E2 `Rules/Rules-Devops.md` §4.9 punto 4; E2 `Rules/Intake-Rules.md` l.142; E2 `Rules/Rules-Base-Conocimiento.md` l.289 (anti-patrón «Conocimiento disfrazado de regla») | El documento se lee como norma y desplaza a las reglas de 09 y 10 del destino |
| RAH1-23 | §3 / §8 | **La PoC HTML es, en el vocabulario del framework, el anfitrión mínimo** de `Rules-Examples.md` §3.6: carga el artefacto **construido desde la salida del proyecto que lo produce y nunca una copia**, invoca la superficie pública y nada más, y si el bundle invoca de vuelta al anfitrión lleva un **doble del consumidor** con la lista de invocaciones **derivada de una sola fuente**. Ese doble es lo que el pedido llama «main.js» de la PoC. | E2 `Rules/Rules-Examples.md` l.210-223; E2 `INPUT` l.254-256 | La PoC HTML copia el bundle o inventa su propia API, y deja de probar lo que usa Blazor |

## 3. Hallazgos

| Id | Ubicación | Afirmación | Evidencia (nivel + ancla) | Severidad | Confianza | Impacto si no se corrige | Dirección propuesta |
|---|---|---|---|---|---|---|---|
| HAH1-01 | `ev-05` l.17 y l.36; `INPUT` l.254 y l.270-271 | **«main.js» designa dos cosas distintas en los insumos.** En `ev-05` es un artefacto emitido por el proyecto del bundle («arrojará dos artefactos: main.js y el bundle»; «registrar callback blazor en la clase de interfaz en main.js»): una capa de adaptación entre Blazor y el componente. En `INPUT` §8.3 es el guion de la página de la PoC HTML que «imita las invocaciones que hace la RCL», y en §8.4 `dist/` aloja «los dos artefactos JavaScript». En el precedente no hay dos artefactos: el adaptador de interop es una función del mismo módulo (`montarConInterop`), y lo que la maqueta tiene aparte es un adaptador **de la maqueta** que no se traslada al producto. | E2 `ev-05` l.17, l.36; E2 `INPUT` l.254, l.270-271; E2 `PM/ts/<componente>.ts` l.291-306; E2 `<F1>-SDD/Maquetas/<F1>-Web/README.md` l.50 («`Adaptador-<Componente>.js` ... Es de la maqueta y no se traslada al producto»); E2 `Rules/Rules-Examples.md` l.220-221 | S2 | 0.9 | Un agente construye un segundo artefacto del bundle para Blazor y otro construye el script de la PoC, ambos llamados `main.js`; el documento no puede fijar criterios de aceptación sobre «los dos artefactos» sin decir cuáles son | El documento fija el vocabulario de RAH1-02: **un módulo, N superficies de salida** (ADR-00043) y **un guion anfitrión por PoC** que no es artefacto del bundle. Si el Product Owner quiere que la capa de adaptación sea un archivo separado, se construye **desde el mismo punto de entrada** (segundo entry del empaquetador) y no como código aparte. Pregunta P-01 |
| HAH1-02 | ADR-00043 §2 y §7 contra `PM/package.json` l.7 y `<F1>-SDD/Maquetas/<F1>-Web/assets/vendor/<componente>.js` | **El precedente no cumple su propia decisión de «dos superficies desde un mismo código en la misma compilación».** La construcción emite un solo archivo, en formato módulo ES. Lo que la maqueta carga como «global» es un **prototipo escrito a mano** el 2026-08-10, que expone un nombre (`PANELES`) que la fuente TypeScript no exporta: las dos «superficies» ya divergieron, que es exactamente lo que ADR-00043 §4 declara evitar. | E2 ADR-00043 l.35-38 («generadas en la misma compilación»), l.69 («No hay copia manual ni artefacto mantenido aparte»); E2 `PM/package.json` l.7 (un solo `--format=esm --outfile=wwwroot/<componente>.js`); E1 `ls wwwroot/` tras `npm run build` → sólo `<componente>.js`; E2 `<F1>-SDD/Maquetas/.../README.md` l.163 («prototipo que acompaña la nota de diseño, forma equivalente a la salida `<componente>.global.js`»); E2 `assets/vendor/<componente>.js` l.3-4 («Prototipo del componente. Es la forma que tendria la salida transpilada»); E1 `grep -c PANELES PM/ts/*.ts` → 0 en todos, contra `assets/js/Adaptador-<Componente>.js` l.250 (`global.<F1><Componente>.PANELES`); E3 la superficie global se obtiene con una invocación más del empaquetador sobre la misma fuente (RAH1-18) | S2 | 0.95 | El documento transmitiría «dos superficies» como práctica probada cuando el precedente sólo prueba una; y la PoC HTML «sin servidor» quedaría sin forma de construcción declarada, con lo que el agente repite la copia a mano | El documento presenta las dos superficies como **decisión con su forma de construcción** (dos invocaciones del empaquetador, un solo punto de entrada, criterio de aceptación «N archivos en una corrida») y cita el precedente sólo por la decisión, no por la evidencia. Si el destino elige una sola superficie, la PoC HTML se abre **servida** (módulo ES exige servidor) y lo declara según `Rules-Examples.md` §3.6. Pregunta P-02 |
| HAH1-03 | `INPUT` cabecera l.7 («13.15»); §2.3 l.49-51; §4 obs. 2 l.142-145; §8.4 l.288-289; §7 PA-01 | **El documento de entrada está escrito contra 13.15 y no incorpora lo que 13.16 fijó para este caso exacto.** Afirma que el uso del `dist/` por el proyecto .NET «se declara como consumo de artefacto publicado, no como arista de compilación» y deja PA-01 abierta. Desde 13.16, `Intake-Rules.md` §4 declara la clase de arista `insumo de construcción` como «la única clase posible entre proyectos de ecosistemas distintos», y `Rules-Examples.md` §3.6 fija la forma del **anfitrión mínimo** y del **doble del consumidor**, que es la PoC HTML del pedido. Lo que cae en mi mandato es §3.6: la PoC HTML tiene figura en el framework y el `INPUT` §8.2 dice que no la tiene. | E2 `INPUT` l.7, l.49-51, l.142-145, l.185 (PA-01 «Abierta»), l.208 («El término «PoC» no existe en el conjunto normativo»); E2 `Rules/Intake-Rules.md` l.142; E2 `Rules/Rules-Examples.md` l.210-223; E2 `SDD/Devs/Guides/Coherencia-Proyecto-De-Otro-Ecosistema.md` l.6-7 (13.16, origen: «el método no sabe poner un proyecto de otro ecosistema en la solución») | S2 | 0.9 | El documento heredaría del `INPUT` una clasificación que el framework vigente contradice, y describiría la PoC HTML como figura sin lugar cuando §3.6 ya le da forma y criterios | Reescribir el encaje de la PoC HTML sobre `Rules-Examples.md` §3.6 (RAH1-23). La reclasificación de la arista y del agrupador es de V1/V3: solicitud en §5 |
| HAH1-04 | `PM/ts/<componente>.ts` l.32-37; `<F1>-SDD/.../Contratos-Abstractions-<F1>-<Componente>.md` §3 | **La superficie emitida del precedente es más grande que su contrato.** El punto de entrada reexporta seis módulos internos; el módulo construido expone 17 nombres (`componerVista`, `dibujar`, `normalizarGeometria`, `TECLAS`, ...) cuando el contrato declara cinco operaciones y el envoltorio Blazor invoca tres (`montarConInterop`, `render`, `destruir`). Además, la operación 3 del contrato («Actualizar la clasificación ... no rehace el dibujo») no tiene función en la superficie: sólo existe `render` completo. | E1 exportaciones del bundle → `17` (lista literal en §1); E2 `PM/ts/<componente>.ts` l.32-37; E2 `PM/DiagramaDeCabecera.razor` l.59, l.69, l.88; E2 contrato §3 l.47-53 (cinco operaciones) y `PM/ts/contrato.ts` l.166-171 (`Instancia<Componente>`: tres métodos) | S3 | 0.85 | El documento tomaría el precedente como modelo de «el punto de entrada expone el contrato» y transmitiría lo contrario; los criterios de aceptación de RAH1-20 (3) no se pueden cumplir sobre este precedente | El documento fija: **el punto de entrada exporta sólo la superficie del contrato**; los internos se prueban por importación directa del módulo fuente, no por el bundle. La discrepancia contrato-documento vs. código (operación 3) es de V2: señal en §5 |
| HAH1-05 | `PM/ts/frontera.test.ts` l.46-54; `PM/<F1>.<Componente>.csproj` l.80-88 | **La batería «sin anfitrión» depende del archivo de proyecto del anfitrión.** `frontera.test.ts` abre `<F1>.<Componente>.csproj` para contar `ProjectReference`; movida la fuente TypeScript sin el `.csproj`, la batería falla (82/83). La decisión es válida —verificar la frontera desde la propia batería— pero es una dependencia cruzada de ecosistemas que el precedente no declara como tal y que el documento, si copia la forma, arrastraría sin motivo. | E1 corrida sin `.csproj`: `# pass 82 / # fail 1`, `error: "ENOENT: no such file or directory, open '.../<F1>.<Componente>.csproj'"`; corrida con `.csproj`: `# pass 83 / # fail 0`; E2 `PM/ts/frontera.test.ts` l.46-54; E2 `PM/<F1>.<Componente>.csproj` l.81 («Corre **sin anfitrion**: sin DOM, sin Blazor y sin navegador») | S3 | 0.9 | Un agente que separe el bundle en su propia carpeta o solución (caso «propia solución» de `Intake-Rules.md` §4) hereda una prueba que falla por un archivo de otro ecosistema | El documento declara qué puede alcanzar una prueba del bundle (RAH1-15) y, si transmite la prueba de frontera, la limita a los `import` del propio módulo; la comprobación del archivo de proyecto .NET se deja del lado .NET (V3) |

## 4. Lo que revisé y está bien

| # | Qué | Evidencia |
|---|---|---|
| 1 | **Gestión de instancias por contenedor y ciclo de vida.** Mapa débil indexado por elemento, `montar` idempotente, `destruir` desregistra lo que registró, vacía y quita su clase; la retención se **mide** y no se infiere. La forma es exactamente la que el documento debe transmitir. | E2 `PM/ts/<componente>.ts` l.44, l.128-141, l.281-289; E1 `npm test` → `retencion.test.ts` cuatro casos en verde dentro de `# pass 83`, incluido «montar dos veces sobre el mismo contenedor no deja la primera instancia viva» |
| 2 | **Probable sin navegador por construcción.** `ownerDocument` desde el contenedor y un doble de contenedor de treinta líneas alcanzan para ejercer teclado, activación y liberación con el runner nativo del runtime, sin biblioteca de pruebas ni DOM simulado. | E2 `PM/ts/<componente>.ts` l.245-250; E2 `PM/ts/componente.test.ts` l.15-35; E1 `npm ci` → 5 paquetes en total (ninguno de pruebas); `npm test` → 83/83 en 273 ms |
| 3 | **Cadena de herramientas fijada y mínima.** Tres dependencias de construcción con versión exacta, instalación reproducible contra archivo de bloqueo, compuerta de tipos (`tsc --noEmit`) antes de empaquetar, artefactos de salida fuera del control de versiones. | E2 `PM/package.json` l.7, l.11-15; E2 `PM/.gitignore`; E2 ADR-00042 §7 l.73-76; E1 `npm run build` desde limpio → un comando, «Done in 5ms» |

## 5. Solicitudes de convocatoria

| # | Rol | Señal, con ubicación | Qué no puedo afirmar sin él |
|---|---|---|---|
| SC-01 | **V1 Arquitectura / V3 Operación y entrega** (ya convocadas; se les dirige la señal) | `INPUT` §2.3 l.49-51 y §8.4 l.288-289 declaran el consumo del `dist/` como «consumo de artefacto publicado, no arista»; `Rules/Intake-Rules.md` l.142 (13.16) declara `insumo de construcción` como «la única clase posible entre proyectos de ecosistemas distintos», y `Rules-Devops.md` §4.9 punto 4 fija el modo de construcción donde falta la cadena | Si el bundle pertenece a la solución .NET (caso «un proyecto la toma como insumo») o es solución propia, y qué implica para el agrupador y para la ubicación de las PoC. Es HAH1-03 fuera de mi mandato |
| SC-02 | **V2 Interfaz / consumidor externo** (ya convocada) | `Contratos-Abstractions-<F1>-<Componente>.md` §3 declara cinco operaciones; `PM/ts/contrato.ts` l.166-171 declara tres métodos y `<componente>.ts` no tiene función para «Actualizar la clasificación» sin redibujar; `contrato.ts` l.4 cita un `.d.ts` normativo que no se construye | Si el contrato que el documento toma como forma es el del documento de arquitectura o el del código, y si una declaración de tipos generada debe formar parte del contrato |
| SC-03 | **AH-2 Interoperabilidad Blazor** (ya convocada) | `PM/ts/<componente>.ts` l.296-306: el adaptador de interop vive dentro del módulo y recibe la referencia .NET como objeto con `invokeMethodAsync`; `DiagramaDeCabecera.razor` l.73-77: el nombre del método invocable se escribe explícito de los dos lados | Si el documento debe fijar que el adaptador viva en el mismo módulo (como el precedente) o como archivo separado (como sugiere «main.js» en `ev-05`); yo sólo puedo afirmar cómo se construye cada opción desde el mismo punto de entrada |

## 6. Preguntas al Product Owner

| Id | Pregunta cerrada | Opciones | Ítem `C` que la funda |
|---|---|---|---|
| P-01 | En el pedido, ¿«main.js» designa un **artefacto que emite el proyecto del bundle** (capa de adaptación para Blazor) o el **guion de la página de la PoC HTML** que imita a Blazor? | (a) artefacto del bundle, y entonces se construye desde el mismo punto de entrada como segunda salida; (b) guion de la PoC, y entonces no es artefacto del bundle y se llama «guion anfitrión» o «doble del consumidor»; (c) las dos cosas, con nombres distintos que el documento fija | C: la lectura de `ev-05` l.17 y l.36 contra `INPUT` l.254 admite las dos; el precedente no tiene un archivo así (HAH1-01) |
| P-02 | ¿El documento debe **exigir** dos superficies de salida (módulo ES y global clásico) o exigir el módulo ES y dejar el global como opción declarada? | (a) las dos, obligatorias, con criterio «una corrida emite ambas»; (b) módulo ES obligatorio, global opcional y declarado, con la PoC HTML abriéndose servida cuando no hay global; (c) sólo módulo ES | C: ADR-00043 lo decide para su producto por la maqueta de la Fase B2, que no existe en el pedido; el precedente no lo construye (HAH1-02) |

Sigue: consolidación · presidente de mesa
