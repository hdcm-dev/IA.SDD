# Actuación 004 — Informe Comisión N2, Verificación

| Campo | Valor |
|---|---|
| Tipo | `informe` |
| Fecha | 2026-09-13 |
| Autor | Comisión N2, Verificación (núcleo permanente) |
| Corrige | — |

Base: `IA.SDD` `a501857` (13.17) · `<F1>` `9aabe5c` (`evidencia/ev-01-base.out`). Todo comando de este informe se corrió el 2026-09-13 sobre esa base; lo que se ejecutó sobre el precedente se ejecutó sobre **una copia en el scratchpad de la sesión** (`git archive 9aabe5c src/<F1>.<Componente>`), sin escribir en el repositorio del cliente.

---

## 1. Mandato y método

**Pregunta de la carta.** Cómo se verifica que `Knowledge-Bundle-JS.md` cumple `Rules-Base-Conocimiento.md`; cómo se verifica que el conocimiento se aplicó bien en un producto (§6 del documento a producir, incluidas las dos PoC como instrumento de diagnóstico); y qué del pedido no es verificable.

**Qué leí, en orden.**

| # | Ruta | Para qué |
|---|---|---|
| 1 | `evidencia/ev-07-cartas-despachadas/00-encargo-comun.md` | Reglas y forma del informe |
| 2 | `evidencia/ev-07-cartas-despachadas/02-N2-verificacion.md` | Mandato |
| 3 | `SDD/Devs/Rules/Rules-Base-Conocimiento.md` (2.2, 535 líneas) | La norma que el documento tiene que cumplir: §4.1, §4.2, §4.4, §4.5, §6.1, §6.2, §7.1 |
| 4 | `evidencia/ev-05-presentacion-original.md` | El pedido literal |
| 5 | `INPUTs/Bundle-JS-En-Solucion-Blazor.md` (0.2, 299 líneas) | El análisis previo; §8 en particular |
| 6 | `actuaciones/001-*.md`, `actuaciones/002-*.md` | Contrato de entrada (§2 de la 002): decisiones cerradas, fuera de alcance, R1 a R5 |
| 7 | `evidencia/ev-01` a `ev-06` (`.sh` y `.out`) | Evidencia ya levantada |
| 8 | `Conocimiento/README.md`, `Conocimiento/Index-Knowledge.md` | Modelo de la base y contrato del índice en uso |
| 9 | `Conocimiento/Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md`, `Knowledge-Conformacion-Pull-Request-Manual.md` | Forma de un §6 ya aceptado; forma de la declaración de ofuscación; forma de §9 sin índice propio |
| 10 | Precedente: `<F1>.<Componente>.csproj`, `package.json`, `tsconfig*.json`, `.gitignore`, `ts/<componente>.ts`, `ts/contrato.ts` (cabecera), `Contrato.cs` (por `ev-03`), `DiagramaDeCabecera.razor`, `ts/componente.test.ts` y `ts/frontera.test.ts` (cabeceras), `ADR-00042`, `ADR-00043`, `Contratos-Abstractions-<F1>-<Componente>.md`, `SDD/Maquetas/<F1>-Web/README.md` §2, §4, §9 | Cómo el precedente prueba el bundle sin anfitrión y cómo se verifica la costura |
| 11 | `SDD/Devs/Rules/Rules-Examples.md` §0, §0.1, §0.2, §3.6, §4.5, §4.6, §6; `SDD/Devs/Guides/Coherencia-Proyecto-De-Otro-Ecosistema.md`; `Intake-Rules.md` §4 (bloque «proyecto de código de otro ecosistema»); `Rules-Devops.md` §4.9 punto 4; `Catalogo-De-Criterios.md` §3 (filas de Rules-Base-Conocimiento) | Dónde el framework ya fija la verificación de un sample que carga un artefacto de otro ecosistema |

**Qué no leí, y por qué.** Ningún informe de otra comisión: en `actuaciones/` sólo existían 001 y 002 al momento de escribir (`ls`, 15:03). No leí el cuerpo completo de `ts/contrato.ts`, `Contrato.cs` ni los módulos internos del bundle: el contenido del contrato es de V2 y la cadena de construcción de V3; tomé de ellos sólo lo que hace falta para decir **cómo se verifica** la costura, no qué dice. No leí `Rules-Calidad-Y-Pruebas.md` entera: la búsqueda de «anfitrión» no devuelve nada en ese archivo, así que no fija la forma de la batería sin anfitrión.

**Método de anclaje.** `E1` con el comando y su salida literal; `E2` con archivo y línea; `E3` para dos contraejemplos construidos en el scratchpad (§3, H-N2-02); `E4` para el contrato de entrada. Los chequeos enumerables de §6.1 se corrieron **sobre los documentos ya publicados en `Conocimiento/`** para probar que el comando detecta antes de aplicarlo al documento que todavía no existe.

---

## 2. Relevamiento para el documento `Bundle-JS`

Sección destino según `Rules-Base-Conocimiento.md` §4.2. Las filas marcadas `[enumerable]` llevan el comando con que la actuación 015 las va a verificar.

| Id | Sección destino | Qué tiene que decir | Ancla (nivel + cita) | Riesgo si falta |
|---|---|---|---|---|
| R-N2-01 | Cabecera | Los once campos de §4.1, ninguno vacío; `Alias: Bundle-JS`, `Naturaleza: propio`, `Compatible-con: Rules-Base-Conocimiento.md 2.2`. `[enumerable]`: `sed -n 1,20p DOC \| grep -cE '^\*\*(Alias\|Naturaleza\|Tema\|Consumidor\|Condicion-de-carga\|Hereda-de\|Sustituye\|Compatible-con\|Versión\|Estado\|Fecha):\*\* \S'` debe dar `11` | E2 `Rules-Base-Conocimiento.md` l.220-239 y l.352; E4 002 §2 `decisiones_cerradas`; E1: el comando da `11` en los tres documentos publicados probados | Sin los once campos la comprobación campo por campo de §7.1 no se puede hacer |
| R-N2-02 | §0 | **Qué queda afuera, con dónde vive**: la estructura de la solución .NET y la ubicación de las PoC en el framework que adopte el documento (R2); la conversión de la RCL a paquete; el contrato de verificación de los samples y su `VER-XXXXX`; la política de CI. Forma: tabla «Fuera de alcance / Dónde vive» como la del documento Blazor | E2 `Rules-Base-Conocimiento.md` l.248 y l.260-262 («§0 y §8 no se negocian»); E2 `Knowledge-Template-Blazor-…` l.26-35; E4 002 §2 R2 y `fuera_de_alcance` | El agente lector completa el hueco por su cuenta (l.260); el documento dicta lo que el PO prohibió dictar |
| R-N2-03 | §0 | **Verificación de ofuscación declarada**: lista de términos buscados del dominio, del cliente y del stack de origen, con cada falso positivo léxico enumerado. Lista mínima a barrer, del precedente y del documento de entrada: `<F1>`, `<Componente>`, `<componente>`, `<prefijo-css>`, `cabecera`, `pines`, `BCM`, `<F1>-srv`, `<F1>`, `Teatro`, `Reservas`, `MapaAsientos`, `asientos`, `sillas`, `googlemaps`. Falsos positivos previsibles: «mapa» como sustantivo genérico; «Aplicada» como prefijo de organización por defecto del framework. `[enumerable]`: `grep -n -i -E '<f1>\|<componente>\|<componente>\|<prefijo-css>\|bcm\|<F1>-srv\|teatro\|reservas\|mapaasientos\|googlemaps' DOC` debe dar vacío salvo lo declarado | E2 `Rules-Base-Conocimiento.md` l.162-166 y l.358; E2 `Conocimiento/README.md` l.44-47; E2 declaración modelo en `Knowledge-Template-Blazor-…` l.41-46; E2 origen de los términos: `ev-03-precedente.out`, `INPUTs/…` l.93 y l.263, `ev-05` l.17 | El ítem `[enumerable]` de §6.1 falla; el documento arrastra nombres de un producto de cliente |
| R-N2-04 | §0 | Toda omisión de sección y la eventual excepción del techo por §5 se declaran acá con su motivo | E2 `Rules-Base-Conocimiento.md` l.266-267 y l.374-376; E2 precedente de la declaración: `Knowledge-Template-Blazor-…` l.37-39 | El ítem «§0 a §10 salvo omisiones que §0 declare» y el del techo no tienen contra qué evaluarse |
| R-N2-05 | §0 / §4.4 | **Un documento, un artefacto.** El documento tiene que decir en una oración cuál es el artefacto único (el bundle con su costura hacia un anfitrión .NET) y por qué las cuatro piezas —proyecto del bundle, PoC HTML, RCL, PoC Blazor— son partes de ese artefacto y no cuatro temas. Si no puede, se parte y §9 declara hermanos | E2 `Rules-Base-Conocimiento.md` l.276 (propiedad) y l.292 (anti-patrón «documento de dos temas», detección: §0 necesita «y además») | El anti-patrón `[interpretativo]` de §4.5 se dispara; el techo de §6.2 se vuelve inalcanzable |
| R-N2-06 | §1 | Supuestos **verificables** del artefacto: el bundle es proyecto de código y no lleva D8; su artefacto es insumo de construcción de la RCL; el entorno de ejecución de JavaScript es requisito de construcción y no de ejecución. Cada supuesto con la comprobación que lo delata cuando no se cumple | E2 `PRODUCT-INTAKE-template.md` l.318 y l.359 (`ev-02`); E2 `Intake-Rules.md` §4 l.123-133; E2 precedente `<F1>.<Componente>.csproj` l.25-29 y l.40-41 | §6 no puede distinguir «el conocimiento se aplicó mal» de «el supuesto no se daba» |
| R-N2-07 | §2 y §6 | **Tres instrumentos de verificación, cada uno con lo que prueba y lo que no**: (a) batería del bundle **sin anfitrión** —sin DOM, sin Blazor, sin navegador— prueba la lógica del bundle; (b) PoC HTML —anfitrión mínimo sin .NET— prueba la superficie pública, la forma de carga y el retorno al anfitrión con un doble del consumidor; (c) PoC Blazor prueba la costura: importación del módulo, nombres invocados, método invocable de vuelta, ruta del estático, liberación | E2 csproj l.80-83 («permite distinguir un fallo del componente de uno de quien lo aloja»); E2 `ts/componente.test.ts` l.1-8 («Corre sin anfitrión: el contenedor es de mentira»); E2 `Rules-Examples.md` l.210-223 (anfitrión mínimo, doble del consumidor); E2 razor l.73-75 («si alguno cambia, el acuse deja de llegar y solo se nota en ejecución») | El PO pide diagnóstico de fallas de contrato (`ev-05` l.25); sin el reparto, las dos PoC prueban lo mismo o nada |
| R-N2-08 | §6 | **Matriz de diagnóstico** por combinación de resultados: batería falla → defecto del bundle; batería pasa y PoC HTML falla → superficie pública, formato o ruta de carga, o doble del consumidor desincronizado; PoC HTML pasa y PoC Blazor falla → costura .NET (nombre invocado, `JSInvokable`, `_content/`, ciclo de vida, liberación); PoC Blazor pasa y el producto falla → anfitrión real, fuera del artefacto | E2 mismas anclas de R-N2-07; E3 §3 H-N2-02 (la PoC HTML falla por forma de carga con el bundle correcto) | Las PoC quedan como demos: ilustran y no discriminan (`Rules-Examples.md` l.322) |
| R-N2-09 | §6 `[enumerable]` | Todo literal que el envoltorio invoca sobre el módulo es una exportación del punto de entrada del bundle. Comando sobre el producto: extraer los literales de `InvokeVoidAsync("…")`/`InvokeAsync<…>("…")` del `.razor` y comprobar cada uno contra `^export function <nombre>` del punto de entrada | E1 sobre el precedente: exports `destruir montar montarConInterop render seleccionar`; invocados `destruir montarConInterop render`; los tres `ok` | Un nombre mal escrito compila del lado .NET y falla en ejecución |
| R-N2-10 | §6 `[enumerable]` | El nombre del método invocable de vuelta coincide de los dos lados: `[JSInvokable("X")]` en el `.razor` y el nombre por omisión que el bundle invoca | E1: `JSInvokable("AlActivarPunto")` en razor l.76 y `nombreDelMetodo = "AlActivarPunto"` en `ts/<componente>.ts` l.299; E2 razor l.73-75 declara que sólo se nota en ejecución | El retorno al anfitrión se pierde en silencio |
| R-N2-11 | §6 `[enumerable]` | La ruta del estático que importa el envoltorio es `_content/<AssemblyName>/…` con el `AssemblyName` del proyecto | E1: `_content/<F1>.<Componente>/` (razor l.57) = `<AssemblyName><F1>.<Componente>` (csproj l.15) | Módulo no encontrado en ejecución; el error nombra una ruta y no la causa |
| R-N2-12 | §6 `[enumerable]` | La versión del contrato se declara **una vez de cada lado** con el mismo literal, y se compara | E1: `Contrato.cs` l.114 `Version … = 1` y `ts/contrato.ts` l.114 `version: 1`; E2 `Contratos-Abstractions-…` l.43 («la fuente de verdad es el código, y 08 verifica que coincidan») | Deriva silenciosa entre los dos tipados |
| R-N2-13 | §6 `[enumerable]` | El bundle no está versionado y **la PoC carga el artefacto construido, nunca una copia**: `git ls-files` sobre la carpeta de salida vacío; huella del archivo que carga la PoC igual a la huella de la salida de construcción | E1: `git ls-files -- wwwroot build` vacío en el precedente; `.gitignore` l.2-4; E2 `ADR-00042` §8 («Archivos de bundle versionados: Cero»); E2 `Rules-Examples.md` l.214-215 («nunca una copia») | La PoC valida un componente que ya no es el que corre (E2 README maqueta l.159) |
| R-N2-14 | §6 `[enumerable]` | El bundle no habla con la red ni con el almacenamiento del navegador: `grep -cE 'fetch\(\|XMLHttpRequest\|WebSocket\|localStorage\|sessionStorage' ts/*.ts` con cero coincidencias | E1: 0 archivos con coincidencias en el precedente; E2 `ADR-00043` §2 y §8 («Peticiones de red: Cero») | La PoC HTML «sin servidor» deja de ser reproducible |
| R-N2-15 | §6 `[enumerable]` | La batería corre sin anfitrión y termina con código 0 sin navegador: `npm test` (o el comando declarado) desde la carpeta del bundle | E1 sobre la copia: `npm ci` → `added 5 packages`; `npm test` → `# tests 83 / # pass 83 / # fail 0`, `exit=0`; sin navegador, sin .NET | Sin batería sin anfitrión no hay forma de separar el defecto del bundle del de la costura |
| R-N2-16 | §6 | **La PoC HTML declara cómo se abre** —sin servidor, o servido— y la superficie que carga es compatible con ese modo: un módulo ES **no se carga desde `file://`**; sin servidor hace falta una superficie global emitida **por la misma cadena** | E3 §3 H-N2-02: importación de módulo desde `file://` bloqueada por CORS; la salida `--format=iife` de la misma cadena carga desde `file://` y expone la API; E2 `Rules-Examples.md` l.218-219; E2 `ADR-00043` §2 («funciona desde el sistema de archivos») | La PoC HTML «no prueba lo que usa Blazor» o directamente no abre |
| R-N2-17 | §6 | **Qué se engancha a la construcción y qué no**: la batería del bundle puede correr en la construcción del proyecto (es prueba, categoría 08); la verificación de una PoC **nunca**: se corre con su propio comando | E2 csproj l.85-88 y l.94-99 (`AfterTargets="Build"`, apagable con `-p:PruebasDeTypeScript=false`); E2 `Rules-Examples.md` l.199-203 y l.505 | Un sample roto se confunde con un producto que no compila (l.202) |
| R-N2-18 | §6 `[interpretativo]` con proxy enumerable | La liberación cubre los dos lados y tolera el circuito caído: `@implements IAsyncDisposable`, invocación de la función de destrucción, `DisposeAsync` del módulo, `Dispose` de la referencia .NET, captura de `JSDisconnectedException`. Proxy: `grep -c` de cada uno de los cinco en el `.razor` | E2 razor l.9 y l.79-98 («Saltearse cualquiera de los dos deja una referencia viva») | Fuga de referencias que ninguna PoC ve en una corrida corta |
| R-N2-19 | §6 `[interpretativo]` | La superficie pública que las PoC ejercitan **es la del contrato y no más**; el conjunto de PoC cubre la superficie completa. Proxy: la lista de funciones exportadas del punto de entrada contra las operaciones del contrato | E1: la superficie global construida en el scratchpad expone doce funciones (`componerVista, destruir, dibujar, …`) contra cinco operaciones del contrato (`Contratos-Abstractions-…` §3) por los `export *` de `ts/<componente>.ts` l.33-37; E2 `Rules-Examples.md` l.222-223 | La PoC prueba una superficie que el anfitrión no consume, o deja sin ejercitar una que sí |
| R-N2-20 | §7 | Anti-patrones **con su verificación**: copia a mano del bundle en la PoC o en la maqueta; criterio de aceptación en prosa; PoC que reproduce la lógica del consumidor real; segunda superficie mantenida aparte | E2 README maqueta l.159 y l.163 (el propio precedente lo hizo: la copia es un prototipo del 2026-08-10); E2 `Rules-Examples.md` l.216 y l.369; E2 `ADR-00043` §4 fila 4 | §7 «es de lo más valioso que una captura puede dejar» (l.330-331) |
| R-N2-21 | §8 | Frontera explícita: contrato de verificación y `VER-XXXXX` → `Rules-Examples.md` §4.6; forma del sample que carga un artefacto → §3.6; pertenencia a la solución y clase de arista → `Intake-Rules.md` §4; modo de construcción sin la cadena → `Rules-Devops.md` §4.9 punto 4; pruebas → categoría 08. Y la frase: **este documento no define criterios de aceptación de ningún artefacto que el framework genera** | E2 `Rules-Base-Conocimiento.md` l.256, l.260-262, l.289, l.363; E2 forma: `Knowledge-Conformacion-…` l.201-202 | El documento «empieza a leerse como norma» (l.261) y el ítem `[interpretativo]` de l.363 falla |
| R-N2-22 | §9 | Índice: «`Index-Knowledge.md` de la base que lo aloje» (no hay índice en `OUTPUTs/`); hermanos si se partió; consumidor; artefacto de referencia: si se deposita una PoC como referencia ejecutable, se declara acá | E2 `Rules-Base-Conocimiento.md` l.153-156 y l.209-212; E2 forma: `Knowledge-Template-Blazor-…` l.925; E4 002 §2 `fuera_de_alcance` (no se cataloga) | Ítem de vinculación de §3.3 sin cumplir |
| R-N2-23 | §10 | Control de cambios con fila 1.0 y la fecha de la cabecera | E2 `Rules-Base-Conocimiento.md` l.258 | Cabecera y registro desincronizados |
| R-N2-24 | Todo el documento `[enumerable]` | Numeración contigua: `grep -oE '^### [0-9]+\.[0-9]+' DOC` no muestra una `N.k` sin `N.1…N.(k-1)`, y una sección con una sola subsección la lleva sin numerar | E2 `Rules-Base-Conocimiento.md` l.359; E1: los tres documentos probados son contiguos | El ítem de §6.1 que el piloto 1.1 sumó justamente por este defecto (l.533) |
| R-N2-25 | Todo el documento `[enumerable]` | Techo: `wc -l DOC` ≤ 600; si lo supera, `resto = total − líneas de §5` ≤ 600 **y** §0 declara la excepción | E2 `Rules-Base-Conocimiento.md` l.372-376; E1 método de conteo: `awk '/^## 5\./{f=1} /^## 6\./{f=0} f' DOC \| wc -l`; medido: Blazor 937/548/389, HTML 877, Mesa 312, PR 218 | Ver H-N2-05 |
| R-N2-26 | Todo el documento `[enumerable]` proxy de R2 | Ninguna ruta literal de la solución .NET se prescribe: `grep -n -E '/samples\|/demos\|/src/\|/tests/\|\.sln\|Unidades-Entrega' DOC` y cada coincidencia está marcada como ilustrativa o pertenece al interior del artefacto | E4 002 §2 R2; E2 `ev-05` l.41 (ejemplo `/demos` vs `/samples`) | Se viola la restricción de fondo del pedido |

---

## 3. Hallazgos

| Id | Ubicación | Afirmación | Evidencia (nivel + ancla) | Severidad | Confianza | Impacto si no se corrige | Dirección propuesta |
|---|---|---|---|---|---|---|---|
| H-N2-01 | `INPUTs/Bundle-JS-En-Solucion-Blazor.md` §2.3 (l.49-51), §4 obs. 2 (l.143-145), §7 PA-01, §8.2 (l.226-227) y cabecera l.7 («13.15») | El documento de entrada está escrito contra 13.15 y **no ve lo que 13.16 fijó el mismo día**: `Rules-Examples.md` §3.6 define **el sample de un artefacto que otro proyecto carga** como «anfitrión mínimo» —carga el artefacto construido y nunca una copia, invoca sólo la superficie pública, declara si abre sin servidor o servido, lleva doble del consumidor si hay retorno, y el conjunto cubre la superficie— y `Intake-Rules.md` §4 declara que el proyecto de otro ecosistema **pertenece a la solución del consumidor** como «insumo de construcción», no como «consumo de artefacto publicado». La afirmación de §8.2 «ninguno es una PoC de integración de un componente» y la obs. 2 de §4 quedan contradichas por el conjunto vigente | E2 `Rules-Examples.md` l.210-223 y fila 6.6 l.786 («2026-09-13 … §3.6 es nueva»); E2 `Intake-Rules.md` l.123-133 y fila 4.3 l.276; E2 `Coherencia-Proyecto-De-Otro-Ecosistema.md` §2.1 («La respuesta negativa obligaba a declarar como consumo de artefacto publicado un artefacto que no se publica») y §2.4; E1 `ev-06` muestra que el documento de entrada cita `Rules-Examples.md` §0, §0.1, §1.2 y **no** §3.6 | S2 | 0,9 | El documento `Bundle-JS` reescribiría en su §6 lo que el framework ya norma para el sample anfitrión —«conocimiento disfrazado de regla», anti-patrón `[enumerable]`— o, peor, lo contradiría en la forma de carga | El documento **cita** §3.6 en su §8 como frontera y formula sus criterios de §6 sobre el artefacto (R-N2-07 a R-N2-19), nunca sobre la forma del sample. Corregir §2.3, §4 obs. 2, §8.2 y PA-01 del documento de entrada antes de usarlo como insumo del dictamen. Lo que excede mi mandato —clase de arista, único generador, modo de construcción— va en §5 |
| H-N2-02 | Precedente: `package.json` l.8; `ADR-00043` §2 y §7; `SDD/Maquetas/<F1>-Web/README.md` l.159 y l.163; `assets/vendor/<componente>.js` cabecera | **El precedente no prueba su bundle sin anfitrión .NET contra el artefacto construido.** `ADR-00043` decide dos superficies «generadas del mismo origen y en la misma compilación» y «sin copia manual»; la cadena emite **sólo el módulo ES**; lo que consume la maqueta es un **prototipo a mano del 2026-08-10**. Y un módulo ES **no se carga desde `file://`**: la superficie que un anfitrión sin servidor necesita no existe en la salida | E1: `npm run build` en la copia → `wwwroot/<componente>.js 9.7kb`, `grep -c 'export{'` = 1, `grep -c '<F1><Componente>'` = 0; E2 `package.json` l.8 (`--format=esm`, único `outfile`); E2 README maqueta l.163 («prototipo que acompaña la nota de diseño … 2026-08-10»); E3 página mínima en el scratchpad con `import("./<componente>.js")` abierta por `file://` en Chromium: `Failed to fetch dynamically imported module`, consola: «blocked by CORS policy: Cross origin requests are only supported for protocol schemes: … http, https»; E3 contraprueba: `npx esbuild ts/<componente>.ts --bundle --format=iife --global-name=<Componente>Global` abierta por `file://` devuelve `OK global: … montar, montarConInterop, render, seleccionar, destruir …` | S2 | 0,9 | Si el documento transmite «la forma» del precedente para la PoC HTML, enseña a validar una copia —lo que `Rules-Examples.md` l.214-215 prohíbe— y una PoC HTML «sin servidor» que no abre. El diagnóstico de la matriz R-N2-08 falla en su primera fila | El documento dice en §4 (decisión ya tomada, con criterio) y §6 (criterio): la PoC HTML declara su modo de apertura; sin servidor exige una superficie global **emitida por la misma cadena**, y la huella del archivo cargado se compara con la salida de construcción (R-N2-13, R-N2-16). El precedente se cita como **contraejemplo** en §7, ofuscado |
| H-N2-03 | `ev-05` Objetivos l.25 («metodologías de desarrollo … fases de especificación, implementación, diagnóstico y evaluación») y Solicitudes l.33 («capture la metodología») frente a `Rules-Base-Conocimiento.md` §0.1 | El pedido pide **método** y el formato obligatorio exige **artefacto**. Dos ítems de la norma lo vuelven verificable y bloqueante: el anti-patrón `[enumerable]` «Conocimiento disfrazado de regla» —«define criterios de aceptación de un entregable del framework»— y el ítem `[interpretativo]` de §6.1 «no define criterios de aceptación, nomenclatura ni gating de artefactos que el framework genera». Las PoC, si se encajan como samples (documento de entrada §8.3), **son artefactos que el framework genera** con su propio contrato de verificación | E2 `Rules-Base-Conocimiento.md` l.47-49, l.289, l.363; E2 `Rules-Examples.md` l.331-342 (`Contrato de verificación`, `VER-XXXXX`); E2 `ev-05` l.25 y l.33; E1 `ev-02` última línea: «PoC» tiene cero ocurrencias en el conjunto normativo | S2 | 0,8 | El documento falla el ítem l.363 en la actuación 015, o se lo aprueba leyéndolo como norma, que es lo que l.261-262 dice que pasa | Comprobación previa para el redactor: en el documento **no aparece** `verificacion:`, `VER-`, `criterio_aceptacion` ni «sección 9 del markdown explicativo». El §6 formula criterios sobre el bundle, el `main.js`, la RCL y las dos PoC **como piezas del artefacto** (qué cargan, qué prueban, qué falla distingue); el contrato de verificación que exija el framework adoptante queda en §8 como frontera |
| H-N2-04 | `Conocimiento/Index-Knowledge.md` l.22 y `Conocimiento/README.md` l.38-40 («se ejecuta el tool-prompt de relevamiento»); lista §6.1 de la norma | **No hay instrumento mecánico para §6.1.** El tool-prompt `Relevar-Conocimiento.md` que el índice y el README nombran **no existe** en el repositorio, y la lista de §6.1 no se corre sola: dos de los cinco documentos publicados no declaran la verificación de ofuscación (ítem `[enumerable]` l.358) y el `Tema` del documento Blazor difiere entre cabecera e índice (ítem l.357); el propio índice declara compatibilidad con la 2.0 de una norma que va por 2.2 | E1: `find . -iname '*Relevar-Conocimiento*' -not -path '*/_legacy/*'` → vacío; E1: `grep -c -i 'ofuscaci'` = 0 en `Knowledge-Conformacion-Pull-Request-Manual.md` y `Knowledge-Mesa-De-Expertos-A-Pedido.md`; E1: comparación de los ocho campos del documento Blazor contra su fila → `DIFF Tema` (cabecera «…template HTML de maqueta … superficies de identidad…», índice «…template HTML como … e identidad…»); E2 `Index-Knowledge.md` l.4 («2.0») contra `Rules-Base-Conocimiento.md` l.6 («2.2») | S3 | 0,85 | La verificación del `Bundle-JS` en la 015 será manual y, por el antecedente, incompleta; el pedido del PO de «evidencia verificable» (`ev-05` l.52) no tiene con qué cumplirse del lado del formato | La actuación 015 corre los comandos enumerables de §2 de este informe (R-N2-01, 03, 09 a 15, 24 a 26) y deja la salida como evidencia. Lo que esto revela del framework **no se corrige acá** (R1): se eleva al PO como observación, §6 |
| H-N2-05 | `Rules-Base-Conocimiento.md` §6.2 l.372-376 y §4.3 l.266-267 | **El techo de 600 líneas no dice cómo se cuenta** —con o sin §5, con o sin líneas en blanco— y los dos `propio` publicados lo superan (937 y 877) acogiéndose a la excepción de §5. El `Bundle-JS` es `propio` con **cuatro esqueletos** (proyecto del bundle, `main.js`/PoC HTML, RCL, PoC Blazor): el riesgo de superar el techo es alto y el criterio de la 015 tiene que estar fijado antes de escribir | E1: `wc -l` → 937, 877, 312, 247, 218; E1: Blazor sin §5 = 389; E2 l.374 («no se justifica: se parte»), l.375-376 (única excepción: §5), l.378 («calibrables») | S3 | 0,8 | Discusión en la 015 sobre si el documento cumple, con el mismo texto y dos lecturas | Criterio fijado por adelantado para la 015: `wc -l` total ≤ 600 cumple; si no, `total − §5` ≤ 600 **y** §0 declara la excepción con el motivo de por qué §5 no se puede partir; si tampoco, se parte en hermanos declarados en §9. Si el presidente quiere calibrar el techo, es intervención al framework y queda fuera (R1) |
| H-N2-06 | 002 §2 `fuera_de_alcance` («catalogar el documento en `Index-Knowledge.md`») frente a `Rules-Base-Conocimiento.md` §6.1 l.354 y l.357, §7.1 l.397 y §9.2 l.487 | **Tres ítems de §6.1 no son verificables contra nada del framework** para un documento que va a `OUTPUTs/`: (a) «existe la fila en `Index-Knowledge.md`» sólo puede evaluarse contra una **fila propuesta**; (b) «alias único en el índice de la base» sólo contra los cinco alias actuales y el catálogo de diseño, no contra la base que lo aloje; (c) la `Condicion-de-carga` se evalúa «contra el tipo D8 y los flags del proyecto de código» y **el bundle no lleva D8**: el propio framework declara ese mecanismo como residuo vivo sin corregir | E4 002 §2; E1: `grep -rn -i 'bundle-js' Conocimiento/Index-Knowledge.md SDD/Devs/References/Design/Index-Design-Rules.md` → 0; E2 `Rules-Base-Conocimiento.md` l.487 y l.495; E2 `PRODUCT-INTAKE-template.md` l.359 (`ev-02`); E2 `Coherencia-Proyecto-De-Otro-Ecosistema.md` §4, fila «Residuo vivo, declarado y no corregido» que nombra `Rules-Base-Conocimiento.md` l.487 y l.495 | S3 | 0,75 | La 015 marca tres ítems como «no aplica» sin criterio, o los da por cumplidos sin evidencia | El documento lleva **la fila propuesta** al pie de §9 (como texto, no como alta); la 015 compara los ocho campos contra esa fila con el comando de R-N2-01; la `Condicion-de-carga` se escribe contra la marca «insumo de construcción» de `Intake-Rules.md` §4 o queda `—` (carga sólo por cita), y §9 dice cuál de las dos y por qué |
| H-N2-07 | `INPUTs/Bundle-JS-En-Solucion-Blazor.md` §8.3 tabla l.244-247 y l.254-256 | La arista B de las dos PoC está **en prosa** —«que el contrato JS responde igual sin .NET», «que la RCL carga los dos JS y el mapa refleja la ocupación»— y «el `main.js` tiene que imitar las invocaciones que hace la RCL» no dice **cómo se verifica** que la imitación no divergió. Ninguna de las dos es una aserción evaluable ni distingue una falla de otra | E2 `INPUTs/…` l.244-247, l.254-256; E2 `Rules-Examples.md` l.369 («no es válido “verificar que el servicio responda correctamente”») y l.220-221 (doble del consumidor **derivado de una sola fuente**); E1 precedente: los tres literales invocados y el nombre del método de vuelta son extraíbles por `grep` (R-N2-09, R-N2-10) | S3 | 0,7 | Si el documento hereda la tabla, sus PoC son demos; el objetivo de diagnóstico del PO (`ev-05` l.25) no se cumple | En el documento, cada PoC lleva: qué carga, cómo se abre, qué invoca, **con qué aserción se decide** (código de salida o texto exacto en la salida) y **qué falla distingue** (R-N2-07, R-N2-08). El doble del consumidor de la PoC HTML se deriva de la lista de invocaciones del envoltorio, no se escribe a mano |

---

## 4. Lo que revisé y está bien

| # | Qué | Evidencia |
|---|---|---|
| 1 | **La forma de §6 ya tiene modelo aceptado.** `Rules-Base-Conocimiento.md` §6.1 marca cada ítem `[enumerable]` o `[interpretativo]`, y los dos documentos de referencia escriben su §6 con esa misma marca por ítem; el `Bundle-JS` tiene forma que copiar y la 015 tiene con qué separar lo que se cuenta de lo que se lee | E2 `Rules-Base-Conocimiento.md` l.352-365; E2 `Knowledge-Template-Blazor-…` l.822-837; E2 `Knowledge-Conformacion-…` l.166-173 |
| 2 | **El precedente prueba el bundle sin anfitrión, y la batería corre en una copia limpia.** Sin DOM, sin Blazor, sin navegador; contenedor de mentira; enganchada a la construcción del proyecto y apagable; 83 pruebas en verde en 263 ms | E1: `npm ci` + `npm test` sobre `git archive 9aabe5c` → `# tests 83 / # pass 83 / # fail 0`, `exit=0`; E2 csproj l.80-99; E2 `ts/componente.test.ts` l.1-8; E2 `ts/frontera.test.ts` l.48-55 (cero `ProjectReference`, verificado por la propia batería) |
| 3 | **La costura del precedente es verificable por comando y hoy coincide.** Literales invocados ⊂ exportaciones; nombre del método de vuelta igual de los dos lados; ruta `_content/` igual al `AssemblyName`; versión del contrato `1` de los dos lados; cero red y cero almacenamiento en el bundle; cero bundle versionado; cero referencias de proyecto | E1 salidas V-1 a V-9 (§2, R-N2-09 a R-N2-14): `ok destruir`, `ok montarConInterop`, `ok render`; `JSInvokable("AlActivarPunto")` = `nombreDelMetodo = "AlActivarPunto"`; `_content/<F1>.<Componente>/` = `<AssemblyName><F1>.<Componente>`; l.114 = l.114; `0`; vacío; `0` |

---

## 5. Solicitudes de convocatoria

| # | Rol | Señal, con ubicación | Qué no puedo afirmar sin él |
|---|---|---|---|
| SC-N2-01 | V1 Arquitectura y V3 Operación y entrega (ya convocadas) | `Intake-Rules.md` §4 l.123-141 (pertenencia, dos clases de arista, único generador) y `Rules-Devops.md` §4.9 punto 4 l.284 (modo de construcción donde falta la cadena), ambos de 13.16; el documento de entrada §2.3 y §8.4 los contradicen (H-N2-01) | Si el árbol de §8.4 del documento de entrada y el «orden de build: bundle → RCL → Web» siguen siendo válidos con la clase «insumo de construcción», y cómo se verifica la construcción de la PoC Blazor en un ambiente sin la cadena de JavaScript |
| SC-N2-02 | V2 Interfaz / consumidor externo (ya convocada) | `ts/<componente>.ts` l.33-37 (`export * from` seis módulos) hace que la superficie construida exponga **doce** funciones contra **cinco** operaciones del contrato (`Contratos-Abstractions-…` §3); E1 en R-N2-19 | Si la superficie pública del artefacto es la del contrato o la del punto de entrada, que es lo que decide qué tiene que cubrir el conjunto de PoC (`Rules-Examples.md` l.222-223) |
| SC-N2-03 | AH-1 Ingeniería del bundle (ya convocada) | H-N2-02: la cadena del precedente emite una sola superficie; el `--format=iife` de la misma cadena sí sirve desde `file://` (E3) | Si la segunda superficie es la forma correcta de darle a la PoC HTML un modo «sin servidor», o si la PoC HTML debe declararse «servida» y consumir el módulo ES |

---

## 6. Preguntas al Product Owner

| # | Pregunta cerrada | Opciones | Conjetura que la funda (`C`) |
|---|---|---|---|
| P-N2-01 | ¿La PoC HTML tiene que abrirse **sin servidor** (doble clic sobre el archivo), o alcanza con que se sirva desde un servidor estático? | (a) sin servidor: el bundle emite además una superficie global y la PoC la carga por etiqueta de script; (b) servida: la PoC importa el módulo ES, el mismo que la RCL; (c) las dos, con el mismo `main.js` | `C`: el pedido dice «sin .NET» (`ev-05` l.37, documento de entrada l.198-199) y no dice «sin servidor»; `Rules-Examples.md` l.218-219 admite ambos modos y obliga a declarar uno. Sin la respuesta, R-N2-16 y H-N2-02 no se pueden cerrar |
| P-N2-02 | Dado que el documento no se cataloga, ¿la `Condicion-de-carga` se escribe igual, para el fork que lo adopte, o queda `—`? | (a) `—`, carga sólo por cita del intake; (b) expresada contra la marca «insumo de construcción» de `Intake-Rules.md` §4; (c) expresada contra un flag que el intake todavía no tiene | `C`: el framework evalúa la condición contra el D8 del proyecto de código, que el bundle no tiene (H-N2-06); qué quiere el PO que herede el fork es decisión suya |

**Observación al Product Owner, no pregunta** (H-N2-04): `Relevar-Conocimiento.md` no existe y la lista §6.1 tiene incumplimientos en la base publicada. No se corrige en este expediente por R1.

Sigue: consolidación · presidente de mesa
