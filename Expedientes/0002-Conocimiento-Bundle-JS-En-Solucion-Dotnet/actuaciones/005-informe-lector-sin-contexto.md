# Actuación 005 — Informe Comisión N3, Lector sin contexto / implementador ingenuo

| Campo | Valor |
|---|---|
| Tipo | `informe` |
| Fecha | 2026-09-13 |
| Autor | Comisión N3, Lector sin contexto / implementador ingenuo |
| Corrige | — |

Base: `IA.SDD` `a501857` (13.17), según el encargo común. Rutas abreviadas en las anclas: `ev-05` es `evidencia/ev-05-presentacion-original.md`; `INPUT` es `IA.SDD.Documentacion/PROMPTs/SDD/Catalogado/02-Extraccion-Concepto-Espcificacion-Integracion-Proyecto-Bundle-JS/INPUTs/Bundle-JS-En-Solucion-Blazor.md`; `RBC` es `SDD/Devs/Rules/Rules-Base-Conocimiento.md`; `REx` es `SDD/Devs/Rules/Rules-Examples.md`; `<Componente>/` es `<repo-privado-F1>/src/<F1>.<Componente>/`; `ADR-42` y `ADR-43` son los dos ADR del precedente; `Contratos` es `Contratos-Abstractions-<F1>-<Componente>.md`; `MaqREADME` es `SDD/Maquetas/<F1>-Web/README.md` del precedente.

---

## 1. Mandato y método

**Quién soy en este informe.** Un agente de otro framework de especificación, con sus PoC en `/demos` y su documentación en `/docs`, que recibe `Knowledge-Bundle-JS.md` y tiene que crear cuatro piezas —el proyecto del bundle, la PoC HTML, la biblioteca de clases Blazor y la PoC Blazor— **sin preguntar nada**. Respondo dos cosas: qué tendría que decir el documento para que eso sea posible, y qué de los insumos me obligaría a preguntar o me impondría una estructura que no es la mía. No juzgo si el framework SDD está bien.

**Qué leí, en este orden.**

| # | Insumo | Ruta |
|---|---|---|
| 1 | Encargo común | `evidencia/ev-07-cartas-despachadas/00-encargo-comun.md` |
| 2 | Mi carta | `evidencia/ev-07-cartas-despachadas/03-N3-lector-sin-contexto.md` |
| 3 | Presentación original | `evidencia/ev-05-presentacion-original.md` |
| 4 | Documento de entrada v0.2 | `INPUT` |
| 5 | Actuaciones 001 y 002 | `actuaciones/001-presentacion-del-product-owner.md`, `actuaciones/002-providencia-convocatoria-de-mesa.md` |
| 6 | Evidencia levantada | `evidencia/ev-02-citas.out`, `ev-03-precedente.out`, `ev-04-input.out`, `ev-06-chequeo-mecanico.out` |
| 7 | Formato obligatorio | `RBC` completo |
| 8 | Modelo de la base | `Conocimiento/README.md`, `Conocimiento/Index-Knowledge.md` |
| 9 | Dos documentos publicados, como forma | `Conocimiento/Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md`, `Conocimiento/Knowledge-Conformacion-Pull-Request-Manual.md` |
| 10 | Precedente | `<Componente>/<F1>.<Componente>.csproj`, `Contrato.cs`, `ts/contrato.ts`, `ts/<componente>.ts`, `DiagramaDeCabecera.razor`, `package.json`, `.gitignore`, `tsconfig.json`, `tsconfig.pruebas.json`; `ADR-42`, `ADR-43`, `Contratos`, `MaqREADME`; y la cabecera de `SDD/Maquetas/<F1>-Web/assets/vendor/<componente>.js` |
| 11 | Conjunto normativo, sólo lo que el caso toca | `SDD/Devs/Rules/Vocabulario-Rules.md` completo; `REx` §0 a §4.6; `Intake-Rules.md` §4; `PRODUCT-INTAKE-template.md` §13 a §16; `PRODUCT-MANIFEST-template.md` §1.2, §2.B, §3; `Rules-Devops.md` §4.9; `Master-Prompt.md` fila B2 de la tabla de fases; `README.md` invariantes D3, D7, D8, D9 y párrafo de autosuficiencia; `SDD/Devs/Guides/Coherencia-Proyecto-De-Otro-Ecosistema.md` |

**Qué no leí y por qué.** Ningún informe de otra comisión: en `actuaciones/` sólo existen 001 y 002 (`ls` del 2026-09-13), y la regla 1 del encargo lo prohíbe aunque existieran. No leí `Maqueta-Rules.md`, `Rules-Devops.md` fuera de §4.9, `Rules-Backlog-Tecnico.md` ni `Marco-Teorico-SDD.md`: no cambian lo que un lector externo necesita del documento, y las citas que el documento de entrada hace de ellos ya las verificó la compuerta mecánica (`ev-06`). No leí `IA/PROMPTs/IA.Prompts/Base/Mesa-Evaluadora.md`: es del método de la mesa, no del objeto.

**Método.** Para cada pieza que tengo que construir, anoté qué necesito saber, busqué si algún insumo lo dice, y si dos insumos dicen cosas distintas o ninguno lo dice, es hallazgo. Para el vocabulario, conté las ocurrencias de términos del SDD en el documento de entrada y verifiqué si el conjunto normativo los define en términos que un forastero pueda mapear. Hechos de plataforma anclados en el precedente (`E2`) o citados por nombre de producto y tema, como manda la regla 3 del encargo.

---

## 2. Relevamiento para el documento `Bundle-JS`

Sección destino según `RBC` §4.2 (§0 a §9). Todo lo que sigue está escrito desde la pregunta «¿puedo construir la pieza sin preguntar?».

| Id | Sección destino | Qué tiene que decir | Ancla (nivel + cita) | Riesgo si falta |
|---|---|---|---|---|
| R-N3-01 | §0 | En una oración: caracteriza **un proyecto de código JavaScript o TypeScript que emite un bundle sin dependencias de ejecución, consumido por una biblioteca de componentes Razor dentro de una solución .NET, y sus dos anfitriones de prueba: una página HTML sin .NET y una app Blazor mínima**. Y qué queda afuera, con nombre: la estructura del repositorio y de la solución .NET; dónde vive cada PoC en el framework que adopte el documento; convertir la biblioteca en paquete NuGet; la lógica del dominio que el bundle dibuja; la maqueta de validación visual del SDD | E2 `ev-05` l.35-43; E2 actuación 002 §2 `restricciones_duras` R2 y `fuera_de_alcance`; E2 `RBC` §4.2 l.248 y l.260 («sin el qué queda afuera, el agente completa el hueco por su cuenta») | Un lector externo completa el hueco con su propio método y el documento deja de transmitir lo que el PO quiere |
| R-N3-02 | §0 | Una **tabla de equivalencias de vocabulario** con tres columnas: término del SDD → qué significa en palabras neutras → cómo lo llama o dónde lo decide el framework que lee. Como mínimo: unidad de entrega, proyecto de código, solución de código, `redistribuible`, tipo D8 y `library`, categoría 10 y `/samples`, Fase B2 y maqueta, arista A y arista B, contrato de verificación, insumo de construcción, `Unidades-Entrega/`, prefijo `Aplicada` | E2 mi carta l.7; E1 recuento sobre `INPUT` (`grep -o -i`): «unidad de entrega» 11, «proyecto de código» 10, «solución de código» 5, «redistribuible» 7, «samples» 14, «arista» 7, «D8» 3, «Fase B2» 3, «Unidades-Entrega» 4; E2 `Vocabulario-Rules.md` §2 l.28-35 define los seis términos por frontera y §7 l.148-155 da su correspondencia de industria (C4 *container*, MSBuild *project*, npm *package*), que es la forma en que un forastero los puede mapear | Sin la tabla, cada término es una pregunta. Ver H-N3-06 |
| R-N3-03 | §0 | Declarar que **toda ruta del documento es ilustrativa** salvo dos: la estructura interna del proyecto del bundle y la estructura interna de cada PoC. Dónde se ponen esas carpetas en el repositorio lo decide quien lee (ejemplo del PO: `/demos`, no `/samples`) | E2 `ev-05` l.41; E2 actuación 002 §2 R2; E2 `RBC` §0.1 l.51-56, fila «Condición de aplicabilidad del conocimiento, declarada en el índice» | El árbol de `INPUT` §4 l.97-138 se lee como norma y me impone `samples/`, `tests/`, `SDD/Docs/`, `AGENTS.md`. Ver H-N3-06 |
| R-N3-04 | §0 | Verificación de ofuscación declarada: términos del precedente que **no** deben viajar al documento —el nombre del producto y de la organización de origen, el prefijo de clases CSS del componente, la familia de códigos de error, los identificadores `CU-`, `RN-`, `BT-`, `ADR-` del producto— y los falsos positivos léxicos, uno por uno | E2 encargo l.25; E2 `RBC` §2.2 l.162-166 y §6.1 l.358; E2 `<componente>.ts` l.12-14 y l.114-121 (cita `CU-00043`, `RN-00042`, `E-PIN-00044` en comentarios de código) | Un esqueleto copiado del precedente arrastra identificadores que no significan nada afuera y nombres de cliente que el repositorio público prohíbe |
| R-N3-05 | §1 | Stack y supuestos, en tabla: .NET con Blazor y render interactivo en servidor (el precedente no prueba WebAssembly); biblioteca de clases Razor como proyecto que aloja el envoltorio; **Node es requisito de construcción, no de ejecución**; el bundle **no tiene dependencias de ejecución** y sus dependencias de desarrollo son del orden de tres; el navegador del anfitrión soporta módulos ES | E2 `csproj` l.25-32 y l.41; E2 `ADR-42` §6 l.67-68 y §8 l.83-85; E2 `DiagramaDeCabecera.razor` l.56-57 (`import` de módulo) | Sin los supuestos no sé si el conocimiento aplica a mi caso (por ejemplo, un anfitrión WebAssembly o un equipo sin Node) |
| R-N3-06 | §1 | **Los dos anfitriones de prueba y qué prueba cada uno**, en tabla: la PoC HTML prueba el contrato JavaScript y el ciclo montar → render → destruir con un doble del lado .NET, sin .NET; la PoC Blazor prueba el ciclo de vida del envoltorio, la interop en las dos direcciones y que el estático se sirve desde la biblioteca. Son dos porque prueban el mismo contrato desde lados distintos | E2 `ev-05` l.37-39; E2 `INPUT` §8.3 l.244-247 (tabla) y l.251-253; E2 `ADR-43` §1 l.26-31; E2 `REx` §3.6 l.210-223 | Si el documento no dice qué prueba cada una, hago una sola o hago dos que prueban lo mismo |
| R-N3-07 | §1 | Distinguir **tres verificaciones que no se confunden**: (a) pruebas unitarias del bundle sin DOM, sin navegador y sin Blazor, enganchadas a la construcción del proyecto; (b) la PoC HTML, en navegador, sin .NET; (c) la PoC Blazor, con .NET. La (a) no es una PoC y las PoC no reemplazan a la (a) | E2 `csproj` l.80-99 (batería «sin anfitrión: sin DOM, sin Blazor y sin navegador», `AfterTargets="Build"`, interruptor `-p:PruebasDeTypeScript=false`); E2 `package.json` l.9 (`node --test build/*.test.js`); E2 `INPUT` §8.3 l.246 («que el contrato JS responde igual sin .NET») nombra la (b) con palabras que también describen la (a) | Confundo «sin anfitrión» con «sin .NET» y construyo la PoC HTML como si fuera la batería unitaria, o al revés |
| R-N3-08 | §2 | Layout interno del proyecto del bundle, en tabla, pieza por pieza: carpeta de fuentes (`ts/`) con los módulos y sus pruebas al lado (`*.test.ts`); `contrato.ts` como archivo aparte; hoja de estilos propia en `css/`; `package.json` con `"private": true` y versión `0.0.0` porque no se publica; archivo de bloqueo versionado; dos `tsconfig` (uno con `noEmit` para el build, otro que emite a `build/` sólo para las pruebas); salida a `wwwroot/` **no versionada**; `node_modules/` y `build/` ignorados y excluidos de los ítems por omisión del proyecto .NET | E2 `ev-03-precedente.out` (árbol); E2 `<Componente>/.gitignore` (4 líneas: `node_modules/`, `wwwroot/<componente>.js`, `wwwroot/<componente>.css`, `build/`); E2 `tsconfig.pruebas.json` l.2 (comentario «este archivo es el único que emite, y solo hacia build/»); E2 `csproj` l.22 y l.107-112; E2 `package.json` l.2-3 | Sin el layout, versiono el bundle, emito las pruebas al mismo lugar que el build, o meto `node_modules` en el artefacto publicado |
| R-N3-09 | §2 | **Dónde vive el bundle respecto de la biblioteca Razor**, como decisión con dos opciones y criterio: (A) en el mismo proyecto de código, carpetas hermanas, el `dotnet build` del proyecto ejecuta el empaquetado —el precedente—; (B) proyecto hermano de otro ecosistema cuyo artefacto la biblioteca toma como insumo de construcción, en la misma solución. Y qué queda prohibido en las dos: la copia manual del artefacto | E2 `ADR-42` §2 l.34 y l.40 («el envoltorio Blazor y el TypeScript que consume viven en el mismo proyecto, con los tipos declarados una vez de cada lado en carpetas hermanas»); E2 `PRODUCT-MANIFEST-template.md` §1.2 l.88-101 (ejemplo de «un paquete npm cuyo artefacto un proyecto .NET toma como insumo de construcción»); E2 `INPUT` §8.4 l.268-275 (opción B con copia a `wwwroot/js/`) | Es la primera decisión de la construcción y los insumos la resuelven distinto. Ver H-N3-02 |
| R-N3-10 | §2 | Los **dos archivos del contrato**, hermanos y con la misma forma: registros C# de un lado, interfaces y uniones de literales TypeScript del otro, un tipo por lado, nombres y formas iguales a propósito; «si dejan de coincidir hay que cambiar los dos archivos y el que se olvide se nota al serializar» | E2 `Contrato.cs` l.3-10; E2 `contrato.ts` l.1-9; E2 `Contratos` §1 l.30 («cruza dos lenguajes ... los tipos se declaran una vez de cada lado») | Sin esto declaro el contrato en un solo lado o intento generarlo, y el documento no dice cuál de las dos cosas es la forma |
| R-N3-11 | §2 | Un **punto de entrada único** del bundle que exporta la API pública (funciones planas que reciben el contenedor) y reexporta los tipos del contrato; los módulos internos no se exportan por separado | E2 `<componente>.ts` l.32-37 (reexportaciones) y l.281-318 (cinco funciones exportadas: `montar`, `montarConInterop`, `render`, `seleccionar`, `destruir`) | Sin punto de entrada único el empaquetador no sabe qué incluir y el envoltorio importa rutas internas |
| R-N3-12 | §2 | Layout interno de cada PoC. **PoC HTML**: una página, un script anfitrión, datos por escenario en archivos separados, el doble del lado .NET, un `README` con cómo se abre y qué se espera ver; carga el artefacto **construido desde la salida del proyecto que lo produce**, nunca una copia. **PoC Blazor**: app Blazor mínima con referencia de proyecto a la biblioteca, una página que aloja el componente, los mismos datos por escenario | E2 `REx` §3.6 l.212-221 («anfitrión mínimo»: carga el artefacto construido y nunca una copia; invoca la superficie pública y nada más; datos por escenario, salida esperada y prueba); E2 `MaqREADME` l.49-52 (forma de la separación datos / render / adaptador / vendor, como referencia de forma); E2 `ev-05` l.37-39 | Sin el layout copio el bundle a la PoC (que es lo que la regla y el precedente prohíben) o la PoC reimplementa lógica del anfitrión real |
| R-N3-13 | §3 | **Operaciones mínimas de la superficie pública**, en tabla: montar (contenedor, opciones) → instancia; montar con interop (contenedor, referencia .NET, nombre del método de vuelta); dibujar (contenedor, DTO completo: «todo cambio es un render completo, no hay mutadores parciales»); seleccionar; destruir. **Las instancias se indexan por contenedor** en un `WeakMap` del módulo y **nunca se sostienen del lado .NET**: el lado servidor trabaja siempre contra el elemento | E2 `<componente>.ts` l.39-44 y l.281-318; E2 `contrato.ts` l.166-171; E2 `Contratos` §3 l.47-53 | Sin esta forma intento guardar la instancia en C# (no se puede) o expongo una clase en vez de funciones sobre el contenedor |
| R-N3-14 | §3 | **Regla de la costura de datos**: propiedades PascalCase en C# ↔ camelCase en TypeScript, porque la serialización de la interop las convierte por omisión; uniones de literales de cadena en TS ↔ clases estáticas de `const string` en C#, **no `enum`**, «porque cruzan a JSON sin ceremonia y el conjunto lo fija el contrato y no el sistema de tipos de .NET»; campos opcionales `?` ↔ parámetros nullable con valor por omisión; un campo `version` fijo en el DTO raíz | E2 `Contrato.cs` l.8-10, l.48-54, l.108-115; E2 `contrato.ts` l.6-8, l.23-33, l.113-119 (`posicion` contra `Posicion`, `version: 1`); hecho de stack citado por nombre: documentación oficial de ASP.NET Core Blazor, tema «JavaScript interop», serialización con `System.Text.Json` y política de nombres camelCase por omisión | El precedente **aplica** la regla de nombres sin enunciarla en ningún archivo. Sin enunciarla, escribo `posicion` en C# o `Posicion` en TS y el acuse llega vacío sin error. Ver H-N3-07 |
| R-N3-15 | §3 | **El callback de vuelta**: el método C# lleva `[JSInvokable("<Nombre>")]` con el nombre **escrito explícito**, y el mismo literal se pasa a la función de montaje con interop; «van juntos a propósito: si alguno cambia, el acuse deja de llegar y sólo se nota en ejecución» | E2 `DiagramaDeCabecera.razor` l.73-77; E2 `<componente>.ts` l.296-306; E1 `grep -n 'JSInvokable("' DiagramaDeCabecera.razor` → l.76 `"AlActivarPunto"`; `grep -n 'nombreDelMetodo = ' ts/<componente>.ts` → l.299 `"AlActivarPunto"` | Sin la regla, un lector usa el nombre del método C# (`RecibirAcuse`) y el bundle invoca el atributo (`AlActivarPunto`): falla silenciosa |
| R-N3-16 | §3 | **El doble del lado .NET para la PoC HTML**: un objeto con `invokeMethodAsync(nombre, ...args)` que devuelve una promesa, que registra o pinta lo recibido; su lista de nombres invocables **se deriva de una sola fuente** (la misma que la biblioteca Razor). La firma del precedente tipa la referencia .NET de forma estructural, así que el doble se construye sin ningún tipo de .NET | E2 `<componente>.ts` l.296-299 (`refDotNet: { invokeMethodAsync(metodo: string, ...args: unknown[]): Promise<void> }`); E2 `REx` §3.6 l.220-221 («doble del consumidor con la lista de invocaciones que ese proyecto expone, derivada de una sola fuente»); E2 `ev-05` l.17 («registrar callback blazor en la clase de interfaz») | Es lo que hace que «como lo haría Blazor» sea construible sin .NET. Sin esto, la PoC HTML invoca una API propia y deja de probar lo que Blazor usa (`INPUT` l.254-256) |
| R-N3-17 | §3 | **Ciclo de vida del envoltorio Razor**: en el primer render importa el módulo, crea la referencia .NET y monta; en cada render con datos llama a dibujar; en `DisposeAsync` libera **en este orden**: destruir la instancia del navegador, liberar el módulo, liberar la referencia .NET; tolera que el circuito ya se haya caído. Los parámetros son el DTO y un `EventCallback` del acuse; el componente «no dibuja nada por su cuenta» | E2 `DiagramaDeCabecera.razor` l.12-15, l.28-35, l.52-71, l.79-98 (`JSDisconnectedException`) | Sin el orden y sin la tolerancia, quedan referencias vivas o el dispose lanza al cerrar el circuito |
| R-N3-18 | §3 | **Ruta del estático**: el bundle se sirve bajo `_content/<nombre del ensamblado>/<archivo>.js` sin configuración en el anfitrión y sin recurso remoto; el envoltorio lo importa por esa ruta relativa | E2 `DiagramaDeCabecera.razor` l.16-18 y l.56-57; E2 `ADR-42` §2 l.39; hecho de stack citado por nombre: documentación oficial de ASP.NET Core, tema «Razor class library» y «static web assets» | Sin la ruta, configuro estáticos a mano o la busco en `wwwroot/js/` del anfitrión, que es lo que `INPUT` §8.4 l.275 sugiere |
| R-N3-19 | §3 | **Integración con la construcción .NET** (cuando el bundle vive en el proyecto Razor): verificar la presencia y la versión del entorno JavaScript declarada en el proyecto; instalar contra el archivo de bloqueo (`npm ci`, nunca instalación flotante) con entrada y salida para no repetir; **construir el bundle antes del descubrimiento de estáticos** («`BeforeBuild` solo no alcanza, porque el descubrimiento de estáticos corre antes»); copiar la hoja de estilos con el mismo criterio; correr las pruebas después de la construcción con un interruptor para apagarlas | E2 `csproj` l.25-32, l.34-42, l.44-52, l.54-66 (`BeforeTargets="ResolveStaticWebAssetsInputs;BeforeBuild"`, `Inputs`/`Outputs`), l.68-78, l.80-99; hecho de stack citado por nombre: documentación oficial de MSBuild, tema «targets», `BeforeTargets`, `Inputs` y `Outputs` | Es el punto que un lector externo no adivina: si el bundle se genera después del descubrimiento, «el manifiesto lo registra sin que el archivo esté en disco y el módulo falla al pedirlo» |
| R-N3-20 | §3 | **Modo de construcción sin la cadena JavaScript**: declarar en qué ambientes falta Node y qué pasa ahí —o un modo explícito y nombrado que recibe el bundle ya generado, o la construcción **falla nombrando la cadena**; nunca termina bien sin el bundle | E2 `Rules-Devops.md` §4.9 punto 4 l.284; E2 `csproj` l.40-41 (error que nombra la cadena) | Sin esto, un pipeline sin Node entrega la biblioteca sin su bundle y nadie lo nota hasta ejecución |
| R-N3-21 | §3 | **Lo que el bundle no hace**: no hace peticiones, no accede a almacenamiento, no conoce la red, no conoce el dominio del anfitrión, no valida lo que el anfitrión ya validó: **declara** («un estado que nunca se pinta es un estado que nunca se valida»); los colores salen como variables CSS con valor por omisión que el anfitrión remapea | E2 `ADR-43` §2 l.40; E2 `csproj` l.4-6; E2 `Contratos` §4 l.75 y §5 l.79-86; E2 `<componente>.ts` l.244-268 (estados declarados en el contenedor) | Sin la frontera, el bundle absorbe reglas del negocio o hace red, y la PoC HTML deja de ser posible sin servidor |
| R-N3-22 | §3 | **Cómo se abre la PoC HTML**, declarado de una de dos formas: **sin servidor** (archivo local, sin red, sin credenciales) o **servida** por un servidor estático. Y la consecuencia del formato: un bundle en módulo ES **no carga desde archivo local** en los navegadores; abrir sin servidor exige una segunda salida global clásica, o se sirve | E2 `REx` §3.6 l.218-219; E2 `ADR-43` §1 l.29-31 («si el componente sólo se publicara como módulo, la maqueta no podría demostrarlo») y §2 l.35-38; E2 `MaqREADME` §1 l.24-38 (cuatro formas de abrir, y l.36 la limitación sobre `file://`); hecho de stack citado por nombre: MDN Web Docs, tema «JavaScript modules», carga local y política de mismo origen | Sin esto, abro `index.html` con doble clic y el `import` falla. Ver H-N3-04 |
| R-N3-23 | §3 | **Cada PoC declara qué verifica, con qué comando y contra qué aserción evaluable por máquina** (código de salida, respuesta, o instantánea comparable), sus precondiciones, y la evidencia de la última corrida con fecha. En vocabulario neutro: «un comando, una aserción, una salida con fecha» | E2 `REx` §4.6 l.335-342 y l.369; E2 `REx` §0.1 l.45 («un sample cuyo criterio de aceptación falla es un hallazgo, no un documento pendiente») | Sin la aserción, la PoC es una demo y no diagnostica nada, que es lo que el PO pide que haga (`ev-05` l.37, l.39) |
| R-N3-24 | §4 | Tabla de bifurcaciones resueltas, con criterio: empaquetar dentro de la construcción .NET vs paso aparte («alguien tiene que acordarse de correrlo»); TypeScript vs JavaScript a mano (la alternativa se declara legítima, no de paja: «cero dependencias nuevas» contra «se pierde el tipado del contrato entre los dos lados»); empaquetador minimalista vs de propósito general; bundle generado en cada compilación vs versionado; dos superficies de consumo vs una; instancias por contenedor vs sostenidas en .NET; literales de cadena vs `enum`; pruebas junto a la fuente vs carpeta de pruebas aparte; acuse emitido por dos vías (función y evento DOM que burbujea) | E2 `ADR-42` §4 l.51-56 y §7 l.77; E2 `ADR-43` §4 l.50-53; E2 `<componente>.ts` l.39-44 y l.143-155; E2 `Contrato.cs` l.8-10 | Sin las decisiones tomadas, las vuelvo a tomar, y distinto |
| R-N3-25 | §4 | Marcar cuáles de esas decisiones son **decisión de stack sustituible** (el empaquetador concreto, el compilador, el corredor de pruebas) y cuáles son **forma que no se sustituye** (contrato doble, instancias por contenedor, artefacto construido y nunca copiado, orden de liberación, nombre del callback explícito de los dos lados) | E2 `RBC` §0.3 l.83-88 (método vs decisión de stack) y §0.4 l.90-103; E2 `ADR-42` §2 l.43 | Un lector con otro empaquetador no sabe qué puede cambiar sin romper lo que el documento enseña |
| R-N3-26 | §5 | Esqueletos **ofuscados** y mínimos: `package.json` con los tres scripts; los dos `tsconfig`; `.gitignore`; el bloque de targets del proyecto Razor; `Contrato.cs` y `contrato.ts` con un DTO de entrada y un acuse de salida; punto de entrada con las cinco funciones y el `WeakMap`; envoltorio Razor completo; `index.html` con script anfitrión y doble; página Blazor mínima que aloja el componente. Si §5 supera el techo, declararlo en §0 con su motivo | E2 `RBC` §4.2 l.253, §4.3 l.264-267, §6.2 l.374-376; E2 `Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md` l.37-39 (precedente de la excepción declarada); E2 `RBC` §2.2 l.153-155 («las formas constructivas del HTML, del CSS y del JavaScript se transmiten mal en prosa») | Sin esqueletos, reconstruyo los targets y el envoltorio desde cero y los errores de orden (R-N3-17, R-N3-19) reaparecen |
| R-N3-27 | §6 | Criterios enumerables: **un** comando construye todo desde limpio; **cero** archivos de bundle versionados; **cero** dependencias en el artefacto publicado; la batería sin anfitrión corre y pasa; la PoC HTML corre sin .NET y su aserción pasa; la PoC Blazor sirve el estático bajo `_content/` y el acuse llega al `EventCallback`; el literal de `[JSInvokable]` y el nombre pasado al montaje coinciden (comprobable con `grep`); el DTO cruza con el mismo nombre de campo en los dos lados; en dispose no quedan escuchas ni referencias vivas | E2 `ADR-42` §8 l.81-87; E2 `ADR-43` §8 l.76-81; E2 `DiagramaDeCabecera.razor` l.73-77; E2 `<componente>.ts` l.128-141 | Sin criterios enumerables, «se aplicó bien» es opinión |
| R-N3-28 | §7 | Anti-patrones con motivo: copiar el bundle a mano al anfitrión o a la PoC («se desincroniza y nadie se entera hasta que la maqueta valida un componente que ya no es el que corre»); editar la copia; generar el bundle después del descubrimiento de estáticos; `npm install` flotante; tomar la versión de Node de la máquina; sostener la instancia del lado .NET; olvidar liberar uno de los dos lados; `enum` en el contrato; validar en JS lo que el anfitrión valida; enganchar la verificación de una PoC a la construcción de la solución; nombre del callback escrito una sola vez | E2 `MaqREADME` l.159; E2 `csproj` l.44-47, l.54-59; E2 `DiagramaDeCabecera.razor` l.80-83; E2 `<componente>.ts` l.128-131; E2 `REx` §3.6 l.199-203; E2 `ADR-42` §4 l.56 | Son los errores que un implementador ingenuo comete primero |
| R-N3-29 | §8 | **Qué es del método SDD y no del artefacto**, para que otro framework lo reemplace por el suyo: la clasificación en unidad de entrega y proyecto de código; el tipo D8 y `library`; `redistribuible` y la obligatoriedad de samples; `/samples` y la categoría 10; el contrato de verificación con identificador `VER-`; la clase de arista «insumo de construcción» y el único generador; el perfil de nombres por ecosistema. Decir explícitamente: **el artefacto no depende de esa clasificación**; lo que depende es dónde el SDD lo documenta | E2 `RBC` §4.2 l.256 y l.260-262 (§8 no se negocia); E2 `RBC` §0.1 l.47-49 («describe el artefacto, no el método»); E2 `Intake-Rules.md` §4 l.123-141; E2 `REx` §0 l.16-24 | Sin §8, el documento se lee como norma SDD y me impone `library`, `redistribuible` y `/samples`. Ver H-N3-06 |
| R-N3-30 | §8 | Declarar las dos **desviaciones del precedente respecto de la estructura del producto** para que un lector externo no las tome como regla: las pruebas del bundle viven junto a la fuente y no en `/tests` («excepción declarada en §16 del intake y acotada a este proyecto»); el proyecto Razor del bundle no referencia ningún otro proyecto de código | E2 `ADR-42` §7 l.77; E2 `csproj` l.4-6 | Sin declararlas, o las copio como regla o las corrijo contra el precedente |
| R-N3-31 | §9 | Precedente citado ofuscado como fuente del relevamiento; los tres archivos de reglas de los que el documento toma la forma de las PoC; sin artefacto de referencia depositado (o, si se deposita, declarado); sin hermanos; consumidor declarado | E2 encargo l.25; E2 `RBC` §3.3 l.210-212 y §2.2 l.153-156 | Sin trazabilidad no se puede volver a la fuente cuando el precedente cambie |
| R-N3-32 | §1 y §3 | **Identidad del paquete** del bundle en su ecosistema: misma raíz que el código .NET, en la forma que el ecosistema exige (minúscula y guion), `"private": true`, sin versión propia porque se versiona con el producto; «no existe la situación de un anfitrión viejo contra un componente nuevo» | E2 `package.json` l.2-3; E2 `PRODUCT-MANIFEST-template.md` §1.2 l.94-101; E2 `Contratos` §6 l.92-96 | Sin esto publico o versiono el bundle por separado y aparece la matriz de compatibilidad que el precedente evita |

---

## 3. Hallazgos

Sobre los insumos y sobre el encaje con el framework, desde la única pregunta de mi mandato: ¿me obliga a preguntar, o me impone una estructura que no es la mía?

| Id | Ubicación | Afirmación | Evidencia (nivel + ancla) | Severidad | Confianza | Impacto si no se corrige | Dirección propuesta |
|---|---|---|---|---|---|---|---|
| H-N3-01 | `ev-05` l.17, l.36-38; `INPUT` §8.1 l.198-199 y §8.4 l.271, l.278 | **`main.js` nombra dos cosas distintas y el precedente no tiene ninguna.** En `ev-05` l.36 es **un artefacto que emite el proyecto del bundle** («arrojará dos artefactos: main.js y el bundle»), y en l.38 la biblioteca Razor «integra el main.js y el bundle». En `ev-05` l.37 y en `INPUT` l.198 es **el script anfitrión de la PoC HTML** («una página con un main.js que invoca el bundle como lo haría Blazor»). En `INPUT` §8.4 l.271 el `dist/` tiene «los dos artefactos JavaScript» sin nombrarlos, y l.278 pone `main.js` en la PoC. En el precedente hay **un** artefacto (`<componente>.js`, módulo ES) cuya capa de interop es una función del mismo módulo (`montarConInterop`), y el adaptador del anfitrión de la maqueta es de la maqueta y no viaja | E2 `ev-05` l.17, l.36, l.37, l.38; E2 `INPUT` l.198-199, l.271, l.278; E2 `<componente>.ts` l.291-306; E2 `MaqREADME` l.51 («Es de la maqueta y no se traslada al producto»); E1 `find src SDD/Maquetas -name main.js -not -path '*/node_modules/*' \| wc -l` → `0` | S1 | 0,9 | No sé si tengo que construir un segundo artefacto en el bundle (una capa de interop separada del dibujo) o un script en cada PoC. Es la primera pregunta que haría, y decide el layout de §2 y la API de §3 | El documento fija **un** modelo y le da nombres distintos a las dos cosas: la **capa de interop** (parte del bundle o segunda salida, con la función de montaje con interop y el nombre del callback) y el **script anfitrión** de cada PoC (que hace el papel del envoltorio Razor). Si el PO quiere dos artefactos emitidos, el documento lo dice y lo justifica contra el precedente, que emite uno. Pregunta P-N3-01 |
| H-N3-02 | `INPUT` §8.4 l.266-283; `ADR-42` §2 l.34, l.40-41; `REx` §3.6 l.214-215 | **Los dos insumos ponen el bundle en lugares distintos, y el del documento de entrada contradice al precedente y a la regla.** `INPUT` §8.4: proyecto `MapaAsientos/` (workspace npm) hermano de `MapaAsientos.Blazor/` (RCL), con `dist/` que la RCL «recibe» en `wwwroot/js/` (l.275). El precedente: **un solo proyecto de código** Razor que contiene `ts/` y emite a su propio `wwwroot/` durante el `dotnet build`, con la consecuencia 4 «el bundle no se versiona: no hay artefacto generado en el árbol que pueda quedar desincronizado». «Recibir el `dist/`» es una copia, y `REx` §3.6 l.214-215 prohíbe la copia para el anfitrión mínimo por la misma razón | E2 `INPUT` l.268-275; E2 `ADR-42` l.34, l.40-41, l.76; E2 `csproj` l.17-23 y l.60-66; E2 `REx` l.214-215; E2 `MaqREADME` l.159 | S1 | 0,9 | Sin decidirlo, el documento enseña dos layouts y un mecanismo de copia que el precedente rechaza. Yo tendría que preguntar cuál construir antes de escribir el primer archivo | El documento adopta el modelo del precedente como forma principal (R-N3-09 opción A) y, si admite la opción B (proyecto hermano), la describe como **insumo de construcción sin copia**: el target del consumidor genera el artefacto directamente en su `wwwroot/`. El árbol de `INPUT` §8.4 no viaja al documento |
| H-N3-03 | `INPUT` §2.3 l.48-51, §4 obs. 2 l.143-145, §8.4 l.288-289, cabecera l.7; `Intake-Rules.md` §4 l.123-141; `Coherencia-Proyecto-De-Otro-Ecosistema.md` l.33-39 | **El documento de entrada está escrito contra 13.15 y contradice lo que 13.16 fijó el mismo día.** `INPUT` afirma «dos soluciones de código (el `.sln` y el workspace npm)» y que el uso del `dist/` «se declara como consumo de artefacto publicado, no como arista de compilación». `Intake-Rules.md` §4 (4.3, 13.16): el proyecto de otro ecosistema **pertenece a la solución de código del proyecto que toma su artefacto como insumo de construcción**, la arista es de clase «insumo de construcción» con **un único generador**, y la nota de coherencia lo dice de frente: «la respuesta negativa obligaba a declarar como consumo de artefacto publicado un artefacto que no se publica». PA-01 (`INPUT` l.185, «¿dónde fija el framework la estructura interna de un proyecto de código no .NET?») queda **parcialmente respondida** por el perfil por ecosistema del manifiesto §1.2 y por `Rules-Devops.md` §4.9 punto 4, que `INPUT` no cita | E2 `INPUT` l.7, l.48-51, l.143-145, l.288-289; E2 `Intake-Rules.md` l.123-131, l.135-141; E2 `Coherencia-Proyecto-De-Otro-Ecosistema.md` l.5-6, l.33-39; E2 `PRODUCT-MANIFEST-template.md` l.88-101, l.144-149; E1 `grep -c -i "insumo de construcci" INPUT` → `0`; `grep -n "^## \[13.1[567]\]" CHANGELOG.md` → 13.17 y 13.16 el 2026-09-13, 13.15 el 2026-09-12 | S2 | 0,95 | Como lector externo recibiría «dos soluciones» y «consumo publicado» de un artefacto que nadie publica, y me preguntaría cómo se construye el conjunto con un comando. Si el documento copia `INPUT`, enseña un modelo que el propio framework acaba de descartar | El documento describe la relación bundle → biblioteca con la figura vigente: misma solución, arista de construcción con un generador, y un modo nombrado donde falta la cadena. `INPUT` §2.3, §4 obs. 2 y §8.4 se reescriben contra 13.17 antes de que la mesa se apoye en ellos. Solicitud SC-N3-01 |
| H-N3-04 | `ADR-43` §2 l.35-38 y §7 l.69; `package.json` l.8; `MaqREADME` l.163; `REx` §3.6 l.218-219 | **La PoC HTML «sin .NET» necesita una decisión de formato que ningún insumo cierra, y el precedente declara una cosa y construye otra.** `ADR-43` decide dos superficies «generadas del mismo origen y en la misma compilación»: módulo ES para Blazor y global clásico para la página estática. El build real emite **sólo** `--format=esm`; no hay ningún `*.global.js` en el repositorio; y la copia que usa la maqueta es un **prototipo escrito a mano** («forma equivalente a la salida `<componente>.global.js`»). Un módulo ES no carga desde archivo local: el propio `ADR-43` §1 lo da como motivo de la decisión. `REx` §3.6 exige declarar «sin servidor» o «servido». Los insumos no dicen cuál de las dos es la PoC HTML del PO | E1 `grep -o -- "--format=[a-z]*" <Componente>/package.json` → `--format=esm`; `grep -c -i "iife\|global-name\|global" package.json csproj` → `0` y `0`; `find . -name "*.global.js" -not -path "*/node_modules/*" \| wc -l` → `0`; `sed -n 4,5p SDD/Maquetas/<F1>-Web/assets/vendor/<componente>.js` → «Prototipo del componente. Es la forma que tendría la salida transpilada»; E2 `ADR-43` l.29-31, l.35-38, l.69; E2 `MaqREADME` l.36, l.163; E2 `REx` l.218-219; hecho de stack por nombre: MDN Web Docs, «JavaScript modules», carga local | S2 | 0,9 | Si el documento copia `ADR-43` como forma, enseña una segunda superficie que el precedente no emite y que yo no sabría configurar; si no la menciona, abro la PoC con doble clic y falla. Cualquiera de las dos me obliga a preguntar | El documento elige y declara: **(a)** PoC HTML servida por un servidor estático, un solo artefacto módulo ES; o **(b)** segunda salida global emitida por el mismo build, con su esqueleto real. Lo que el precedente hace (una superficie) y lo que decidió (dos) se distinguen en §4 y §7. Pregunta P-N3-03; solicitud SC-N3-02 |
| H-N3-05 | `ev-05` l.39; `INPUT` §8.3 l.247; `REx` §4.6 l.335-342 | **La PoC Blazor no tiene, en ningún insumo, una aserción evaluable por máquina ni un comando.** `ev-05` pide que sirva «a la diagnosticación»; `INPUT` §8.3 le asigna como arista B «que la RCL carga los dos JS y el mapa refleja la ocupación», que es prosa. La regla que la mesa propone como encaje exige `comando`, `criterio_aceptacion` como código de salida, respuesta o instantánea, y `evidencia` con fecha. Para la PoC HTML el precedente ofrece un camino (batería en Node); para una app Blazor interactiva, ninguno de los insumos dice si se afirma sobre una respuesta HTTP, sobre el estático servido bajo `_content/`, sobre una instantánea del DOM o sobre una prueba de navegador | E2 `ev-05` l.39; E2 `INPUT` l.247; E2 `REx` l.335-342, l.369, l.321 (anti-patrón «criterio redactado como prosa») | S2 | 0,85 | Construyo la PoC Blazor como demo sin aserción, o pregunto qué correr. La finalidad de diagnóstico que el PO le pide (`ev-05` l.39) queda sin instrumento | El documento fija el nivel mínimo de aserción de la PoC Blazor, sin herramienta obligatoria: como piso, una petición a la app que devuelve 200 y **el estático bajo `_content/<ensamblado>/` que devuelve 200 con el bundle construido**; como nivel superior y opcional, una prueba de navegador que verifica que el acuse llega. Se declara qué cubre cada nivel |
| H-N3-06 | `INPUT` §4 l.97-138, §5 l.157-167, §8.3 l.239-247, §8.4 l.277-279; `ev-05` l.35, l.41; `RBC` §4.1 l.226-227 | **El documento de entrada impone la estructura del SDD en cada sección, y los términos con que lo hace no significan nada afuera.** El árbol de §4 trae `samples/`, `tests/`, `SDD/Intake/`, `SDD/Maquetas/`, `SDD/Docs/00-Contexto/…11-Documentacion/`, `Producto/Vista-Producto.md`, `AGENTS.md`; §8.3 hace depender **la existencia de las dos PoC** de declarar una «unidad de entrega `library` con `redistribuible: false`» (PA-04, «decisión del PO»); §8.4 nombra `samples/01-basico-html-sin-blazor/`. Nada de eso es del artefacto: es de dónde el SDD lo documenta. El PO lo prohíbe expresamente («no debe dictar cómo se estructura la solución .NET», `/demos` contra `/samples`). Además, la cabecera obligatoria del documento lleva `Consumidor: 03, 05` y `Condicion-de-carga` contra el tipo D8, que son campos del índice SDD y no del lector | E1 recuento de términos SDD en `INPUT` (ver R-N3-02): «samples» 14, «unidad de entrega» 11, «proyecto de código» 10, «redistribuible» 7, «arista» 7, «Unidades-Entrega» 4; E2 `INPUT` l.97-138, l.239-247, l.277-279; E2 `ev-05` l.35, l.41; E2 actuación 002 §2 R2; E2 `RBC` l.226-227 y l.256 | S1 | 0,95 | Si el documento hereda la forma de `INPUT`, un lector de otro framework o pregunta qué es cada término o adopta `/samples` y `Unidades-Entrega/` sin querer. En los dos casos el documento falla en lo único que el PO puso como restricción de fondo | Dos capas explícitas en el documento: **lo que el artefacto exige** (layouts internos, contrato, ciclo de vida, aserciones) en §2 a §7, escrito en vocabulario neutro con la tabla de equivalencias de R-N3-02; y **cómo lo cataloga el SDD** (unidad de entrega, D8, `redistribuible`, categoría 10, `/samples`, insumo de construcción) confinado a §8 y a la cabecera. PA-04 se resuelve en §8 y no condiciona §2 a §7: las dos PoC existen porque prueban el contrato desde dos lados, no porque una clasificación las obligue |
| H-N3-07 | `ev-05` l.36; `Contrato.cs` l.3-10, l.49; `contrato.ts` l.6-8, l.25 | **El lenguaje del bundle está abierto («typescript, javascript, etc») y la regla del contrato doble sólo funciona tal como el precedente la escribe si hay tipos.** El precedente vende «el tipado cruza la costura: si el envoltorio Blazor y el TypeScript dejan de coincidir, no compila» (`ADR-42` §5.2), y el propio `Contrato.cs` l.5-6 admite el límite: «si dejan de coincidir, hay que cambiar los dos archivos y **el que se olvide se nota al serializar**», es decir, en ejecución. En JavaScript plano no hay ni siquiera el primer lado. A eso se suma la regla de nombres que el precedente aplica sin enunciar: `Posicion` en C# es `posicion` en TS porque la serialización de la interop convierte a camelCase, y ningún archivo del precedente lo dice | E2 `ev-05` l.36 («en typescript, javascript, etc»); E2 `ADR-42` l.54 (JavaScript a mano como alternativa legítima) y l.61; E2 `Contrato.cs` l.3-10, l.49; E2 `contrato.ts` l.6-8, l.25; E1 `grep -c -i "camel" <Componente>/Contrato.cs <Componente>/ts/contrato.ts <Componente>/DiagramaDeCabecera.razor` → `0` en los tres | S2 | 0,8 | Sin decidir el lenguaje, no sé si escribo `contrato.ts` o un `.d.ts` junto a JavaScript, o nada; sin la regla de nombres enunciada, el primer DTO cruza con campos vacíos y sin error | El documento decide TypeScript como forma principal y declara JavaScript plano como variante con lo que pierde (R-N3-24), o admite las dos con la regla del contrato reescrita para cada una (con TS: dos archivos hermanos; con JS: un `.d.ts` de referencia y la verificación en la batería). Y enuncia la regla de nombres de R-N3-14 como convención con su motivo. Pregunta P-N3-02 |

Tope de siete cumplido. Quedaron fuera por cupo, y los anoto para el presidente sin desarrollarlos: la ubicación de las pruebas del bundle (`INPUT` §8.4 l.281-282 las pone en `tests/`, el precedente junto a la fuente por excepción declarada, R-N3-30); y que `INPUT` §8.3 l.246 describe la arista B de la PoC HTML con palabras que también describen la batería unitaria sin DOM (R-N3-07).

---

## 4. Lo que revisé y está bien

| # | Qué | Por qué está bien | Ancla |
|---|---|---|---|
| B-N3-01 | **La forma de la PoC HTML ya está escrita en el framework, en palabras que un forastero entiende.** `REx` §3.6 «El sample de un artefacto que otro proyecto carga» dice: anfitrión mínimo que carga el artefacto construido y nunca una copia, invoca la superficie pública y nada más, lleva datos por escenario y salida esperada, declara si se abre sin servidor o servido, y si el artefacto invoca de vuelta al que lo carga, lleva un doble del consumidor con la lista de invocaciones derivada de una sola fuente | Es exactamente la PoC HTML que `ev-05` l.37 pide, y el documento puede transcribirla sin inventar y sin nombrar una herramienta. Lo único SDD de esa sección es la palabra «sample» y la remisión a `Intake-Rules.md` §4 | E2 `REx` l.210-223; E2 `ev-05` l.37 |
| B-N3-02 | **El precedente hace construible el doble del lado .NET sin .NET.** `montarConInterop` tipa la referencia .NET de forma estructural —`{ invokeMethodAsync(metodo: string, ...args: unknown[]): Promise<void> }`— y el nombre del método de vuelta es un parámetro con valor por omisión. Con eso, la PoC HTML pasa un objeto de tres líneas y prueba el mismo camino que Blazor | Es lo que vuelve verdadera la frase «como lo haría Blazor» del PO. No hay que diseñar nada nuevo para la PoC HTML: hay que copiar la firma | E2 `<componente>.ts` l.296-306; E2 `DiagramaDeCabecera.razor` l.58-59 |
| B-N3-03 | **El framework ya previó el caso exacto, en el único lugar donde admite nombrar un ecosistema.** El ejemplo del perfil por ecosistema del manifiesto es «un producto con proyectos .NET y un paquete npm cuyo artefacto un proyecto .NET toma como insumo de construcción», con la identidad del paquete en minúscula y guion y «ninguna» extensión de agrupador porque pertenece a la solución de su consumidor | Da al documento la respuesta a «cómo se llama y a qué solución pertenece el bundle» sin que el documento tenga que legislar, y confirma que el conocimiento `propio` puede nombrar el stack porque el perfil ya lo hace | E2 `PRODUCT-MANIFEST-template.md` l.88-104; E2 `Coherencia-Proyecto-De-Otro-Ecosistema.md` l.21-22 |

---

## 5. Solicitudes de convocatoria

| Id | Rol faltante | Señal, con ubicación | Qué no puedo afirmar sin él |
|---|---|---|---|
| SC-N3-01 | V1 Arquitectura (o N1 Requisitos) | `INPUT` §2.3, §4 obs. 2 y §8.4 contra `Intake-Rules.md` §4 (13.16): dos soluciones y consumo publicado, contra una solución e insumo de construcción con único generador (H-N3-03) | Cuál de los dos modelos enseña el documento cuando el bundle es proyecto hermano, y si el documento de entrada se reescribe antes de la consolidación. Yo sólo puedo afirmar que me obligaría a preguntar |
| SC-N3-02 | AH-1 Ingeniería del bundle, con V3 Operación y entrega | `ADR-43` decide dos superficies emitidas en la misma compilación; el build emite una sola (`--format=esm`), no existe `*.global.js` y la copia de la maqueta es prototipo a mano (H-N3-04, E1) | Si la segunda superficie es deuda del precedente, decisión no ejecutada, o forma que el documento no debe enseñar; y cómo se emitiría (parámetros del empaquetador) si el documento la adopta. No me corresponde juzgar el precedente |
| SC-N3-03 | AH-2 Interoperabilidad Blazor ↔ JavaScript | La regla de nombres camelCase de la serialización de interop y el límite «se nota al serializar» (H-N3-07, R-N3-14); el nivel mínimo de aserción de la PoC Blazor (H-N3-05) | Que la convención de nombres es la de la plataforma y no del precedente, y qué aserción sobre una app Blazor interactiva es evaluable por máquina sin prueba de navegador. Puedo decir que hace falta; no puedo fijar el hecho de stack con `E2` del precedente porque el precedente no lo enuncia |

---

## 6. Preguntas al Product Owner

Cada una nace de una conjetura `C` mía sobre lo que el PO quiso decir; la conjetura no entra al documento.

| Id | Conjetura (C) | Pregunta cerrada | Opciones |
|---|---|---|---|
| P-N3-01 | C: el PO usa `main.js` para dos cosas porque piensa en el par «adaptador del anfitrión / bundle» de la maqueta, y lo que quiere es que la capa de interop tenga nombre propio (H-N3-01) | ¿Qué es `main.js`? | (a) Un **segundo artefacto emitido por el proyecto del bundle**: la capa de interop (montaje con referencia .NET, nombre del callback), separada del bundle que dibuja. (b) El **script anfitrión de cada PoC**, que hace el papel del envoltorio Razor y no es salida del bundle. (c) Las dos cosas, con nombres distintos que el documento fija |
| P-N3-02 | C: «typescript, javascript, etc» es una enumeración abierta por costumbre y el PO acepta que el documento fije TypeScript como en el precedente (H-N3-07) | ¿El documento fija el lenguaje del bundle? | (a) **TypeScript** como forma única, con el contrato en dos archivos hermanos. (b) TypeScript como principal y **JavaScript plano como variante declarada**, con un `.d.ts` de referencia y lo que se pierde. (c) Los dos al mismo nivel, con las reglas de la costura escritas para cada uno |
| P-N3-03 | C: «sin .NET» significa para el PO «sin servidor de aplicación», no «sin ningún servidor» (H-N3-04) | ¿Cómo se abre la PoC HTML? | (a) **Servida** por un servidor estático cualquiera; un solo artefacto módulo ES. (b) **Abierta como archivo local**, lo que obliga a una segunda salida global emitida por el mismo build. (c) Las dos, y el documento describe la segunda salida como opcional |

---

Sigue: consolidación · presidente de mesa
