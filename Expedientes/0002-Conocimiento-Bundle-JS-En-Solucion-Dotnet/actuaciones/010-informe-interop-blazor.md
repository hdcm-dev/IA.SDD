# Actuación 010 — Informe Agente ad hoc AH-2

| Campo | Valor |
|---|---|
| Tipo | `informe` |
| Fecha | 2026-09-13 |
| Autor | Comisión AH-2, Interoperabilidad Blazor ↔ JavaScript y biblioteca de clases Razor |
| Corrige | — |

## 1. Mandato y método

**Mandato** (carta `evidencia/ev-07-cartas-despachadas/08-AH2-interop-blazor.md`): cómo integra una biblioteca de clases Razor un módulo JavaScript —carga del módulo, referencia al elemento, referencia .NET para callbacks, serialización de DTO, liberación en los dos lados, circuito caído, render mode— y qué de eso tiene que quedar declarado para que la PoC Blazor sirva como diagnóstico. Competencia: `Microsoft.JSInterop`, estáticos de RCL bajo `_content/`, `OnAfterRenderAsync`, `IAsyncDisposable`, render modes. Fuera: código del bundle (AH-1), contrato de datos (V2), MSBuild (V3). Tope: 5 hallazgos.

**Qué leí, en orden:**

| # | Ruta | Alcance de lectura |
|---|---|---|
| 1 | `evidencia/ev-07-cartas-despachadas/00-encargo-comun.md` | Completo |
| 2 | `evidencia/ev-07-cartas-despachadas/08-AH2-interop-blazor.md` | Completo |
| 3 | `evidencia/ev-05-presentacion-original.md` | Completo |
| 4 | `IA.SDD.Documentacion/PROMPTs/SDD/Catalogado/02-Extraccion-Concepto-Espcificacion-Integracion-Proyecto-Bundle-JS/INPUTs/Bundle-JS-En-Solucion-Blazor.md` | Completo |
| 5 | `actuaciones/001-presentacion-del-product-owner.md`, `actuaciones/002-providencia-convocatoria-de-mesa.md` | Completos |
| 6 | `evidencia/ev-02-citas.out`, `evidencia/ev-03-precedente.out`, `evidencia/ev-03-precedente.sh` | Completos |
| 7 | Precedente `<F1>/src/<F1>.<Componente>/`: `DiagramaDeCabecera.razor`, `Contrato.cs`, `ts/<componente>.ts`, `ts/contrato.ts`, `<F1>.<Componente>.csproj`, `package.json` (vía ev-03) | Completos |
| 8 | Precedente, decisiones: `SDD/Docs/05-Arquitectura-Tecnica/Adrs/ADR-00042-Cadena-De-Construccion-De-Javascript.md`, `ADR-00043-Dos-Superficies-De-Consumo.md`, `Contratos-Abstractions-<F1>-<Componente>.md`; `SDD/Maquetas/<F1>-Web/README.md` §9 | Completos los tres primeros; del README, §9 y líneas 150-175 |
| 9 | Precedente, lado anfitrión (no listado en el encargo, necesario para el mandato): `src/<F1>.Web/Components/App.razor` l.10-32, `Components/Inventario/DiagramaDelEquipo.razor` l.1-80, `Components/Pages/MapaDelEquipo.razor` l.205-225; `tests/<F1>.Web.Pruebas/ElRegresoDesdeLaFichaPruebas.cs` l.26-56, `PuntosAccionablesPruebas.cs` l.470-486; `ts/frontera.test.ts` l.1-60, `ts/retencion.test.ts` l.1-50 | Parciales, las líneas indicadas |
| 10 | `IA.SDD/SDD/Devs/Rules/Rules-Base-Conocimiento.md` | Completo |
| 11 | `IA.SDD/Conocimiento/README.md`, `Index-Knowledge.md` | Completos |
| 12 | `IA.SDD/Conocimiento/Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md` | §0-§4 completos (l.1-270), §5.6 (l.608-667), §6-§10 (l.817-937) |
| 13 | `IA.SDD/SDD/Devs/References/Design/Design-Rules-Blazor-Mudblazor.md` §2 (l.78-92); `SDD/Devs/Intake/PRODUCT-INTAKE-template.md` l.401-420 (clases de contrato) | Parciales |
| 14 | Conjunto normativo `SDD/Devs/` y `README.md`: barrido por `grep` de términos de interoperabilidad (comando C1 abajo) | Sólo barrido; no leí los archivos enteros |

**Qué no leí y por qué:** ningún informe de otra comisión (no existen en `actuaciones/` al momento del despacho; trabajo a ciegas). `Knowledge-Conformacion-Pull-Request-Manual.md`: referencia de forma que el documento Blazor ya cubre para mi tema. `Master-Prompt.md`, `Rules-Examples.md`, `Maqueta-Rules.md`, `Rules-Devops.md` enteros: son de V1, V3 y N1; sólo tomo de ellos lo que `ev-02` fija. `ev-01`, `ev-04`, `ev-06`: los resume la actuación 002 y no aportan a la costura .NET ↔ JS.

**Comandos ejecutados (E1).** Todos sobre el árbol de trabajo; C2 confirma que los archivos citados del precedente son idénticos al commit `9aabe5c` que `ev-01` fija como base.

| Id | Comando (cwd) | Salida |
|---|---|---|
| C1 | `grep -rniE 'JSInterop\|IJSRuntime\|IJSObjectReference\|DotNetObjectReference\|JSInvokable\|JSDisconnected\|_content/\|Razor Class Library\|RCL\b\|interoperabilidad\|OnAfterRender\|IAsyncDisposable\|render ?mode\|rendermode\|prerrender\|WebAssembly' --include=*.md SDD/ README.md Conocimiento/ \| grep -v _legacy` (`IA.SDD`) | En `SDD/Devs/` sólo: `Guides/Marco-Teorico-SDD.md:1816` (glosario «RCL (Razor Class Library)»), `Bootstrap/*` (auditoría de esa entrada), `References/Design/Design-Rules-Blazor-Mudblazor.md:80-82` (render mode por defecto), `Guides/Coherencia-Templates-De-Maqueta.md:45`. **Cero** ocurrencias de `JSInterop`, `IJSRuntime`, `IJSObjectReference`, `DotNetObjectReference`, `JSInvokable`, `JSDisconnected`, `_content/`. El resto de las coincidencias son de `Conocimiento/Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md` |
| C2 | `git diff --stat 9aabe5c -- src/<F1>.<Componente>/{Contrato.cs,DiagramaDeCabecera.razor,ts/<componente>.ts,ts/contrato.ts,<F1>.<Componente>.csproj} src/<F1>.Web/Components/{App.razor,Inventario/DiagramaDelEquipo.razor,Pages/MapaDelEquipo.razor}` (`<F1>`) | Sin salida, exit 0: idénticos al commit base |
| C3 | `grep -rn 'montarConInterop' src/<F1>.<Componente>/ts/*.test.ts tests/ \| grep -v /bin/ \| wc -l` (`<F1>`) | `0` |
| C4 | `grep -c 'export' src/<F1>.<Componente>/wwwroot/<componente>.js` y `head -c 160` del mismo | `1`; el archivo empieza con `var h={SinPuntos:"E-PIN-00041",...` (un solo módulo ES minificado; no hay superficie global emitida por el build) |
| C5 | `grep -rn 'JsonSerializer\|JsonPropertyName\|PropertyNamingPolicy' src --include=*.cs --include=*.razor \| grep -v /obj/` (`<F1>`) | Sólo en `src/<F1>.Infrastructure/Mapa/*` (lector del mapa). **Ninguna** en `<F1>.<Componente>/`: el contrato de interoperabilidad no declara política de nombres |

**Convención de anclas.** `E2 doc:` cita por nombre un tema de la documentación oficial de ASP.NET Core Blazor, sin enlace, como manda el encargo (regla 3). `E2 prec:` cita el precedente por ruta relativa a `<F1>/`. Los nombres de dominio del precedente (`<Componente>`, `Cabecera`, `Acuse`, `<prefijo-css>`) aparecen sólo como ancla; en el documento final van ofuscados (R4 de la actuación 002).

---

## 2. Relevamiento para el documento `Bundle-JS`

| Id | Sección destino | Qué tiene que decir | Ancla (nivel + cita) | Riesgo si falta |
|---|---|---|---|---|
| RAH2-01 | §0 | La costura .NET ↔ JS que el documento caracteriza es una y tiene tres asas del lado .NET: la referencia al módulo (`IJSObjectReference`), la referencia al elemento contenedor (`ElementReference`) y la referencia al objeto .NET que recibe los callbacks (`DotNetObjectReference<T>`). Lo que queda afuera: el interior del bundle, el diseño de los DTO y la cadena de construcción; se dice dónde viven (05 del producto y las secciones del documento que les correspondan) | E2 prec: `src/<F1>.<Componente>/DiagramaDeCabecera.razor` l.24-26; E4: carta AH-2, campo `competencia` | Sin el «qué queda afuera», §0 no cumple `Rules-Base-Conocimiento.md` §4.2 y el agente completa el hueco solo |
| RAH2-02 | §1 | La biblioteca de clases Razor es un proyecto `Microsoft.NET.Sdk.Razor`; sus estáticos de `wwwroot/` se sirven en el anfitrión bajo `_content/<PackageId>/` sin configuración adicional, y ese prefijo lo decide el identificador del paquete/ensamblado, no el nombre de carpeta | E2 prec: `<F1>.<Componente>.csproj` l.1, l.14-15, l.17-21; `DiagramaDeCabecera.razor` l.17-18; E2 doc: «Consume ASP.NET Core Razor components from a Razor class library (RCL)», sección sobre recursos estáticos y la ruta `_content/{PACKAGE ID}` | El agente inventa rutas o copia el JS al `wwwroot` del anfitrión, y el import falla con 404 |
| RAH2-03 | §1 | Supuesto de render mode: el precedente y `Template-Blazor-Interactive-Server-SDD-Default` cubren **Interactive Server** (interoperabilidad asincrónica sobre circuito). El documento declara que sólo ese modo está verificado por precedente y enumera qué cambia en WebAssembly (interoperabilidad en proceso, sin `JSDisconnectedException`, `IJSInProcessRuntime` disponible) y en Auto (las dos cosas, según la sesión) como **supuesto no verificado** | E2 prec: `src/<F1>.Web/Components/Pages/MapaDelEquipo.razor` l.217 (`@rendermode="RenderMode.InteractiveServer"`); E2 doc: «ASP.NET Core Blazor render modes» y «ASP.NET Core Blazor JavaScript interoperability (JS interop)», apartados sobre interoperabilidad sincrónica en WebAssembly | Un framework destino con WebAssembly aplica reglas de circuito que no existen ahí, o al revés, omite el manejo de circuito caído en Server |
| RAH2-04 | §2 | Piezas mínimas de la integración y rol de cada una: (a) el archivo de contrato del lado .NET, (b) el componente envoltorio `.razor` que sostiene el ciclo de vida, (c) el módulo JS de entrada con la función de montaje que recibe la referencia .NET, (d) el estático emitido a `wwwroot/`, (e) la página anfitriona que declara el render mode. El envoltorio **no dibuja nada**: delega | E2 prec: `DiagramaDeCabecera.razor` l.12-15; `Contrato.cs` l.3-6; `ts/<componente>.ts` l.291-306; `MapaDelEquipo.razor` l.217 | Sin el reparto de roles, el agente pone lógica de dibujo en el `.razor` o lógica de circuito en el JS |
| RAH2-05 | §3 | El módulo se carga con `import` por `IJSRuntime.InvokeAsync<IJSObjectReference>("import", "./_content/<PackageId>/<archivo>.js")` en `OnAfterRenderAsync(firstRender)`, **nunca** en `OnInitialized{Async}` ni `OnParametersSet{Async}`: durante el prerenderizado no hay navegador y la llamada lanza `InvalidOperationException` («JavaScript interop calls cannot be issued at this time»); `OnAfterRender` no corre durante el prerenderizado | E2 prec: `DiagramaDeCabecera.razor` l.52-59; E2 doc: «Call JavaScript functions from .NET methods in ASP.NET Core Blazor», apartados «JavaScript isolation in JavaScript modules» y «Prerendering»; «ASP.NET Core Razor component lifecycle», apartado sobre `OnAfterRender` y prerenderizado | Es el fallo más frecuente y su mensaje es oscuro; sin esta fila la PoC Blazor no lo reproduce ni lo diagnostica |
| RAH2-06 | §3 | La ruta del `import` es una **cadena literal** que sólo se resuelve en ejecución: forma parte del contrato de integración y se declara en un solo lugar del envoltorio. Cambiar `PackageId`/`AssemblyName` o el nombre del archivo emitido la rompe sin error de compilación | E2 prec: `DiagramaDeCabecera.razor` l.56-57; `<F1>.<Componente>.csproj` l.14-15, l.64 (`Outputs="wwwroot/<componente>.js"`) | Un renombre del proyecto deja el import apuntando a un 404 que sólo se ve en el navegador |
| RAH2-07 | §3 | El JS indexa las instancias **por contenedor** (`WeakMap<HTMLElement, Instancia>`) y todas las operaciones exportadas reciben el `ElementReference`; el lado .NET no guarda una referencia a la instancia JS. Es una decisión, no una limitación de la plataforma (ver HAH2-05): la alternativa —devolver la instancia y sostenerla como `IJSObjectReference`— existe y se nombra en §4 con su costo (una superficie más que difiere entre PoC HTML y Blazor) | E2 prec: `ts/<componente>.ts` l.39-44, l.308-318; E2 doc: «Call JavaScript functions from .NET methods», apartado sobre `IJSObjectReference` devuelta por una función JS | El agente copia la justificación errada del precedente o, al revés, sostiene instancias sin liberarlas |
| RAH2-08 | §3 | Callback JS → .NET: el envoltorio crea `DotNetObjectReference.Create(this)` **después** de importar el módulo y lo pasa en la llamada de montaje; el JS lo guarda y llama `invokeMethodAsync("<Nombre>", dto)`. El método receptor lleva `[JSInvokable("<Nombre>")]` **con el nombre explícito**, y el mismo literal es el valor por omisión del lado JS. El documento tiene que decir que **ese nombre no se verifica en ninguna compilación**: si difiere, el callback deja de llegar en silencio | E2 prec: `DiagramaDeCabecera.razor` l.58-59, l.73-77; `ts/<componente>.ts` l.296-306; E2 doc: «Call .NET methods from JavaScript functions in ASP.NET Core Blazor», apartados sobre métodos de instancia y `DotNetObjectReference` | Sin la advertencia, un renombre del método se diagnostica como «el bundle no emite eventos» |
| RAH2-09 | §3 | El JS que conoce la referencia .NET **no importa nada de Blazor**: la tipa estructuralmente como `{ invokeMethodAsync(metodo: string, ...args: unknown[]): Promise<void> }`. Es lo que permite que la PoC HTML pase un objeto falso con la misma forma y ejercite la misma función de montaje que usa Blazor | E2 prec: `ts/<componente>.ts` l.296-299; E3: un objeto `{ invokeMethodAsync: async (m, dto) => registrar(m, dto) }` satisface la firma y hace que `montarConInterop` corra sin .NET (contraejemplo construido sobre la firma citada; el precedente no lo tiene, C3) | Si el JS importa tipos de Blazor, la PoC HTML no puede imitar la invocación y deja de probar lo que usa Blazor |
| RAH2-10 | §3 | Serialización de DTO: la interoperabilidad usa `System.Text.Json` con política **camelCase** al serializar y lectura **insensible a mayúsculas** al deserializar, por omisión. Por eso los `record` en PascalCase del lado .NET se corresponden con interfaces en camelCase del lado TS **sin atributos**. El documento tiene que declarar esa política como parte del contrato y advertir que cualquier `JsonSerializerOptions` propio del anfitrión no la altera (la interoperabilidad tiene las suyas) y que cambiarla rompe los dos lados | E2 prec: `Contrato.cs` l.48-54 (`Posicion`, `Columna`), `ts/contrato.ts` l.23-33 (`posicion`, `columna`); C5 (ninguna política declarada en el proyecto); E2 doc: «Call .NET methods from JavaScript functions», apartado «JSON serialization» y su equivalente en «Call JavaScript functions from .NET methods» | Un agente que agrega `JsonPropertyName` o cambia la política «para que coincida» rompe lo que ya coincidía, o un DTO llega con todos los campos nulos y nadie sabe por qué |
| RAH2-11 | §3 | Conjuntos cerrados como **cadenas** con constantes del lado .NET y uniones de literales del lado TS, no `enum`: cruzan a JSON sin conversión y sin depender de la numeración | E2 prec: `Contrato.cs` l.8-10; `ts/contrato.ts` l.7-8 | Un `enum` .NET serializa como entero y el JS recibe números donde espera nombres |
| RAH2-12 | §3 | Campos opcionales del TS (`?`) se declaran del lado .NET como anulables con valor por omisión `null`; un `null` explícito en el acuse JS («presente exactamente cuando…») se corresponde con `string?` del lado .NET. El documento fija una sola regla de ausencia por costura | E2 prec: `ts/contrato.ts` l.31-32, l.147-148; `Contrato.cs` l.47, l.140 | Ausencia representada de dos formas (campo omitido vs `null`) produce DTO inválidos de un lado |
| RAH2-13 | §3 | Liberación en `DisposeAsync`, en este orden: (1) pedirle al JS que destruya la instancia (desregistra escuchas y vacía el índice), (2) `IJSObjectReference.DisposeAsync()`, (3) `DotNetObjectReference.Dispose()`. Todo lo que habla con el navegador va dentro de `try { } catch (JSDisconnectedException) { }`; la referencia .NET se libera **fuera** del `try`, siempre | E2 prec: `DiagramaDeCabecera.razor` l.79-98; `ts/<componente>.ts` l.128-141; E2 doc: «Call JavaScript functions from .NET methods», apartado sobre `JSDisconnectedException` y liberación de referencias; «Call .NET methods from JavaScript functions», apartado sobre liberar `DotNetObjectReference` | Saltear un paso deja una referencia viva; no atrapar la excepción hace que el descarte del componente falle cuando el circuito ya se cayó |
| RAH2-14 | §3 | Del lado JS, `invokeMethodAsync` devuelve una promesa que **se rechaza** cuando el circuito no está; el módulo tiene que capturar ese rechazo (o declarar que lo deja escapar a la consola) en vez de un `void` sin manejo | E2 prec: `ts/<componente>.ts` l.302-304 (`void refDotNet.invokeMethodAsync(...)`, sin `catch`); E2 doc: «Call .NET methods from JavaScript functions», sobre `invokeMethodAsync` que devuelve una promesa | Rechazos no manejados en la consola durante la reconexión, indistinguibles de un fallo del bundle |
| RAH2-15 | §3 | Obligaciones del **anfitrión** que la RCL no puede asumir por él: (a) declarar el render mode interactivo sobre el componente o la página; (b) enlazar la hoja de estilos del componente en el `<head>` del documento raíz (una hoja inyectada por el componente llega después del primer dibujo); (c) referenciar la RCL para que sus estáticos entren al manifiesto; (d) servir estáticos | E2 prec: `MapaDelEquipo.razor` l.217; `App.razor` l.19-25; E2 doc: «Consume ASP.NET Core Razor components from a Razor class library (RCL)»; «ASP.NET Core Blazor static files» | La PoC Blazor «anda» en la app del PoC pero no en la app real porque la real no cumple (a) o (b) |
| RAH2-16 | §3 | Frontera estático → interactivo: si la página anfitriona se sirve estática, el componente interactivo recibe **sólo parámetros serializables**; un `EventCallback` no cruza esa frontera, así que el acuse se recibe **adentro** de la isla interactiva. El envoltorio de la isla es del anfitrión, no de la RCL | E2 prec: `src/<F1>.Web/Components/Inventario/DiagramaDelEquipo.razor` l.10-16; E2 doc: «ASP.NET Core Blazor render modes», apartado sobre pasar parámetros a componentes interactivos desde componentes estáticos (deben ser serializables en JSON) | Error en ejecución al pasar un delegado desde una página estática, con diagnóstico oscuro |
| RAH2-17 | §3 | Prerenderizado: el `<div>` contenedor se emite dos veces (prerender e interactivo) y `OnAfterRenderAsync(firstRender)` corre una sola vez, con el circuito; el envoltorio no debe suponer que el primer render y el primer `OnAfterRender` coinciden | E2 prec: `tests/<F1>.Web.Pruebas/PuntosAccionablesPruebas.cs` l.478-482 («prerenderizado por el circuito interactivo»; el HTML estático ya contiene el contenedor); `Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md` l.193-196; E2 doc: «ASP.NET Core Razor component lifecycle», prerenderizado | El agente apaga el prerenderizado «para que el JS funcione» y degrada la primera pintura |
| RAH2-18 | §3 | Re-render: cada `OnAfterRenderAsync` con `Vista` no nula vuelve a llamar `render`; el contrato JS declara «todo cambio es un render completo, sin mutadores parciales». Es la forma más simple y tiene un costo (un viaje por el circuito por cada re-render del anfitrión) que el documento nombra | E2 prec: `DiagramaDeCabecera.razor` l.67-70; `ts/contrato.ts` l.166-168 | El anfitrión que hace `StateHasChanged` seguido redibuja el bundle sin necesidad y lo atribuye al bundle |
| RAH2-19 | §4 | Bifurcaciones resueltas, con criterio: módulo ES importado por componente vs. script global (módulo: aislamiento y liberación; el global queda para la superficie sin Blazor); índice por contenedor vs. `IJSObjectReference` a la instancia (contenedor: la misma API para la PoC HTML y Blazor); nombre `JSInvokable` explícito vs. inferido (explícito, porque se duplica en el JS); cadenas vs. `enum`; hoja de estilos enlazada por el anfitrión vs. inyectada; render mode del anfitrión vs. fijado por la RCL; dos funciones de montaje en el mismo módulo (`montar` para HTML, `montarConInterop` para Blazor) vs. una sola | E2 prec: ADR-00043 §2 y §4; `ts/<componente>.ts` l.281-306; `App.razor` l.19-25; `MapaDelEquipo.razor` l.217; `DiagramaDeCabecera.razor` l.73-77 | Sin §4 el agente reabre las bifurcaciones y las resuelve distinto en cada producto |
| RAH2-20 | §5 | Esqueleto del envoltorio `.razor` mínimo: `@inject IJSRuntime`, `@implements IAsyncDisposable`, `<div @ref>`, tres campos privados, `OnAfterRenderAsync` con `firstRender`, un `[JSInvokable]` con nombre explícito, `DisposeAsync` con el orden y el `catch` de RAH2-13. Ofuscado: sin nombres del dominio del precedente | E2 prec: `DiagramaDeCabecera.razor` l.7-99 (forma completa a extraer) | Un esqueleto es lo que `Rules-Base-Conocimiento.md` §4.2 pide para naturaleza `propio`; sin él la prosa no transmite el orden |
| RAH2-21 | §5 | Esqueleto del lado JS de la costura: función de montaje que recibe `(contenedor, refDotNet, nombreDelMetodo = "<Nombre>")` y adapta el callback a `invokeMethodAsync` con manejo del rechazo; y esqueleto del **doble falso** para la PoC HTML: `{ invokeMethodAsync: async (metodo, dto) => { ... } }` que registra lo recibido y lo muestra en la página | E2 prec: `ts/<componente>.ts` l.296-306; E3: doble construido sobre la firma de l.298 (ver RAH2-09) | Sin el doble, la PoC HTML no invoca «como lo haría Blazor» (pedido literal de ev-05, Solicitudes, segundo ítem) |
| RAH2-22 | §5 | Esqueleto de la página anfitriona de la PoC Blazor: `@rendermode InteractiveServer` (o el que el destino use, declarado), `<link>` a `_content/<PackageId>/<hoja>.css` en el documento raíz, un parámetro serializable, y un manejador del `EventCallback` que muestra el último DTO recibido | E2 prec: `MapaDelEquipo.razor` l.217; `App.razor` l.25; `DiagramaDelEquipo.razor` l.56-58 | La PoC no reproduce las obligaciones del anfitrión de RAH2-15 |
| RAH2-23 | §6 | Criterios de aceptación de la costura, verificables: (1) el literal de `[JSInvokable]` es igual al valor por omisión del JS (prueba que los lee de los dos archivos, o que ejercita el camino completo); (2) un DTO de ida y vuelta conserva todos los campos (sin nulos inesperados); (3) `GET _content/<PackageId>/<archivo>.js` responde 200 en la PoC Blazor; (4) montar y liberar N veces no deja escuchas ni entradas en el índice (medido, no leído); (5) ningún `InvokeAsync` fuera de `OnAfterRender*`; (6) `DisposeAsync` no lanza con el circuito caído | E2 prec: `ts/retencion.test.ts` l.1-11 (criterio 4 existe como prueba sin anfitrión); C3 (criterio 1 no existe en el precedente: es el hueco que la PoC Blazor cierra); E2 doc: los temas de RAH2-05, RAH2-10, RAH2-13 | Sin criterios, el «está integrado» se declara por inspección, que es lo que `retencion.test.ts` l.4-6 excluye a propósito |
| RAH2-24 | §6 | Tabla de diagnóstico síntoma → causa → dónde mirar, para el agente que diagnostica: «JavaScript interop calls cannot be issued at this time» → interop antes de `OnAfterRender` / prerender; 404 en `_content/...` → estático no emitido antes del descubrimiento, RCL no referenciada o `PackageId` distinto; «el callback no llega» → nombre `JSInvokable` distinto o `DotNetObjectReference` liberada; DTO con campos nulos → política de nombres o campo con nombre distinto de un lado; `JSDisconnectedException` en el descarte → falta el `catch`; rechazos en consola al reconectar → RAH2-14; `Cannot pass EventCallback` desde página estática → RAH2-16 | E2 prec: `<F1>.<Componente>.csproj` l.54-59 (el 404 por orden de targets, documentado por el propio precedente); `DiagramaDeCabecera.razor` l.73-75, l.91-94; E2 doc: temas de RAH2-05, RAH2-08, RAH2-13, RAH2-16 | Es lo que ev-05 pide bajo «diagnóstico de fallas de contrato»; sin la tabla la PoC sólo muestra que anda, no por qué deja de andar |
| RAH2-25 | §7 | Anti-patrones con motivo: interop en `OnInitializedAsync`; `DotNetObjectReference` sin `Dispose`; `catch (Exception)` alrededor del descarte (esconde fallos reales; sólo `JSDisconnectedException`); alcanzar `document`/`window` globales desde el módulo en vez de `contenedor.ownerDocument` (rompe fuera de la ventana principal y las pruebas sin DOM); prueba bUnit que invoca el `EventCallback` directamente y se declara prueba de la costura (no ejercita `JSInvokable` ni la serialización); `JSRuntimeMode.Loose` como modo por omisión en las pruebas del envoltorio (silencia llamadas no configuradas); hoja de estilos inyectada por el componente | E2 prec: `ts/<componente>.ts` l.244-249; `tests/<F1>.Web.Pruebas/ElRegresoDesdeLaFichaPruebas.cs` l.32, l.50-53; `App.razor` l.20-24; E2 doc: temas de RAH2-05 y RAH2-13 | `Rules-Base-Conocimiento.md` §5.4 pregunta 14: lo que salió mal es lo más valioso de la captura |
| RAH2-26 | §8 | Frontera con las reglas: el conjunto normativo **no norma** la interoperabilidad JS, `_content/`, el ciclo de vida, el prerenderizado ni los render modes distintos de Interactive Server (C1; `Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md` §8.2 lo declara para su tema). Lo que sí está normado y el documento cita sin repetir: render mode por defecto, `EventCallback` hijo → padre, prohibición de almacenamiento de navegador (`Design-Rules-Blazor-Mudblazor.md` §2); liberación con `IAsyncDisposable` (`Template-Blazor-Interactive-Server-SDD-Default` §3.2); clase de contrato, integración vs. compilación (`PRODUCT-INTAKE-template.md` §14, l.403-408; V2 decide) | E1: C1; E2: `Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md` l.911-917; `Design-Rules-Blazor-Mudblazor.md` l.82-86; `PRODUCT-INTAKE-template.md` l.403-408 | Sin §8 el documento se lee como norma (`Rules-Base-Conocimiento.md` §4.2: «§0 y §8 no se negocian») |
| RAH2-27 | §9 | Hermano declarado: `Template-Blazor-Interactive-Server-SDD-Default` (§3.2 higiene del circuito y §5.6 interoperabilidad mínima son el caso de un archivo JS suelto en `wwwroot` del anfitrión; `Bundle-JS` es el caso del módulo en una RCL). Un hecho, un lugar: `Bundle-JS` no repite la higiene del circuito, la cita. Consumidor sugerido para la parte de la costura: 05 (diseño) y 08 (los criterios de RAH2-23 son pruebas). La fila de índice queda fuera de alcance (002 §2) | E2: `Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md` l.181-191, l.637-646; `Rules-Base-Conocimiento.md` §4.4 fila «Un hecho, un lugar», §3.3 | Duplicar la higiene del circuito garantiza deriva entre los dos documentos |

---

## 3. Hallazgos

| Id | Ubicación | Afirmación | Evidencia (nivel + ancla) | Severidad | Confianza | Impacto si no se corrige | Dirección propuesta |
|---|---|---|---|---|---|---|---|
| HAH2-01 | Documento de entrada §8.3 («El contrato JS del mapa es un contrato de compilación entre el bundle y la RCL»); precedente ADR-00042 §5 punto 2 («si el envoltorio Blazor y el TypeScript dejan de coincidir, no compila») y `Contratos-Abstractions-<F1>-<Componente>.md` §1 («Que compilen juntos es lo que impide que se desincronicen») | **La costura .NET ↔ JS no se verifica en ninguna compilación.** Los tipos se declaran dos veces (`Contrato.cs`, `ts/contrato.ts`) sin ninguna herramienta que los compare; el nombre del `JSInvokable` es un literal duplicado; la política de nombres es implícita. El propio precedente lo admite en dos lugares. Que el intake la clasifique como contrato «de compilación» (entre proyectos de código) no la vuelve verificada por el compilador: la clase de contrato es de V2, pero la afirmación «no compila» es falsa y es de mi competencia | E2 prec: `Contrato.cs` l.5-6 («el que se olvide se nota **al serializar**»); `DiagramaDeCabecera.razor` l.73-75 («si alguno cambia, el acuse deja de llegar y **solo se nota en ejecución**»); ADR-00042 l.62 y §5 punto 2; `Contratos-Abstractions-<F1>-<Componente>.md` §1 último párrafo; E3: renombrar el literal de `[JSInvokable("AlActivarPunto")]` a otro valor compila (es una cadena) y el acuse deja de llegar; E1: C5 (ninguna política de nombres declarada) | S2 | 0,95 | El documento hereda la creencia de que el build protege la costura y **no manda la prueba ni la PoC que la ejercite**; el agente que diagnostica busca el fallo en el bundle cuando está en un literal | El documento clasifica la costura como **verificada sólo en ejecución** y deriva de eso los criterios RAH2-23 (1) y (2) y la tabla RAH2-24. Si V2 la clasifica como contrato de compilación por el intake, el documento dice las dos cosas: clase «de compilación» por su lugar en el grafo, verificación en ejecución por su mecánica |
| HAH2-02 | Precedente, baterías: `src/<F1>.<Componente>/ts/*.test.ts` y `tests/<F1>.Web.Pruebas/` | **Ninguna prueba del precedente cruza la costura.** Las baterías TS ejercitan `montar` (sin referencia .NET); la prueba bUnit del anfitrión invoca `AlActivarPunto.InvokeAsync` directamente sobre la instancia, salteando `JSInvokable`, `DotNetObjectReference` y la serialización. El precedente da la **forma** del envoltorio pero **no da la forma de la PoC Blazor como diagnóstico**: eso hay que diseñarlo, no extraerlo | E1: C3 (`0` referencias a `montarConInterop` en pruebas); E2 prec: `tests/<F1>.Web.Pruebas/ElRegresoDesdeLaFichaPruebas.cs` l.50-53; `ts/retencion.test.ts` l.16 (`import { destruir, montar }`) | S2 | 0,9 | Si la mesa asume que el precedente ya resolvió la PoC Blazor, el documento saldrá sin el único artefacto que verifica la costura, que es lo que ev-05 pide bajo «diagnosticar y testear fallos» | El documento declara en §6 que el precedente no verificó la costura y fija los criterios (1) a (3) de RAH2-23 como **lo que la PoC Blazor existe para probar**; §5 aporta el doble de RAH2-21 para que la PoC HTML pruebe lo mismo desde el otro lado |
| HAH2-03 | Documento de entrada §8.1 (PoC Blazor: «un componente Blazor que consume los dos archivos JavaScript») y §8.4 (RCL con `MapaAsientos.razor`, `.razor.cs` y `wwwroot/js/` «recibe el dist/ del bundle»; PoC como «app mínima con el componente») | **El documento de entrada omite las obligaciones del anfitrión.** Una RCL no corre sola: alguien declara el render mode, enlaza la hoja de estilos en el `<head>`, referencia la RCL y sirve estáticos; y si la página es estática, sólo pasan parámetros serializables. Nada de eso está en §8.1 ni §8.4, y sin eso «app mínima» no dice qué tiene que tener la app | E2: documento de entrada l.198-204, l.266-283; E2 prec: `MapaDelEquipo.razor` l.217; `App.razor` l.19-25; `DiagramaDelEquipo.razor` l.10-16; E2 doc: «Consume ASP.NET Core Razor components from a Razor class library (RCL)»; «ASP.NET Core Blazor render modes» | S3 | 0,85 | La PoC Blazor «anda» y la app real no, por una obligación del anfitrión que la PoC cumplía sin declararla | Sumar al documento §3 la fila RAH2-15 y RAH2-16, y al esqueleto de la PoC Blazor (RAH2-22) esas obligaciones **como lista verificable**, sin fijar dónde vive la PoC en el árbol (R2 de 002) |
| HAH2-04 | Conjunto normativo 13.17, `SDD/Devs/` | **El framework no dice nada de la interoperabilidad .NET ↔ JS**: cero ocurrencias de `JSInterop`, `IJSRuntime`, `IJSObjectReference`, `DotNetObjectReference`, `JSInvokable`, `JSDisconnected`, `_content/`. Lo único: una entrada de glosario para «RCL» y el render mode por defecto de `Design-Rules-Blazor-Mudblazor.md` §2. El documento Blazor de `Conocimiento/` ya declara que el framework no norma ciclo de vida, prerenderizado ni render modes | E1: C1; E2: `Guides/Marco-Teorico-SDD.md` l.1816; `Design-Rules-Blazor-Mudblazor.md` l.82; `Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md` l.911-917 | S3 | 1,0 | Todo lo que `Bundle-JS` diga sobre la costura es **decisión del documento, no norma**; si no lo declara en §8, se lee como regla sin gobierno (anti-patrón «conocimiento disfrazado de regla», `Rules-Base-Conocimiento.md` §4.5) | §8 del documento con el texto de RAH2-26: qué está normado (y se cita) y qué es decisión propia («si el framework las incorpora, manda el framework», fórmula ya usada en el documento Blazor l.916-917). No es un pedido de cambiar el framework: regla 5 |
| HAH2-05 | Precedente `ts/<componente>.ts` l.39-43 («Blazor no puede sostener la instancia, así que la sostiene el módulo») y l.292-294 («el lado .NET no puede sostener la instancia») | **La justificación del índice por contenedor es inexacta como hecho de plataforma.** Una función JS puede devolver un objeto y el lado .NET lo sostiene como `IJSObjectReference` y lo libera con `DisposeAsync`. La decisión (indexar por contenedor) es defendible por otra razón —la misma API exportada sirve a la PoC HTML y a Blazor— pero el documento no debe copiar la razón escrita en el precedente | E2 prec: `ts/<componente>.ts` l.39-44, l.291-294; E2 doc: «Call JavaScript functions from .NET methods in ASP.NET Core Blazor», apartado sobre `IJSObjectReference` como resultado de una función JS y su liberación | S3 | 0,8 | Un hecho de plataforma falso en un documento de conocimiento se propaga a todos los productos que lo carguen; y cierra una alternativa legítima que §4 debería nombrar | En §4 (RAH2-19): registrar la bifurcación «índice por contenedor vs. `IJSObjectReference` a la instancia» con el criterio correcto (una API para dos anfitriones) y el costo de cada rama. Extraer la forma del precedente, no la justificación |

---

## 4. Lo que revisé y está bien

| # | Qué | Ancla | Por qué está bien |
|---|---|---|---|
| 1 | El ciclo de vida del envoltorio del precedente: `import` en `OnAfterRenderAsync(firstRender)`, `DotNetObjectReference` creada después del módulo, liberación en `DisposeAsync` en el orden JS → módulo → referencia .NET, `JSDisconnectedException` atrapada sólo alrededor de lo que habla con el navegador | E2 prec: `DiagramaDeCabecera.razor` l.52-59, l.79-98; E2 doc: «Call JavaScript functions from .NET methods» (aislamiento en módulos, prerenderizado, `JSDisconnectedException`); «Call .NET methods from JavaScript functions» (liberar `DotNetObjectReference`) | Coincide punto por punto con la guía oficial; es la forma que §5 del documento tiene que extraer (RAH2-20) |
| 2 | El nombre del `JSInvokable` explícito de los dos lados, con el comentario que dice por qué («si alguno cambia, el acuse deja de llegar y sólo se nota en ejecución»), y la referencia .NET tipada estructuralmente del lado TS sin importar nada de Blazor | E2 prec: `DiagramaDeCabecera.razor` l.73-77; `ts/<componente>.ts` l.296-299 | Es la práctica correcta para un literal que ninguna compilación verifica; y es lo que hace posible el doble de la PoC HTML (RAH2-09, RAH2-21). Que no esté probado es HAH2-02, no un defecto de esta forma |
| 3 | La frontera de render del anfitrión: la isla interactiva recibe sólo la vista serializable, el `EventCallback` se consume adentro de la isla, el render mode lo declara la página y no la RCL, y la hoja de estilos se enlaza en el `<head>` con la razón escrita | E2 prec: `DiagramaDelEquipo.razor` l.10-16; `MapaDelEquipo.razor` l.217; `App.razor` l.19-25 | Deja a la RCL agnóstica del render mode del anfitrión, que es exactamente lo que necesita un documento que no puede dictar cómo se estructura la solución destino (R2 de 002) |

---

## 5. Solicitudes de convocatoria

```yaml
solicitud_convocatoria:
  rol: AH-1 (Ingeniería del bundle) y V3 (Operación y entrega), ya convocados; se les deriva la señal
  señal: "ADR-00043 §2 decide dos superficies «generadas en la misma compilación» (módulo ES y global clásico), pero el build del precedente emite una sola: `package.json` script `build` produce sólo `wwwroot/<componente>.js` (formato esm), C4 muestra un único módulo ES y `SDD/Maquetas/<F1>-Web/README.md` l.163 declara que la copia de la maqueta es un «prototipo que acompaña la nota de diseño, forma equivalente a la salida <componente>.global.js», fechada 2026-08-10"
  ubicación: "ev-03-precedente.out, bloque package.json; <F1>/src/<F1>.<Componente>/wwwroot/ (C4); SDD/Maquetas/<F1>-Web/README.md l.159-165; SDD/Maquetas/<F1>-Web/assets/js/Adaptador-<Componente>.js l.28 y l.250 (usa `<F1><Componente>.PANELES`, símbolo que el módulo ES no exporta)"
  qué_no_puedo_afirmar: "Si la PoC HTML del documento puede consumir el mismo artefacto que consume la RCL (un módulo ES cargado con `<script type=module>` desde un servidor estático) o necesita la segunda superficie; y si el precedente sirve como forma para «los dos artefactos» que ev-05 pide o sólo para uno. Es código del bundle y cadena de construcción, fuera de mi carta"
```

```yaml
solicitud_convocatoria:
  rol: V2 (Interfaz / consumidor externo)
  señal: "`Contratos-Abstractions-<F1>-<Componente>.md` §3 declara la operación 3 «Actualizar la clasificación» («no rehace el dibujo y no pierde la selección ni el foco»), pero el módulo no la exporta: `Instancia<Componente>` tiene sólo `render`, `seleccionar`, `destruir`, y `ts/contrato.ts` l.167 dice «Todo cambio es un render completo. No hay mutadores parciales». El envoltorio sólo llama `render`"
  ubicación: "<F1>/SDD/Docs/05-Arquitectura-Tecnica/Contratos-Abstractions-<F1>-<Componente>.md §3 fila 3; src/<F1>.<Componente>/ts/contrato.ts l.166-171; ts/<componente>.ts l.308-318; DiagramaDeCabecera.razor l.67-70"
  qué_no_puedo_afirmar: "Cuál de los dos es el contrato (el documento o el código) y qué forma de declaración del contrato debe transmitir `Bundle-JS` para que esa divergencia no se repita. Es diseño del contrato, fuera de mi carta; a mi tema sólo le importa que el envoltorio llama lo que el módulo exporta (RAH2-18)"
```

---

## 6. Preguntas al Product Owner

| Id | Conjetura (`C`) que la funda | Pregunta cerrada | Opciones |
|---|---|---|---|
| PAH2-01 | ev-05, Contexto, cuarto párrafo: «registrar callback blazor en la clase de interfaz en main.js para luego cuando el bundle.js dispare dichos callback», y Solicitudes, primer ítem: el proyecto del bundle «arrojará dos artefactos: main.js y el bundle». Conjeturo que `main.js` es la capa que conoce la referencia .NET (`invokeMethodAsync`) y que el bundle no sabe que .NET existe. El precedente no separa esas dos cosas: un solo módulo exporta `montar` (callbacks planos, para HTML) y `montarConInterop` (referencia .NET) — `ts/<componente>.ts` l.281-306. No puedo extraer de ahí cuál de las dos formas quiere el PO | ¿Qué es `main.js` respecto de la referencia .NET? | (a) **Adaptador a .NET**: `main.js` es el único JS que conoce `invokeMethodAsync`; el bundle expone callbacks planos y no sabe de Blazor; la PoC HTML usa el bundle directo o `main.js` con un doble de la referencia. (b) **Fachada única**: `main.js` es el punto de entrada que usan la PoC HTML y Blazor por igual, y la referencia .NET es un callback más entre otros (forma del precedente, con dos funciones de montaje en el mismo módulo). (c) Otra, que el PO describa |

---

Sigue: consolidación · presidente de mesa
