# Actuación 003 — Informe Comisión N1, Requisitos

| Campo | Valor |
|---|---|
| Tipo | `informe` |
| Fecha | 2026-09-13 |
| Autor | Comisión N1, Requisitos |
| Corrige | — |

## 1. Mandato y método

**Pregunta que respondo** (carta `ev-07/01-N1-requisitos.md`): qué tiene que cumplir el documento `Bundle-JS` para satisfacer el pedido, y qué del pedido está mal formulado, es ambiguo, se contradice o no es verificable. No decido arquitectura del bundle (V1, AH-1, AH-2), ni formato del documento (N2), ni cómo lo lee un tercero (N3).

**Qué leí, en orden.** Rutas absolutas abreviadas: `IA.SDD` = `<workspace>/IA/SDD/IA.SDD`; `EXP` = `IA.SDD/Expedientes/0002-Conocimiento-Bundle-JS-En-Solucion-Dotnet`; `INPUT` = `<workspace>/IA/SDD/IA.SDD.Documentacion/PROMPTs/SDD/Catalogado/02-Extraccion-Concepto-Espcificacion-Integracion-Proyecto-Bundle-JS/INPUTs/Bundle-JS-En-Solucion-Blazor.md`; `PREC` = `<repo-privado-F1>`, leído por `git show 9aabe5c:` para coincidir con `ev-03`.

| # | Insumo | Qué busqué |
|---|---|---|
| 1 | `EXP/evidencia/ev-07-cartas-despachadas/00-encargo-comun.md`, `01-N1-requisitos.md` | Reglas y mandato |
| 2 | `EXP/evidencia/ev-05-presentacion-original.md` | El pedido, literal |
| 3 | `INPUT` (299 líneas, huella en `ev-04`) | Análisis previo: qué afirma del framework |
| 4 | `EXP/actuaciones/001-*.md`, `002-*.md` | Resumen del pedido y contrato de entrada (E4) |
| 5 | `EXP/evidencia/ev-01..ev-06 .out/.sh` | Base, citas verificadas, precedente, chequeo mecánico |
| 6 | `IA.SDD/SDD/Devs/Rules/Rules-Base-Conocimiento.md` | Qué le exige el formato al contenido: §0.1, §3.2, §4.1, §4.2, §4.4, §4.5, §6, §7 |
| 7 | `IA.SDD/Conocimiento/README.md`, `Index-Knowledge.md`, `Knowledge-Conformacion-Pull-Request-Manual.md`, `Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md` (§0, §1, §6–§10) | Cómo un documento real declara alcance, frontera y desviación |
| 8 | `IA.SDD/CHANGELOG.md` [13.17] y [13.16]; `SDD/Devs/Guides/Coherencia-Proyecto-De-Otro-Ecosistema.md` | Qué cambió en el framework el mismo día del caso |
| 9 | `IA.SDD/SDD/Devs/Rules/Intake-Rules.md` §4; `Rules-Examples.md` §0–§3.6, §4.5, §4.6, §6; `Rules-Devops.md` §4.8, §4.9; `Maqueta-Rules.md` §1.2, §4.1, §7.2; `Vocabulario-Rules.md` §2; `Intake/PRODUCT-INTAKE-template.md` §13, §14, §16, §17.P.13, §18; `Intake/PRODUCT-MANIFEST-template.md` §1.2, §2, §3; `Orchestrator/Master-Prompt.md` l.573-574; `README.md` invariantes | Dónde el framework ya dice algo del caso y dónde no |
| 10 | `PREC`: `src/<F1>.<Componente>/{Contrato.cs, ts/contrato.ts, ts/<componente>.ts, DiagramaDeCabecera.razor, <F1>.<Componente>.csproj, package.json, .gitignore, tsconfig.json}`; `SDD/Docs/05-Arquitectura-Tecnica/Adrs/ADR-00042-*.md`, `ADR-00043-*.md`, `Contratos-Abstractions-<F1>-<Componente>.md`; `SDD/Maquetas/<F1>-Web/README.md` §9, `assets/js/Adaptador-<Componente>.js` (cabecera), `assets/vendor/<componente>.js` (cabecera) | La forma del precedente, no su dominio |

**Qué no leí y por qué.** Ningún otro informe en `actuaciones/` (regla 1: a ciegas; al momento del despacho sólo existen 001 y 002). No leí `Mesa-Evaluadora.md` de `IA.Prompts`: el encargo fija forma y reglas, y el marco es del presidente. No leí el cuerpo de `<componente>.ts` fuera de la cabecera y la API exportada, ni los `*.test.ts`: la forma de la API alcanza para requisitos; el interior es de AH-1. Comandos ejecutados y sus salidas están citados en línea como `E1`.

**Convenciones de este informe.** Niveles `E1`–`E4`, `C` según el encargo. Severidad: S1 contradice al framework o vuelve inútil el documento; S2 deja un requisito sin poder cumplirse o verificarse; S3 ambigüedad que un lector resuelve mal con probabilidad; S4 forma. Cada fila de relevamiento lleva su **criterio de aceptación** dentro de la columna «Qué tiene que decir», precedido por «Criterio:».

---

## 2. Relevamiento para el documento `Bundle-JS`

| Id | Sección destino | Qué tiene que decir | Ancla (nivel + cita) | Riesgo si falta |
|---|---|---|---|---|
| RN1-01 | Cabecera (§4.1) | Alias `Bundle-JS`, naturaleza `propio`, `Hereda-de: —`, `Sustituye: —`, `Compatible-con: Rules-Base-Conocimiento.md 2.2`. Criterio: once campos, ninguno vacío; los siete primeros coinciden literalmente con la fila de índice propuesta | E4: actuación 002 §2 «decisiones cerradas» · E2: `Rules-Base-Conocimiento.md` l.236-239 «Once campos, ninguno vacío» · E2: `Index-Knowledge.md` l.4 «Compatible con: Rules-Base-Conocimiento.md 2.0» y filas con `2.2` | Sin cabecera coincidente el §6.1 ítem 6 falla y ningún fork puede catalogarlo sin editarlo |
| RN1-02 | Cabecera (§4.1) | `Tema` en una línea que nombre las **cuatro piezas** del pedido: proyecto del bundle (dos artefactos), anfitrión HTML sin .NET, biblioteca de clases Razor que integra, anfitrión Blazor mínimo. Criterio: las cuatro piezas aparecen en `Tema` y en §0 con el mismo nombre | E2: `ev-05` l.36-39 (los cuatro «Crear …») · E2: actuación 001 §2 punto 2 | Un lector que decide por el índice (§4.1 «divulgación progresiva», l.241-242) no sabe que el documento cubre los anfitriones y no sólo el bundle |
| RN1-03 | Cabecera (§4.1) | `Condicion-de-carga` expresada contra campos del intake: un proyecto de código de ecosistema JavaScript cuyo artefacto un proyecto Blazor de la misma solución toma como **insumo de construcción** (§13.2 del intake, columna Stack y marca). Criterio: la condición nombra sólo campos que el intake tiene | E2: `Rules-Base-Conocimiento.md` l.397 «Expresada contra campos del intake, flags o tipo D8» · E2: `PRODUCT-INTAKE-template.md` l.353 (pregunta por proyecto de otro ecosistema e insumo de construcción) · E2: `Intake-Rules.md` l.123-143 | Con `—` sólo se carga por cita de alias; con una condición inventada nunca dispara |
| RN1-04 | Cabecera (§4.1) | `Consumidor` como lista, y **justificada por pieza**: quién escribe el contrato (05), quién escribe los anfitriones como samples (10), quién declara la cadena de construcción (09), quién prueba sin anfitrión (08). Criterio: cada consumidor listado tiene al menos una sección del documento que lo nombra como destinatario | E2: `Rules-Base-Conocimiento.md` l.417-419 (§7.3: obligatorio, admite lista, evita inflación) · E2: `ev-05` l.33 «agentes IA, bajo sus especialidades y tareas asignadas» | Consumidor único: el conocimiento no llega al agente que lo necesita; consumidor `transversal`: se inyecta en doce despachos |
| RN1-05 | §0 Propósito y alcance | Declarar la **frontera R2** con precisión operativa: queda **afuera** todo lo que el framework anfitrión genera o nombra —ubicación de carpetas (`/src`, `/samples`, `/demos`), pertenencia al agrupador de la solución, nombres de proyecto, cantidad y nombre de samples—; queda **adentro** la estructura interna del proyecto del bundle y de la biblioteca de clases Razor, y el contrato entre ambos. Criterio: §0 tiene una tabla «afuera / dónde vive» y ninguna ruta de solución aparece en §2–§5 salvo como marcador `<según el framework anfitrión>` | E2: `ev-05` l.35 «no debe dictar como se estructura la solución .NET» y l.41 (`/demos` vs `/samples`) · E4: 002 §2 R2 · E2: `Rules-Base-Conocimiento.md` l.51-56 tabla de §0.1: «Nomenclatura interna del artefacto caracterizado» va en conocimiento; «Nomenclatura y trazabilidad de lo que el framework genera» no · E2: `Knowledge-Conformacion-Pull-Request-Manual.md` l.36-43 (forma de la tabla «Qué queda explícitamente afuera») | Sin frontera escrita, cada lector traza la suya: uno omite la estructura del proyecto (documento inútil) y otro dicta `/samples` (viola R2). Ver HN1-04 |
| RN1-06 | §0 Propósito y alcance | Declarar fuera de alcance, con dónde vive: convertir la RCL en NuGet; la decisión de si el mapa es unidad de entrega (`PA-04`); el dominio del ejemplo; la integración de una API JavaScript de terceros con clave (salvo que el PO la incluya, ver P-01). Criterio: cada exclusión con destino nombrado | E2: `ev-05` l.38 «queda fuera del alcance este objetivo de convertirse en un nuget» · E4: 002 §2 `fuera_de_alcance` · E2: `ev-05` l.17 nombra «apis javascript externas (como es el caso de googlemaps)» y ningún otro insumo lo cubre (E1 abajo, HN1-06) | «Sin el qué queda afuera de §0, el agente completa el hueco por su cuenta» (`Rules-Base-Conocimiento.md` l.260-261) |
| RN1-07 | §0 / §1 | Definir el término «PoC» **como término propio del documento**, mapeado a la figura del framework anfitrión: en este framework es el **anfitrión mínimo** de `Rules-Examples.md` §3.6, un sample de categoría 10; en otro framework, la figura que ese framework tenga para «código ejecutable que carga el artefacto construido». Criterio: «PoC» aparece definido una vez y todas las demás menciones lo usan con ese único sentido | E1: `ev-02-citas.out` última línea, `grep -rniE '\bPoC\b|prueba de concepto|proof of concept'` sobre `SDD PROMPTS README.md Conocimiento` = **0** · E2: `Rules-Examples.md` l.210-223 («El sample de un artefacto que otro proyecto carga … es un anfitrión mínimo») · E2: `Rules-Examples.md` l.518 (vocabulario del método se cita sin redefinir) | El documento acuña un término que el método no tiene y el lector lo confunde con maqueta (Fase B2) o con spike |
| RN1-08 | §1 Identidad del artefacto | Qué es lo caracterizado, en una oración: **la costura entre un proyecto de código JavaScript/TypeScript y una biblioteca de clases Razor que lo carga**, con sus dos anfitriones de verificación. Supuestos: Blazor con interoperabilidad JavaScript por módulo; un ecosistema de paquetes de JavaScript con archivo de bloqueo; una cadena de empaquetado. Criterio: §1 no nombra ninguna herramienta como obligatoria (nombra la clase de herramienta) | E2: `ev-05` l.17 «artefactos javascript en el que se le pasan los datos definidos por DTOs y este artefacto opera sobre la visualización independizándose de … la lógica de negocio» · E2: `ADR-00043` §2 «Hacia adentro el componente no se comunica con nada» · E2: `Coherencia-Proyecto-De-Otro-Ecosistema.md` l.21-22 (ninguna herramienta en texto normativo) | Sin identidad, §5.1 pregunta 1 de `Rules-Base-Conocimiento.md`: la captura sale como resumen del proyecto |
| RN1-09 | §1 Identidad | Definir **los dos artefactos** que el pedido exige —`main.js` y el bundle— con responsabilidad de cada uno: quién los produce, quién los carga, qué invoca a qué, y si `main.js` es salida de compilación o fuente escrita a mano. Criterio: una tabla de dos filas con columnas «lo produce / lo carga / expone / invoca», sin celda vacía | E2: `ev-05` l.36 «Este arrojará dos artefactos: main.js y el bundle» y l.17 «llaman a través de métodos de un main.js a un bundle javascript … o bien registrar callback blazor en la clase de interfaz en main.js» · E1: `git ls-tree -r --name-only 9aabe5c -- src/<F1>.<Componente> \| grep -c 'main\.js'` = **0**: el precedente no tiene `main.js`; su superficie de interoperabilidad es `montarConInterop` dentro del punto de entrada (`ts/<componente>.ts` l.296-306) · E1: `grep -rn 'main.js' SDD/Devs README.md Conocimiento \| wc -l` = **0** en el framework | Ver HN1-03: sin esta definición, «main.js» significa una cosa en el proyecto del bundle y otra en la PoC HTML |
| RN1-10 | §1 / §4 | Distinguir **artefactos** (lo que el pedido cuenta: dos) de **superficies de consumo** (módulo ES para Blazor; global clásico para un anfitrión sin empaquetador), y decir si el documento exige una o las dos. Criterio: la tabla de §1 dice cuántos archivos salen de la compilación y con qué formato cada uno | E2: `ADR-00043` §2 «dos superficies de consumo, generadas en la misma compilación: módulo ES … global clásico» · E1: `git show 9aabe5c:src/<F1>.<Componente>/package.json \| grep -o 'outfile=[^ "]*'` = `outfile=wwwroot/<componente>.js` (una sola salida, `--format=esm`); `grep -c global` = **0** | Ver HN1-05: el precedente declara dos superficies y construye una; copiarlo sin mirarlo transmite una decisión que no está implementada |
| RN1-11 | §2 Estructura | Layout interno del **proyecto del bundle**: fuente (`ts/` o `src/`), contrato como archivo propio, punto de entrada, pruebas junto a la fuente o aparte, `package.json` con `build` y `test`, archivo de bloqueo versionado, configuración del compilador estricta, y **la salida como directorio de construcción no versionado**. Criterio: cada archivo del layout tiene rol declarado; el `.gitignore` de referencia excluye la salida y `node_modules` | E2: `ev-03-precedente.out` árbol de `src/<F1>.<Componente>` (27 entradas) · E2: `PREC` `.gitignore` (`node_modules/`, `wwwroot/<componente>.js`, `wwwroot/<componente>.css`, `build/`) · E2: `ADR-00042` §2 punto 4 «El bundle no se versiona» · E2: `tsconfig.json` (`strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`) | Sin layout, `Rules-Base-Conocimiento.md` §6.2: un `propio` sin estructura no le da al agente «identidad, estructura, contrato y esqueletos» |
| RN1-12 | §2 Estructura | Layout de la **biblioteca de clases Razor**: componente `.razor` envoltorio, tipos del contrato del lado .NET en archivo hermano, `wwwroot/` como destino de los artefactos, y cómo se sirven los estáticos de una RCL (ruta `_content/<Nombre>/`). Criterio: el documento dice que `wwwroot/` es salida y no fuente, y que la ruta de carga se deriva del nombre del ensamblado | E2: `DiagramaDeCabecera.razor` l.17 «El estatico se sirve solo bajo `_content/<F1>.<Componente>/`» y l.57 (`import` de `./_content/…/<componente>.js`) · E2: `.csproj` comentario l.74-78 «`wwwroot/` es salida de construccion y no fuente» · Stack: documentación oficial de ASP.NET Core Blazor, tema «Reusable Razor UI in class libraries» (ruta `_content/{PACKAGE ID}`) | El agente pone el bundle a mano en `wwwroot/` del consumidor y pierde la propiedad «un solo comando construye todo» |
| RN1-13 | §2 / §4 | Decir si el TypeScript vive **dentro** del proyecto de la RCL (un proyecto de código, dos cadenas) o en un **proyecto de código aparte** (dos proyectos, arista `insumo de construcción`), y las consecuencias de cada opción en el manifiesto del framework anfitrión. Criterio: las dos composiciones descriptas con la misma tabla de consecuencias; el documento **no elige por el destino** (R2) o, si elige, lo rotula como decisión ya tomada en §4 con su criterio | E2: `ADR-00042` §2 punto 3 «el envoltorio Blazor y el TypeScript que consume viven en el mismo proyecto» · E2: `INPUT` §8.4 l.266-283 (dos proyectos) · E2: `Intake-Rules.md` l.127-135 (pertenencia por insumo de construcción) y l.143 («la única clase posible entre proyectos de ecosistemas distintos») · E2: `Rules-Devops.md` l.284 «dentro de un mismo proyecto de código» (el framework admite las dos) | Elegir una sola sin decirlo dicta la estructura de la solución (viola R2); no describir ninguna deja al agente sin forma. La elección es de V1; el requisito es que el documento la presente como alternativa con criterio |
| RN1-14 | §2 Estructura | Los **dos anfitriones** como piezas del artefacto y no como apéndices: anfitrión HTML sin .NET (página + script de arranque + doble del consumidor + datos por escenario) y anfitrión Blazor mínimo (app que referencia la RCL y muestra el componente con datos por escenario). Criterio: cada anfitrión tiene tabla de archivos con rol, y ninguno contiene lógica del consumidor real | E2: `ev-05` l.37 y l.39 · E2: `Rules-Examples.md` l.214-221 (carga el artefacto construido, invoca la superficie pública y nada más, datos por escenario, doble del consumidor) · E2: `Adaptador-<Componente>.js` l.7-8 «Este archivo es de la maqueta y NO se traslada al producto» (forma del script de arranque en el precedente) | Un anfitrión que reproduce el consumidor real deja de probar el contrato y prueba la aplicación |
| RN1-15 | §3 Contrato de uso | La **forma del contrato entre los dos lenguajes**: tipos declarados **una vez de cada lado** en archivos hermanos, con los mismos nombres y las mismas formas; conjuntos cerrados como uniones de literales del lado TypeScript y constantes de cadena del lado .NET (no `enum`); un campo de versión del esquema en el DTO raíz. Criterio: el documento da la tabla de correspondencia tipo a tipo y dice qué pasa si un lado cambia sin el otro | E2: `Contrato.cs` l.3-4 «declarados una vez de cada lado de la costura» y l.8-10 (cadenas con conjunto cerrado en vez de `enum`) · E2: `contrato.ts` l.7-8 · E2: `Contrato.cs` l.114 `Version = 1` y `contrato.ts` l.114 `version: 1` · E2: `Contratos-Abstractions-<F1>-<Componente>.md` §1 «cruza dos lenguajes … se declaran una vez de cada lado» | Es lo que el pedido llama «base contractual entre los diferentes artefactos» (`ev-05` l.17); sin esto el documento no responde el pedido |
| RN1-16 | §3 Contrato de uso | La **convención de nombres a través de la costura**: PascalCase en .NET, camelCase en JavaScript, y que la correspondencia la resuelve la serialización de la interoperabilidad de Blazor con su política por defecto; qué hacer cuando un nombre no mapea. Criterio: un ejemplo de par (`Posicion` / `posicion`) y la regla escrita | E2: `Contrato.cs` l.48-56 (`Posicion`, `Columna`, `Rotulo`) contra `contrato.ts` l.23-33 (`posicion`, `columna`, `rotulo`) · Stack: documentación oficial de ASP.NET Core Blazor, tema «JavaScript interop: JSON serialization» | El agente escribe los DTO en PascalCase del lado JS o configura el serializador a mano, y los dos lados dejan de coincidir «en silencio» |
| RN1-17 | §3 Contrato de uso | El **protocolo de invocación** en las dos direcciones: .NET importa el módulo, monta pasándole el elemento contenedor y una referencia .NET; el módulo sostiene la instancia indexada por contenedor; el módulo invoca de vuelta un método con nombre **declarado explícito de los dos lados**; la liberación se hace en un orden (primero el navegador, después la referencia .NET) y tolera el circuito caído. Criterio: secuencia de cinco pasos con quién hace cada uno, y el nombre del callback aparece dos veces en el esqueleto (una por lado) | E2: `DiagramaDeCabecera.razor` l.56-59, l.72-77 («si alguno cambia, el acuse deja de llegar y solo se nota en ejecucion, asi que el nombre se escribe explicito de los dos lados»), l.81-95 · E2: `<componente>.ts` l.36-44 («Blazor no puede sostener la instancia, asi que la sostiene el modulo»; `WeakMap<HTMLElement, …>`), l.296-306 · Stack: documentación oficial de ASP.NET Core Blazor, temas «Call JavaScript functions from .NET methods» y «Call .NET methods from JavaScript functions» (`DotNetObjectReference`, `[JSInvokable]`, `IJSObjectReference`, `JSDisconnectedException`) | Es el «registrar callback blazor en la clase de interfaz» de `ev-05` l.17. Sin protocolo, la fuga de referencias y el callback mudo son los dos fallos que el pedido pide diagnosticar |
| RN1-18 | §3 Contrato de uso | La **superficie mínima del bundle** que un anfitrión invoca: montar (con opciones y manejador de salida), dibujar/actualizar, liberar; y la forma de la única salida (evento con datos, información y no orden). Criterio: la superficie está listada como tabla de operaciones con «recibe / devuelve», y el doble del consumidor de la PoC HTML la cubre entera | E2: `<componente>.ts` l.281-318 (`montar`, `montarConInterop`, `render`, `seleccionar`, `destruir`) · E2: `Contratos-Abstractions-<F1>-<Componente>.md` §3 (cinco operaciones y una salida) · E2: `Rules-Examples.md` l.222-223 «La superficie pública completa la cubre el conjunto de samples» | Sin superficie declarada no hay criterio para «la PoC HTML invoca como lo haría Blazor» |
| RN1-19 | §3 / §4 | La **cadena de construcción** como contrato: versión del entorno de ejecución de JavaScript declarada en el proyecto y verificada antes de construir; instalación reproducible contra archivo de bloqueo; el bundle se emite **antes** de que el anfitrión .NET descubra sus estáticos; las pruebas del bundle corren sin anfitrión; y qué pasa donde falta la cadena (falla y la nombra, o modo de construcción explícito). Criterio: cinco filas con «qué / por qué / qué se rompe si falta» | E2: `.csproj` l.31 (`VersionDeNodeRequerida`), l.34-43 (`VerificarNode`, error que nombra la cadena), l.45-52 (`npm ci` contra bloqueo), l.53-66 («si se genera despues, el manifiesto lo registra sin que el archivo este en disco y el modulo falla al pedirlo»; `BeforeTargets="ResolveStaticWebAssetsInputs;BeforeBuild"`), l.79-99 (pruebas sin anfitrión, apagables) · E2: `Rules-Devops.md` l.284 (cadena ausente: modo explícito o falla que la nombra) y l.274-275 (anti-patrones) · E2: `ADR-00042` §7 · Stack: documentación oficial de MSBuild, tema «Target build order»; de ASP.NET Core, tema «Static web assets» | Es lo que el pedido llama «diagnóstico de detección de errores» (`ev-05` l.25): el fallo más frecuente del precedente es de orden de construcción, y el documento tiene que nombrarlo |
| RN1-20 | §4 Decisiones ya tomadas | Tabla de bifurcaciones con criterio: bundle no versionado vs. versionado; tipos declarados dos veces vs. generados de un lado; empaquetador mínimo vs. de propósito general; TypeScript vs. JavaScript a mano; instancia sostenida por el módulo vs. por .NET; pruebas enganchadas a la construcción vs. a `dotnet test`. Criterio: cada fila con «cómo se resolvió» y «criterio», y las que el documento deja al destino marcadas como tales | E2: `ADR-00042` §4 (tabla de alternativas, incluida «JavaScript a mano … no es una alternativa de paja») · E2: `.csproj` l.79-90 («Va enganchada a la construccion y no a `dotnet test`, porque este proyecto … es una biblioteca») · E2: `Rules-Base-Conocimiento.md` l.252 (§4: «Las bifurcaciones que el artefacto resolvió, con el criterio de cada una») | El agente rehace decisiones ya tomadas o, peor, toma la contraria sin saber que existía la bifurcación |
| RN1-21 | §5 Esqueletos | Lo mínimo reproducible de **cada una de las cuatro piezas**, ofuscado: `package.json` con `build` y `test`; contrato en los dos lenguajes (un DTO, una unión, un evento); punto de entrada con montar/render/destruir y variante con interop; envoltorio Razor con importación, montaje, callback y liberación; fragmento de proyecto .NET con los targets de la cadena; página HTML con script de arranque y doble del consumidor; componente de app Blazor mínima. Criterio: cada esqueleto compila o se abre con el comando que el documento declara, y ningún esqueleto lleva nombre del dominio de origen | E2: `Rules-Base-Conocimiento.md` l.253 y l.374-376 (§5 es la única excepción admitida al techo) · E2: `Rules-Base-Conocimiento.md` l.153-156 («las formas constructivas del HTML, del CSS y del JavaScript se transmiten mal en prosa») · E4: 002 §2 R4 (ofuscado) | Sin esqueletos, un `propio` no cumple su techo por defecto (§6.2: «hay que darle … esqueletos»). Ver HN1-07 por el tamaño |
| RN1-22 | §6 Criterios de aceptación | Criterios **verificables** de que el conocimiento se aplicó: los tipos coinciden nombre a nombre entre los dos lados; el nombre del callback aparece idéntico en los dos lados; el bundle no está versionado; la construcción del consumidor falla nombrando la cadena si falta; el anfitrión HTML carga la salida de construcción y no una copia; el anfitrión Blazor referencia la RCL y no copia el bundle; las pruebas del bundle corren sin navegador; el doble del consumidor de la PoC HTML lista las mismas invocaciones que el envoltorio expone, derivadas de una sola fuente. Criterio: cada ítem marcado `[enumerable]` o `[interpretativo]` con el comando o la lectura que lo decide | E2: `Rules-Base-Conocimiento.md` l.254 (§6 «Cómo se verifica que el conocimiento se aplicó bien») · E2: `Rules-Examples.md` l.214-221 · E2: `Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md` l.822-837 (forma de los criterios con marca) · E2: `ADR-00042` §8 (métricas: archivos de bundle versionados = 0; comandos para construir = 1) | El objetivo del pedido «evaluación de diseño de contratos … autoajustes» (`ev-05` l.25) no tiene hoy criterio de aceptación; ver HN1-07 |
| RN1-23 | §7 Anti-patrones | Los fallos con síntoma y causa: bundle generado después del descubrimiento de estáticos (síntoma: el módulo no se encuentra en ejecución aunque el archivo exista después); callback con nombre distinto de un lado (síntoma: el acuse nunca llega, sin error); liberar la referencia .NET antes que la instancia del navegador (fuga); anfitrión que consume una copia del bundle (valida un componente que no existe); `enum` cruzando JSON; instancia guardada del lado .NET; `npm install` flotante; bundle versionado en el árbol. Criterio: cada fila con síntoma observable, causa y corrección | E2: `.csproj` l.53-59 · E2: `DiagramaDeCabecera.razor` l.72-75, l.81-84 · E2: `Maquetas/<F1>-Web/README.md` l.159 «a mano no, porque se desincroniza y nadie se entera hasta que la maqueta valida un componente que ya no es el que corre» · E2: `Contrato.cs` l.8-10 · E2: `Rules-Examples.md` l.214-215 «una copia deja de probar el contrato real en cuanto el productor cambia» · E2: `Rules-Base-Conocimiento.md` l.330-331 («¿Qué se intentó y salió mal? Eso es §7 … de lo más valioso») | Es el «diagnóstico de fallas de contratos por parte de los agentes» de `ev-05` l.25: sin síntomas, el agente no diagnostica, adivina |
| RN1-24 | §8 Frontera con las reglas | Qué de este tema es normativo **en este framework** y se cita sin redefinir: pertenencia del proyecto de otro ecosistema a la solución y clase de arista (`Intake-Rules.md` §4); único generador y cadena ausente (`Rules-Devops.md` §4.9 punto 4); forma y ubicación de los samples, anfitrión mínimo y prohibición de enganchar su verificación a la construcción (`Rules-Examples.md` §2.3, §3.6); la maqueta como cosa distinta (`Maqueta-Rules.md` §4.1, §7.2); contrato de compilación vs. de integración (`PRODUCT-INTAKE-template.md` §14). Y **cómo se lee esa sección desde otro framework**: cada fila dice qué propiedad rige y que el destino la busca en su propia norma. Criterio: §8 no define criterios de aceptación de samples, ADR ni manifiesto; toda regla citada existe en 13.17 | E2: `Rules-Base-Conocimiento.md` l.256, l.260-262 («Sin §8, el documento empieza a leerse como norma») y l.289 (anti-patrón «Conocimiento disfrazado de regla») · E2: `Master-Prompt.md` l.574 («Un documento de conocimiento nunca redefine criterios de aceptación, nomenclatura de artefactos generados ni gating») · E2: `ev-05` l.17 «transmitir el conocimiento a otros Frameworks SDD similares» | Sin §8, el documento dicta dónde van las PoC y viola R2 por la vía indirecta |
| RN1-25 | §8 Frontera | Declarar la **desviación respecto de la regla de samples** si el anfitrión HTML se construye como sample de este framework: `Rules-Examples.md` §3.6 exige cargar el artefacto **construido**, nunca una copia; el precedente lo hace con copia. El documento tiene que ir con la regla y decirlo. Criterio: §7 nombra la copia como anti-patrón y §8 cita §3.6 como la regla que manda | E2: `Rules-Examples.md` l.214-215 · E1: `git show 9aabe5c:SDD/Maquetas/<F1>-Web/assets/vendor/<componente>.js \| grep -n 'Prototipo del componente'` = l.4 («Prototipo del componente. Es la forma que tendria la salida transpilada») · E2: `Maquetas/<F1>-Web/README.md` l.163 (fecha de copia 2026-08-10, «prototipo que acompaña la nota de diseño») | Ver HN1-05: transmitir la forma del precedente tal cual transmite el anti-patrón que la regla vigente prohíbe |
| RN1-26 | §9 Trazabilidad | Índice al que pertenece (el `Index-Knowledge.md` de la base que lo aloje), hermanos (los que resulten de partir por el techo), consumidor, artefacto de referencia (si los esqueletos de §5 no alcanzan, un anfitrión HTML ejecutable depositado junto al documento), origen del relevamiento **ofuscado**: «un producto del workspace con una biblioteca de clases Razor que compila TypeScript en su construcción». Criterio: ninguna ruta ni nombre del cliente en §9 | E2: `Rules-Base-Conocimiento.md` l.210-212 (§3.3) y l.153-156 (§2.2 artefacto de referencia) · E4: 002 §2 R4 · E2: encargo común «el documento final lo va a citar ofuscado» | Sin origen declarado, nadie puede volver a la fuente cuando la forma cambie; con origen sin ofuscar, viola D7 |
| RN1-27 | §9 / fila de índice | La **fila de índice propuesta** con las diez columnas, entregada junto al documento aunque el destino sea `OUTPUTs/`, y una nota de adopción: «copiar el documento a `Conocimiento/` del fork y la fila a su `Index-Knowledge.md`; ninguna edición». Criterio: la fila pasa los ocho campos comunes contra la cabecera sin cambios | E4: 002 §2 `objeto` «más su fila de índice propuesta» · E2: `Rules-Base-Conocimiento.md` l.146-149 (tabla maestra: fila obligatoria) y l.357 · E2: `PRODUCT-INTAKE-template.md` l.584 «Un alias que no resuelve contra el índice es error bloqueante» · E2: `Master-Prompt.md` l.573 (el orquestador sólo carga lo que el índice declara) | Ver HN1-06: sin fila, ningún agente de este framework ni de un fork recibe el documento, y el objetivo del pedido queda inverificable |
| RN1-28 | §1 / §8 | Qué **no** es lo caracterizado aunque esté al lado: la maqueta de la Fase B2 (sin build, datos hardcodeados, valida experiencia); el spike; la unidad de entrega `library` redistribuible como paquete del ecosistema JavaScript (caso B del análisis previo). Criterio: tres filas «no es / se parece en / diferencia» | E2: `Maqueta-Rules.md` l.48 y l.335-341 (§4.1 sin build) y l.493-497 (§7.2 excepción por ADR, y convención de escala por sustitución) · E2: `Rules-Backlog-Tecnico.md` l.247 (spike con caja temporal) · E2: `INPUT` §5 y §8.2 · E2: `Rules-Base-Conocimiento.md` l.313 (pregunta 5) | El agente usa la maqueta como PoC HTML y le mete un proceso de build, que `Maqueta-Rules.md` §7 prohíbe |
| RN1-29 | §0 | Declarar la **versión del framework contra la que se escribió** el contenido de §8 (13.17) y que el documento no es normativo para el framework: vive en `OUTPUTs/` por decisión del PO y no cataloga nada por sí solo. Criterio: `Compatible-con` y la mención de versión coinciden con `ev-01-base.out` | E1: `ev-01-base.out` «framework vigente: ## [13.17] - 2026-09-13» · E2: `ev-05` l.43 · E4: 002 §1 | Un lector que lo tome por norma del framework desplaza `Rules-Examples.md`; uno que lo tome por viejo lo descarta |

---

## 3. Hallazgos

| Id | Ubicación | Afirmación | Evidencia (nivel + ancla) | Severidad | Confianza | Impacto si no se corrige | Dirección propuesta |
|---|---|---|---|---|---|---|---|
| HN1-01 | `INPUT` cabecera l.7; §4 obs. 2 l.143-145; §6 l.175-177; §8.4 l.288-289 | El análisis previo está escrito contra 13.15 y **quedó superado por 13.16 el mismo día**: afirma «hay dos soluciones de código (el `.sln` y el workspace npm)» y que el uso del `dist/` es «consumo de artefacto publicado, no arista», y que incorporar el bundle al `.sln` «no está en el framework». Desde 13.16 el framework dice lo contrario: un proyecto de otro ecosistema **pertenece a la solución** de un proyecto que toma su artefacto como insumo de construcción, la arista existe y se llama `insumo de construcción`, y declararla como consumo publicado obligaba «a declarar como consumo de artefacto publicado un artefacto que no se publica» | E1: `grep -c 'insumo de construcci' INPUT` = **0**; `grep -c '§3.6' INPUT` = **0** · E1: `grep -c 'insumo de construcci' Intake-Rules.md Rules-Examples.md` = 5 y 1 · E2: `Intake-Rules.md` l.127-132, l.143 · E2: `CHANGELOG.md` [13.16] «La decisión de fondo: sí, con frontera» · E2: `INPUT` l.7 «13.15 según memoria de proyecto; verificar en CHANGELOG.md» (la verificación pedida no se hizo) | S2 | 0.95 | El documento hereda del insumo un modelo de composición que el framework anfitrión ya no acepta; su §8 citaría reglas que dicen otra cosa, y el intake de un destino que lo siga falla la validación de `Intake-Rules.md` §4 | Reescribir §2.3, §4 obs. 2, §6 y §8.4 del análisis previo contra 13.17 antes de usarlo como insumo del documento; PA-01 queda parcialmente contestada por `Intake-Rules.md` l.161-165 («la forma concreta … es del destino y se declara en sus decisiones de arquitectura») |
| HN1-02 | `INPUT` §8.2 l.208-231 | Afirma que «lo más cercano en las reglas son tres figuras» (spike, maqueta, sample) y que la PoC HTML no tiene lugar; **omite la cuarta**, que es la que encaja: el **anfitrión mínimo** de `Rules-Examples.md` §3.6, un sample que carga el artefacto construido, invoca su superficie pública y nada más, con doble del consumidor cuando el artefacto invoca de vuelta. El término «PoC» sigue con cero ocurrencias, pero la figura existe | E1: `ev-02-citas.out` última línea = 0 · E1: `grep -ci 'anfitri.n m.nimo' INPUT` = **0** · E2: `Rules-Examples.md` l.210-223 · E2: `CHANGELOG.md` [13.16] «§3.6 es nueva … la forma del anfitrión mínimo de un artefacto que otro proyecto carga» | S2 | 0.9 | El encaje de §8.3 del análisis previo (samples por nivel: básico/intermedio) se propone sin la figura que ya resuelve el caso, y PA-04 se plantea como si sólo la unidad `library` diera lugar normativo a la PoC; §3.6 no exige unidad propia: exige que «la unidad de entrega contenga un proyecto de código cuyo artefacto otro proyecto toma como insumo de construcción» | Reescribir §8.2-8.3 del análisis previo sobre §3.6; reformular PA-04 en esos términos antes de elevarla al PO |
| HN1-03 | `ev-05` l.17, l.36, l.37; encargo común «El caso»; `INPUT` l.198, l.254, l.278 | «`main.js`» se usa con **dos referentes**: (a) un artefacto que **produce el proyecto del bundle** y donde se registran los callbacks de Blazor («Este arrojará dos artefactos: main.js y el bundle»); (b) el **script de la PoC HTML** que «invoca el bundle como lo haría Blazor». No son el mismo archivo: (a) lo carga la RCL, (b) lo carga una página sin .NET. El precedente no tiene ninguno de los dos con ese nombre: su interoperabilidad es una función exportada del bundle (`montarConInterop`) y su anfitrión HTML tiene un adaptador propio que «NO se traslada al producto» | E1: `grep -c 'main.js' ev-05` = 4 · E1: `git ls-tree … \| grep -c 'main\.js'` = 0 en `src/<F1>.<Componente>` · E2: `<componente>.ts` l.296-306 · E2: `Adaptador-<Componente>.js` l.7-8 · E2: `Vocabulario-Rules.md` §9 (un término con dos referentes en el mismo contexto de lectura se desambigua) | S2 | 0.85 | El documento define «dos artefactos» y un lector no sabe si el segundo es salida de compilación, fuente de la RCL o fuente del anfitrión; la PoC HTML «como lo haría Blazor» no se puede verificar sin saber qué imita | El documento fija un nombre por referente (por ejemplo «módulo de interoperabilidad», emitido por el bundle, y «script de arranque del anfitrión», escrito en la PoC); la intención del PO se cierra con P-02 |
| HN1-04 | `ev-05` l.35-41 contra `Rules-Base-Conocimiento.md` §4.2 §2 y §6.2 | El pedido exige a la vez «no dictar cómo se estructura la solución .NET» y un documento `propio`, que por definición debe dar «identidad, **estructura**, contrato y esqueletos», con §2 = «layout de archivos». **La frontera entre «estructura de la solución» y «estructura del artefacto» no está definida en ningún insumo**: el único ejemplo es `/demos` vs `/samples`. Sin definición, la restricción es inverificable: no hay forma de decidir si «`wwwroot/` es salida y no fuente» o «el TypeScript vive en el proyecto de la RCL» dictan la solución | E2: `ev-05` l.35, l.41 · E2: `Rules-Base-Conocimiento.md` l.251, l.372 · E4: 002 §2 R2 · E2: `Rules-Base-Conocimiento.md` l.51-56 (la tabla de §0.1 sí distingue «nomenclatura interna del artefacto» de «lo que el framework genera», y es la base para definir la frontera) | S2 | 0.8 | Cada comisión y después cada lector traza la frontera distinta: un documento que no dice dónde va nada es inútil; uno que dice dónde va todo viola R2 y a un fork con `/demos` | Definir R2 operativamente en §0 con la tabla de `Rules-Base-Conocimiento.md` §0.1: **afuera** lo que el framework anfitrión genera o nombra (carpetas de solución, agrupador, nombres de proyecto, cantidad y nombre de samples); **adentro** lo interno del proyecto del bundle, de la RCL y de los anfitriones. RN1-05 lo convierte en requisito |
| HN1-05 | `PREC` `ADR-00043` §2 y §7; `package.json`; `SDD/Maquetas/<F1>-Web/README.md` l.159-165; `assets/vendor/<componente>.js` l.4 | El precedente **declara** dos superficies «generadas del mismo origen y en la misma compilación» y un anfitrión HTML que consume «copia generada»; **construye una sola** (módulo ES) y su anfitrión HTML carga un **prototipo escrito a mano** anterior a la fuente TypeScript. Es decir: la parte del precedente que corresponde a la PoC HTML es exactamente lo que `Rules-Examples.md` §3.6 (13.16) prohíbe para un sample —«nunca una copia»— y lo que la propia documentación del precedente advierte como riesgo | E1: `git show 9aabe5c:src/<F1>.<Componente>/package.json \| grep -c global` = 0; `grep -o 'outfile=…'` = `wwwroot/<componente>.js` (un solo `--format=esm`) · E1: `… assets/vendor/<componente>.js \| grep -n 'Prototipo del componente'` = l.4 · E2: README l.163 «prototipo que acompaña la nota de diseño … 2026-08-10» · E2: `ADR-00043` §2 y §8 («Copias manuales del bundle en el árbol: cero») · E2: `Rules-Examples.md` l.214-215 · E2: `Contratos-Abstractions-…` §6 «La maqueta de la Fase B2 es la excepción a vigilar» | S2 | 0.9 | Si el documento extrae «la forma» del precedente para la PoC HTML sin este dato, transmite un anfitrión que valida un componente que no existe; y cita como `E2` una emisión doble que no está implementada | El documento no puede anclar la PoC HTML en el precedente: la construye como `E3` (contraejemplo/esqueleto) sobre `Rules-Examples.md` §3.6, y usa el precedente sólo para el envoltorio Blazor, el contrato y la cadena. Lo que AH-1 decida sobre una o dos superficies se registra como decisión del documento, no como observación del precedente |
| HN1-06 | `ev-05` l.43 (destino `OUTPUTs/`) contra `Master-Prompt.md` l.573 y `PRODUCT-INTAKE-template.md` l.584 | El objetivo del pedido —que «los agentes … puedan guiarse» leyendo el documento— **no es verificable en el lugar donde el PO lo deja**: el orquestador carga conocimiento sólo desde `Index-Knowledge.md` de `Conocimiento/` (por condición o por alias citado), y un alias que no resuelve contra el índice es **error bloqueante** de intake. Un documento en `OUTPUTs/` sin fila de índice no llega a ningún despacho de este framework ni de un fork mientras alguien no lo copie y catalogue. Además `ev-05` l.17 nombra el caso de una API JavaScript de terceros (mapas) y ningún insumo lo cubre: el precedente lo excluye por requisito («sin ningún recurso remoto») y el framework no lo trata | E2: `Master-Prompt.md` l.573 · E2: `PRODUCT-INTAKE-template.md` l.584-586 · E2: `Rules-Base-Conocimiento.md` l.446-448 (§8: «Producís exactamente dos cosas, y las dos en `Conocimiento/`») · E4: 002 §2 «Destino `OUTPUTs/`» es decisión cerrada · E1: `grep -rniE 'api externa|terceros' ev-05` alcanza sólo l.17; `ADR-00042` §2 punto 2 «sin ningún recurso remoto» | S3 | 0.85 | El PO recibe un documento cuya utilidad declarada nadie puede probar hasta que exista un fork con la fila; y un lector supone que el caso de API externa está cubierto porque el contexto lo nombra | No reabrir el destino (cerrado). Exigir: fila de índice propuesta + procedimiento de adopción de tres pasos + un criterio de §6 que diga «copiado a `Conocimiento/` de un fork con su fila, pasa `Rules-Base-Conocimiento.md` §6.1 sin editar». El caso de API externa se declara afuera en §0 o entra, según P-01 |
| HN1-07 | `ev-05` l.25 y l.33 (objetivos) contra `Rules-Base-Conocimiento.md` §6.2 y el precedente `Knowledge-Template-Blazor-…` | Dos requisitos del pedido no tienen criterio de aceptación y uno colisiona con el techo: (a) «útil en las fases de autoajustes de contratos, o diagnósticos de fallas» no dice qué observable lo prueba; (b) «estilos y formas de codificación» no dice de qué lenguaje ni con qué medida; (c) el pedido dice «**un** documento» para cuatro piezas con esqueletos, y el único `propio` comparable del catálogo tiene 937 líneas contra un techo de 600, acogiéndose a la excepción de §4.3; el techo manda «se parte el documento en dos» | E2: `ev-05` l.25, l.33 · E2: `Rules-Base-Conocimiento.md` l.372-376 · E1: `wc -l Conocimiento/Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md` = **937** · E2: ese documento l.37-39 (omisión declarada por §4.3) · E2: `Rules-Base-Conocimiento.md` l.322-323 («Lo que no se puede responder acá probablemente no sea una convención sino una costumbre, y no va») | S3 | 0.75 | (a) y (b) se cumplen «por sensación» y el jurado no puede votar sobre ellos; (c) el documento sale de 900 líneas invocando la excepción o se parte y el PO recibe dos cuando pidió uno | (a) se vuelve verificable con la tabla síntoma/causa de RN1-23 y los criterios enumerables de RN1-22; (b) se acota a «convenciones que, ignoradas, rompen la costura» (RN1-15, RN1-16) y lo demás no va; (c) es de N2 decidir la partición; para requisitos, «un documento» se lee como «un alias con hermanos declarados en §9», y se le informa al PO |

---

## 4. Lo que revisé y está bien

| # | Qué | Por qué está bien | Ancla |
|---|---|---|---|
| B-01 | La decisión cerrada «el bundle es proyecto de código» y la distinción «PoC HTML no es maqueta» del análisis previo | Siguen válidas en 13.17: la pregunta de `PRODUCT-INTAKE-template.md` §13 decide «proyecto de código» por artefacto propio y dependencias propias, y `Maqueta-Rules.md` §4.1 y §7.2 prohíben el build en la maqueta salvo ADR. 13.16 no tocó ninguna de las dos | E2: `PRODUCT-INTAKE-template.md` l.318 (`ev-02`) · E2: `Maqueta-Rules.md` l.335-341, l.493 · E2: `CHANGELOG.md` [13.16] «`Vocabulario-Rules.md` no se modifica» y `Coherencia-Proyecto-De-Otro-Ecosistema.md` §1 «D8 no cambia» |
| B-02 | La restricción R2 del PO («no dictar la estructura de la solución», ejemplo `/demos` vs `/samples`) | No contradice al framework: es la misma frontera que `Rules-Base-Conocimiento.md` §0.1 ya traza —un documento de conocimiento no fija «nomenclatura y trazabilidad de lo que el framework genera»— y que `Master-Prompt.md` l.574 impone. El pedido y el formato obligatorio piden lo mismo; lo que falta es la definición operativa (HN1-04), no la compatibilidad | E2: `Rules-Base-Conocimiento.md` l.51-56, l.289 · E2: `Master-Prompt.md` l.574 · E2: `ev-05` l.41 |
| B-03 | La forma del contrato en el precedente como fuente de la «base contractual» que el pedido exige | Tipos declarados una vez de cada lado, con los mismos nombres; conjuntos cerrados como uniones de literales / constantes de cadena; campo de versión en el DTO raíz; nombre del callback explícito en los dos lados con el comentario que explica por qué. Es forma sin dominio: se puede extraer entera con nombres neutros y responde literalmente a `ev-05` l.17 | E2: `Contrato.cs` l.3-10, l.114 · E2: `contrato.ts` l.7-8, l.114 · E2: `DiagramaDeCabecera.razor` l.72-77 · E2: `Contratos-Abstractions-<F1>-<Componente>.md` §1-§2 |

---

## 5. Solicitudes de convocatoria

Ninguna. Las señales fuera de mi mandato que encontré ya tienen rol convocado en 002 §4: composición un proyecto o dos (V1; RN1-13 lo deja como alternativa), una o dos superficies de consumo (AH-1; HN1-05 le deja el dato de que el precedente construye una), protocolo de interoperabilidad y liberación (AH-2; RN1-17), cadena de construcción y modo sin cadena (V3; RN1-19), tamaño y partición del documento (N2; HN1-07 c). La API de terceros con clave queda para el refutador según 002 §4.2, y para el PO según P-01.

---

## 6. Preguntas al Product Owner

Las dos nacen de una conjetura (`C`) sobre la intención del pedido; ninguna funda contenido del documento.

| Id | Conjetura que la justifica | Pregunta cerrada | Opciones |
|---|---|---|---|
| P-01 | `C`: que al nombrar «apis javascript externas (como es el caso de googlemaps)» en `ev-05` l.17 el PO espera que `Bundle-JS` cubra un bundle que envuelve una API de terceros con clave y recursos remotos. Ningún insumo lo cubre y el precedente lo excluye por requisito | ¿El bundle que integra una API JavaScript de terceros (con clave y carga remota) entra en el alcance de `Bundle-JS`? | (a) Sí, como sección propia de §3 y §7 con la clave fuera del bundle. (b) No: se declara afuera en §0 con destino «documento hermano futuro». (c) No: se declara afuera sin destino |
| P-02 | `C`: que «`main.js`» en `ev-05` l.36 designa una salida de compilación del proyecto del bundle y no un archivo escrito a mano; y que el «`main.js`» de la PoC HTML (`ev-05` l.37, encargo) es otro archivo. HN1-03 muestra los dos referentes | ¿Qué es `main.js` en el pedido? | (a) Segunda salida de la compilación del bundle: el módulo de interoperabilidad con Blazor, que la RCL importa. (b) Fuente escrita a mano dentro de la RCL que importa el bundle. (c) El script de arranque de la PoC HTML, y el proyecto del bundle emite un solo artefacto. (d) Dos archivos distintos: (a) para la RCL y un script propio para la PoC HTML |

Sigue: consolidación · presidente de mesa
