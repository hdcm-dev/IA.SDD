# Actuación 008 — Informe Comisión V3, Operación y entrega

| Campo | Valor |
|---|---|
| Tipo | `informe` |
| Fecha | 2026-09-13 |
| Autor | Comisión V3, Operación y entrega |
| Corrige | — |

## 1. Mandato y método

**Mandato** (carta `ev-07/06-V3-operacion-entrega.md`): cómo se construye, se prueba y se entrega el bundle junto con la solución .NET —comandos, qué se versiona, cómo se fija la cadena de JavaScript, cómo llega el estático a la app y a las PoC—, cuáles de esas cosas son decisiones con alternativas que el documento presenta sin imponer, y qué se rompe en cada alternativa y cómo se diagnostica. Fuera: el contenido del contrato (V2) y el código del bundle (AH-1).

**Abreviaturas de este informe.** `P1` = precedente `<F1>` `9aabe5c`, raíz `<repo-privado-F1>/`; `<Componente>` = `P1/src/<F1>.<Componente>/`. `P2` = `Lab-Geometria` `d12fb1c`, raíz `<workspace>/PROG2/Geometria/Lab-Geometria/` (ver más abajo por qué se leyó). `FW` = `IA.SDD` `a501857` (13.17), raíz `<workspace>/IA/SDD/IA.SDD/`. `DE` = documento de entrada `INPUTs/Bundle-JS-En-Solucion-Blazor.md` 0.2.

**Orden de lectura, completo:**

| # | Qué | Ruta |
|---|---|---|
| 1 | Encargo común y carta propia | `evidencia/ev-07-cartas-despachadas/00-encargo-comun.md`, `06-V3-operacion-entrega.md` |
| 2 | Presentación original | `evidencia/ev-05-presentacion-original.md` |
| 3 | Documento de entrada | `IA.SDD.Documentacion/PROMPTs/SDD/Catalogado/02-.../INPUTs/Bundle-JS-En-Solucion-Blazor.md` |
| 4 | Actuaciones 001 y 002 | `actuaciones/001-*.md`, `002-*.md` |
| 5 | Evidencia levantada | `evidencia/ev-02-citas.out`, `ev-03-precedente.out` |
| 6 | Formato obligatorio | `FW/SDD/Devs/Rules/Rules-Base-Conocimiento.md` (completo) |
| 7 | Modelo de la base | `FW/Conocimiento/README.md`, `Index-Knowledge.md` |
| 8 | Referencia de forma | `FW/Conocimiento/Knowledge-Template-Blazor-Interactive-Server-SDD-Default.md` (completo); `Knowledge-Conformacion-Pull-Request-Manual.md` (cabecera y §0, como referencia de forma) |
| 9 | Precedente P1 | `<Componente>/<F1>.<Componente>.csproj`, `package.json`, `tsconfig.json`, `tsconfig.pruebas.json`, `.gitignore`, `DiagramaDeCabecera.razor`; `P1/SDD/Docs/05-Arquitectura-Tecnica/Adrs/ADR-00042-*.md`, `ADR-00043-*.md`; `Contratos-Abstractions-<F1>-<Componente>.md` §1–§6; `P1/SDD/Maquetas/<F1>-Web/README.md` §1, §2, §6, §9; y, por el mandato, `P1/CONSTRUIR.md`, `global.json`, `Directory.Build.props`, `herramientas/dotnet`, `herramientas/Dockerfile.construccion`, `.github/workflows/integracion.yml`, `despliegue/publicar.sh`, `src/<F1>.Web/<F1>.Web.csproj` (referencias) |
| 10 | Conjunto normativo, por secciones citadas | `Rules-Devops.md` §4.2, §4.6, §4.8, §4.9 y tabla §2; `Rules-Examples.md` §3.6, §4.5, §4.6, §6; `Intake-Rules.md` §4; `PRODUCT-MANIFEST-template.md` §1.2, §2.B, §3; `Master-Prompt.md` §3.5 y filas de `Pipeline-Producto.md`; `Vocabulario-Rules.md` fila «Solución de código»; `CHANGELOG.md` entradas 13.16 y 13.17 |
| 11 | Precedente P2, fuera de la lista del encargo | `P2/src/GeometriaFactory.Web/GeometriaFactory.Web.csproj` l.50–120, `visor/geometriafactory-visor.csproj`, `visor/package.json`, `scripts/build-visor.sh`, `deploy/Dockerfile.web` (líneas con `visor`/`Skip`), `.gitignore` l.438–444, `GeometriaFactory.sln` (nodos `visor` y `samples`), `samples/visor/01-basico/index.html` l.31 |

**Por qué se leyó P2.** `FW/CHANGELOG.md` l.47 declara que las reglas 13.16 sobre el proyecto de otro ecosistema —insumo de construcción, único generador, modo de construcción sin la segunda cadena, sample como anfitrión mínimo— salieron de ese destino («reporte `30`»). Las reglas que gobiernan mi mandato tienen origen en P2 y no en P1; leer sólo P1 habría dejado a esta comisión anclando contra reglas cuyo caso de origen no vio. No se leyó nada de P2 fuera de la cadena de construcción. La regla 1 del encargo (no leer informes de otras comisiones) se cumplió: en `actuaciones/` sólo se abrieron 001 y 002.

**Qué no se leyó y por qué.** `Rules-Calidad-Y-Pruebas.md` (las pruebas del bundle son de AH-1; acá sólo interesa dónde se enganchan); los `.ts` del bundle y `Contrato.cs` (V2 y AH-1); el resto de `SDD/Docs/` de P1 y P2.

**Evidencia ejecutable levantada (E1).** Tres construcciones del proyecto del bundle de P1 en su contenedor de construcción, sobre el árbol en `9aabe5c` con cinco archivos modificados sin confirmar ajenos a `<Componente>`; `git status --short` antes y después de las tres corridas es **idéntico** (diff vacío), de modo que ninguna escribió fuera de lo ignorado por git. Salidas completas en el scratchpad de la sesión; las líneas que fundan cada ítem se transcriben en el ítem.

```
E1-a  ./herramientas/dotnet build src/<F1>.<Componente>/<F1>.<Componente>.csproj -c Release -v minimal
      → Restored ... <F1>.<Componente>.csproj (in 1.58 sec)
      → (no aparece «<f1>-<componente>@0.0.0 build»: el bundle estaba al día por Inputs/Outputs)
      → > <f1>-<componente>@0.0.0 test ... # tests 83 / # pass 83 / # fail 0
      → Build succeeded. 0 Warning(s) 0 Error(s)   Time Elapsed 00:00:09.51   exit=0
E1-b  rm -f src/<F1>.<Componente>/wwwroot/<componente>.js (archivo generado, ignorado por git)
      ./herramientas/dotnet build ... -p:PruebasDeTypeScript=false
      → > <f1>-<componente>@0.0.0 build
      → > tsc --noEmit && esbuild ts/<componente>.ts --bundle --format=esm --minify --outfile=wwwroot/<componente>.js
      → Build succeeded.  exit=0 ; wwwroot/<componente>.js regenerado (9977 bytes)
      grep -o '<componente>[^"]*\.js' obj/Release/net10.0/staticwebassets.build.json → (vacío)
      grep -o '<componente>[^"]*\.css' ... staticwebassets.build.json → <componente>.css, <componente>.ea9in9axrn.css
E1-c  ./herramientas/dotnet build ... -p:PruebasDeTypeScript=false   (segunda vez, con el .js presente)
      → sin «npm run build» (0 ocurrencias) ; Build succeeded.
      grep -o '<componente>[^"]*\.js' ... staticwebassets.build.json → <componente>.js, <componente>.ohixhhffwb.js
E1-d  ls src/<F1>.<Componente>/wwwroot → <componente>.css, <componente>.js   (no existe <componente>.global.js)
E1-e  grep -rn -i "npm audit\|audit" .github/workflows/integracion.yml package.json *.csproj
      → única coincidencia: csproj l.50 «npm ci --no-audit --no-fund»
E1-f  grep -rn "VersionDeNodeRequerida\|VersionDeNodePresente" src/ → csproj l.31 (declaración) y l.37 (captura); ningún uso
```

## 2. Relevamiento para el documento `Bundle-JS`

Sección destino según `Rules-Base-Conocimiento.md` §4.2. «Decisión» marca lo que el documento presenta como bifurcación con alternativas, sin imponer una (restricción R2 de la actuación 002 §2).

| Id | Sección destino | Qué tiene que decir | Ancla (nivel + cita) | Riesgo si falta |
|---|---|---|---|---|
| RV3-01 | §1 Identidad | El bundle es un **proyecto de código de otro ecosistema**. Pertenece a la **solución de código de su consumidor** cuando éste toma su artefacto como insumo de construcción; sólo es su propia solución si nadie lo toma. La arista con la RCL es de clase **insumo de construcción**, con **un único generador**: el proyecto cuya construcción ejecuta la del bundle | E2 `FW/SDD/Devs/Rules/Intake-Rules.md` l.127–132, l.143, l.145–148; `FW/SDD/Devs/Intake/PRODUCT-MANIFEST-template.md` l.211–217, l.226–229; E4 actuación 002 §2 «el bundle es proyecto de código» | El agente declara «consumo de artefacto publicado» de un artefacto que no se publica, y ordena un build que ningún comando ejecuta (`MANIFEST` l.208–209) |
| RV3-02 | §2 Estructura · Decisión | **Tres composiciones del árbol**, con quién genera en cada una: (a) el TypeScript vive **dentro** de la RCL y ésta se genera a sí misma —`ts/` → `wwwroot/`—; (b) paquete npm **hermano** (`package.json` propio) y la RCL o la app es el generador que lo construye y copia `dist/` a su `wwwroot/`; (c) paquete npm como **solución propia**, sólo si nadie de la solución .NET toma su artefacto (es el caso B del DE §5: se publica). Qué se rompe: en (b) sin `Inputs`/`Outputs` el bundle se reconstruye siempre o nunca; en (c) hay que publicar antes de consumir | E2 (a) `<Componente>/<F1>.<Componente>.csproj` l.1–23, `package.json` l.8 (`--outfile=wwwroot/<componente>.js`); (b) `P2/src/GeometriaFactory.Web/GeometriaFactory.Web.csproj` l.54–60, l.69–81, `P2/scripts/build-visor.sh` l.25–31; (c) `Intake-Rules.md` l.130–132 | El documento impone una sola composición y con eso dicta la estructura de la solución .NET (R2) |
| RV3-03 | §2 Estructura | **Qué se versiona y qué no.** Se versiona: fuentes (`ts/` o `src/`), `package.json`, **`package-lock.json`**, `tsconfig*.json`, la configuración del empaquetador. No se versiona: `node_modules/`, el bundle emitido, la salida de pruebas, `dist/`. Si el bundle se copia a un `wwwroot/js/` del consumidor, ese destino también se ignora, con un `.gitkeep` | E2 `<Componente>/.gitignore` l.1–4; `git ls-files <Componente>` (28 archivos, ninguno de `wwwroot/` ni `build/`); `P2/.gitignore` l.438–444; `P1/.../ADR-00042-*.md` l.41 («El bundle no se versiona»), l.87 (métrica: archivos de bundle versionados = 0); `<Componente>/tsconfig.pruebas.json` l.2 (`build/` «no se versiona ni se empaqueta») | Un bundle versionado queda desincronizado de su fuente sin que nada falle (ADR-00042 l.41, l.62) |
| RV3-04 | §3 Contrato de uso · Decisión | **Cuántos comandos construyen el producto.** Alternativa elegida por los dos precedentes: **uno** (`dotnet build <sln>`), con la cadena JavaScript colgada de targets del `.csproj` generador. Alternativa: paso previo separado. Qué se rompe con el paso separado: un clon limpio construye el consumidor **sin** su bundle y nada falla; se nota en ejecución | E1-a (un comando restaura, empaqueta si hace falta, corre 83 pruebas y termina en 9,5 s); E2 `P1/CONSTRUIR.md` l.66–71; ADR-00042 l.38, l.56, l.83; `P2/...Web.csproj` l.55–57 («un clon limpio construía un front SIN VISOR sin que nada fallara»); `FW/.../Rules-Devops.md` l.274 (anti-patrón «insumo de construcción omitido en silencio») | Es el modo de falla con antecedente real en los dos precedentes |
| RV3-05 | §3 Contrato de uso · Decisión | **Cómo se fija la cadena JavaScript.** (1) Versión mayor del entorno de ejecución declarada **en un solo lugar** y verificada en la construcción; (2) instalación **reproducible** con `npm ci` contra el archivo de bloqueo, nunca `npm install` en la construcción; (3) versiones **exactas** en `devDependencies`. Alternativas para (1): propiedad del `.csproj` comparada en el target; archivo de versión del gestor de Node; campo `engines` con modo estricto; imagen de construcción. Qué se rompe: sin lock, `npm ci` falla o la instalación flota; con versión de Node distinta, el empaquetador puede no arrancar | E2 `<Componente>/<F1>.<Componente>.csproj` l.25–32, l.44–52; `package.json` l.13–16 (`esbuild` `0.25.12`, `typescript` `5.9.3` exactas; `@types/node` `^22.20.1` flotante); ADR-00042 l.73–74; `P2/scripts/build-visor.sh` l.16–23 (`npm ci` si hay lock, `npm install` y versionar el resultado si no). Hecho de stack: documentación de npm, comando `npm ci` (exige lock coherente con `package.json`) | Instalación flotante: dos máquinas construyen bundles distintos del mismo commit |
| RV3-06 | §3 Contrato de uso | **El bundle tiene que existir antes de que se descubran los estáticos**, y eso **no alcanza**: los ítems de un proyecto se evalúan antes de que corra cualquier target, así que un archivo escrito en `wwwroot/` durante la construcción **no entra al manifiesto de recursos estáticos en la primera construcción desde limpio**; entra en la segunda. Síntoma: 404 en `_content/<Id>/<bundle>.js` (o en `js/<bundle>.js`) con `Build succeeded`. Salidas: declarar el archivo generado como `Content` **dentro de un target** posterior a la generación y anterior a la resolución de estáticos (con `Exclude="@(Content)"` para no duplicarlo cuando ya existía), o `dotnet publish`, que reevalúa | **E1-b/E1-c** (manifiesto sin `<componente>.js` tras regenerarlo; con él en la segunda corrida); E2 `<Componente>/<F1>.<Componente>.csproj` l.54–66 (el comentario cree que `BeforeTargets="ResolveStaticWebAssetsInputs"` alcanza); `P2/...Web.csproj` l.62–66 («medido: el endpoint faltaba en el build 1 y aparecía en el 2») y l.83–91 (target `DeclareVisorAsset`). Hecho de stack: documentación de MSBuild, evaluación de ítems frente a ejecución de targets | Es el diagnóstico más probable para un agente que sigue P1 al pie de la letra: construye bien y la PoC Blazor no encuentra el módulo |
| RV3-07 | §3 Contrato de uso · Decisión | **Cómo llega el estático a la app.** (a) Si el generador es una RCL, los estáticos de su `wwwroot/` se sirven **solos** bajo `_content/<PackageId>/` en toda app que la referencie por `ProjectReference`, sin configuración; (b) si el generador es la app, el bundle se copia a su `wwwroot/` y se referencia por ruta propia. Qué se rompe: en (a), un `PackageId`/`AssemblyName` que cambia rompe la ruta de importación escrita en el envoltorio; en (b), dos apps consumidoras son dos generadores (anti-patrón) | E2 `<Componente>/DiagramaDeCabecera.razor` l.17–18, l.56–57 (`import "./_content/<F1>.<Componente>/<componente>.js"`); `P1/src/<F1>.Web/<F1>.Web.csproj` l.53 (`ProjectReference` a <Componente>); ADR-00042 l.39; `P2/...Web.csproj` l.70, l.96; `Rules-Devops.md` l.275. Hecho de stack: documentación de ASP.NET Core, «Razor class library», estáticos bajo `_content/{PACKAGE ID}` | El agente configura estáticos a mano o copia el bundle a la app aunque venga de una RCL |
| RV3-08 | §3 Contrato de uso · Decisión | **Cómo llega el estático a la PoC HTML.** La regla vigente: el anfitrión mínimo carga el artefacto **construido, desde la salida del proyecto que lo produce, y nunca una copia**. Alternativas de ruta: relativa a la salida del productor (`../../<productor>/dist/` o `.../wwwroot/`), o servida por la app bajo `_content/`. Prohibido: copia versionada en la carpeta de la PoC | E2 `FW/SDD/Devs/Rules/Rules-Examples.md` l.210–215; `P2/samples/visor/01-basico/index.html` l.31 (`src="../../../visor/dist/geometriafactory-visor.js"`); contraejemplo: `P1/SDD/Maquetas/<F1>-Web/README.md` l.52, l.159, l.163 (copia en `assets/vendor/`, y además prototipo a mano) | La PoC valida un bundle que ya no es el que corre (`Maquetas/.../README.md` l.159 lo dice con sus palabras) |
| RV3-09 | §4 Decisiones · Decisión | **Formato de salida del bundle**: módulo ES (lo que Blazor importa con `import`), global clásico (IIFE con un nombre global, cargable con `<script>` desde el sistema de archivos), o **ambos desde la misma compilación**. Qué se rompe: un módulo ES **no se carga abriendo el HTML por doble clic** (`file://`), así que una PoC HTML «sin servidor» exige el global o un servidor estático; dos implementaciones separadas divergen | E2 `P1/.../ADR-00043-*.md` l.35–40, l.48–53, l.69; `Rules-Examples.md` l.218–219 (el sample declara si abre «sin servidor» o «servido»); `P1/SDD/Maquetas/<F1>-Web/README.md` l.36 (sobre `file://` no todo funciona). Hecho de stack: documentación de MDN, «JavaScript modules», carga de módulos sobre `file://`; documentación de esbuild, opciones `--format=iife` y `--global-name` | El agente abre la PoC HTML sin servidor, el módulo no carga y diagnostica el bundle en vez de la carga |
| RV3-10 | §4 Decisiones · Decisión | **Dónde corren las pruebas del bundle.** (a) Colgadas del `Build` del proyecto generador, con interruptor para construir sin correrlas; (b) proyecto de pruebas .NET que lanza la batería JavaScript; (c) paso separado del pipeline. Qué se rompe: `dotnet test <sln>` **no visita** una biblioteca, así que con (c) la batería puede no correr nunca en local; con (a) cada build paga décimas de segundo y una prueba rota rompe la construcción. Nota de frontera: esto es la batería del bundle; la **verificación de un sample nunca se engancha a la construcción** | E1-a (83 pruebas dentro del build, `# fail 0`), E1-b (`-p:PruebasDeTypeScript=false` las apaga); E2 `<Componente>/<F1>.<Componente>.csproj` l.80–99; `P1/CONSTRUIR.md` l.79–99; `Rules-Examples.md` l.199–203, l.328 | `dotnet test` verde con el bundle roto |
| RV3-11 | §4 Decisiones · Decisión | **Construcción donde falta la cadena JavaScript.** La regla: o la construcción **falla nombrando la cadena que falta**, o el ambiente usa un **modo de construcción explícito y nombrado** que omite la generación y recibe el bundle hecho por otra etapa. P1 hace lo primero (target `VerificarNode`); P2 hace las dos (`-p:SkipVisorBuild=true` y un Dockerfile de dos etapas: Node genera, el SDK .NET publica recibiendo el bundle). Qué se rompe sin esto: el consumidor se entrega sin su insumo | E2 `Rules-Devops.md` l.284, l.274; `<Componente>/<F1>.<Componente>.csproj` l.34–42; `P2/...Web.csproj` l.59–60, l.77–79; `P2/deploy/Dockerfile.web` l.37, l.53, l.69–72, l.94 | Publicación silenciosa sin bundle |
| RV3-12 | §4 Decisiones · Decisión | **Versionado.** Cuando el bundle viaja dentro del producto: **no tiene versión propia** (`package.json` con `0.0.0` y `private: true`), el contrato se versiona con el producto, y la versión del producto la calcula la herramienta de versionado desde etiquetas de git, sin escribirla en ningún archivo. Alternativa (caso B del DE): paquete npm con versión propia, canales preview/stable y `guia-publicacion-paquete-npm.md`. Fuera de alcance: NuGet (decisión cerrada) | E2 `<Componente>/package.json` l.3–4; `P2/visor/package.json` l.3–4; `P1/Directory.Build.props` l.31–37; `P1/.../Contratos-Abstractions-<F1>-<Componente>.md` l.92–96; `Rules-Devops.md` l.86; E4 actuación 002 §2 `decisiones_cerradas` | Se inventa una matriz de compatibilidad para un artefacto que compila y se publica junto con su anfitrión |
| RV3-13 | §4 Decisiones · Decisión | **Caché del navegador.** El bundle cambia en cada construcción **con el mismo nombre**; sin sello, el navegador sigue sirviendo el anterior. Alternativas: sello derivado del **contenido** (hash) en la consulta de la URL; huella de recursos estáticos del propio marco (el manifiesto ya emite `<nombre>.<huella>.js`); importación por nombre fijo (P1) asumiendo control de caché del servidor. Síntoma: «el cambio no se ve» con build verde | E1-c (manifiesto con `<componente>.ohixhhffwb.js`); E2 `P2/...Web.csproj` l.94–100, l.102–118 (`SealVisorAsset`, SHA-256 del bundle); `<Componente>/DiagramaDeCabecera.razor` l.57 (nombre fijo). Hecho de stack: documentación de ASP.NET Core, «Map static files», huella de recursos estáticos | El agente reconstruye el bundle diez veces sin ver el cambio |
| RV3-14 | §3 Contrato de uso | **Higiene del proyecto mixto.** Excluir `node_modules/**` de los ítems por omisión (o `EnableDefaultItems=false` en un nodo sin ensamblado); quitar `package.json`, lock y `tsconfig*` de `Content` para que no se publiquen; fuentes JavaScript con `CopyToOutputDirectory=Never` y `CopyToPublishDirectory=Never`. Qué se rompe: miles de ítems en el glob (P2 midió 6.592), configuración de la cadena publicada al artefacto | E2 `<Componente>/<F1>.<Componente>.csproj` l.22, l.107–111; `P2/visor/geometriafactory-visor.csproj` l.17–28 | Build lento y artefacto publicado con insumos de construcción |
| RV3-15 | §3 Contrato de uso | **Incrementalidad declarada.** Restauración con `Inputs="package-lock.json"` `Outputs="node_modules/.instalado"`; empaquetado con `Inputs` = fuentes + lock + configuración del empaquetador, `Outputs` = el bundle. Qué se rompe si un insumo no está en `Inputs`: se cambia (p. ej. `tsconfig.json`, que P1 **no** lista) y el bundle no se regenera con build «exitoso» | E1-a/E1-c (segundo build sin `npm run build`), E1-b (borrar el bundle lo regenera); E2 `<Componente>/<F1>.<Componente>.csproj` l.48–52, l.60–66 (`Inputs="@(TypeScriptFuente);package-lock.json"`); `P2/...Web.csproj` l.74 (lista `webpack.config.js` y `tsconfig.json`) | Bundle viejo con build verde; el agente busca el error en el código |
| RV3-16 | §2 / §3 Entrega | **Qué llega al artefacto publicado y qué no.** `dotnet publish` del consumidor lleva el bundle ya construido; Node es **requisito de construcción, no de ejecución**: el servidor no lo tiene ni lo necesita. Dependencias de ejecución en el artefacto: cero | E1-c (`staticwebassets.publish.json` registra `<componente>.js` y variantes comprimidas); E2 `P1/herramientas/Dockerfile.construccion` l.8–9; `<Componente>/<F1>.<Componente>.csproj` l.26–28; `P1/despliegue/publicar.sh` l.4–5, l.20–24; ADR-00042 l.67, l.85 | Se instala Node en producción o se agrega una dependencia de ejecución al bundle |
| RV3-17 | §3 Contrato de uso | **Cómo entran las dos PoC al agrupador de la solución.** PoC Blazor: proyecto del ecosistema de la solución, **se construye con ella**; PoC HTML: **sin construcción**, como elemento visible del agrupador, y se verifica sólo con el `comando` de su contrato. La forma concreta del agrupador la declara el destino, no este documento | E2 `Rules-Examples.md` l.191–197, l.205–208; `P2/GeometriaFactory.sln` l.24–28 (nodo `visor` y nodo `samples`), `P2/visor/geometriafactory-visor.csproj` l.4–15 (nodo inerte: no corre `npm`, sin `ProjectReference`); E4 actuación 002 §2 R2 | Construir la PoC HTML con la solución: la construcción del producto pasa a depender de un sample |
| RV3-18 | §6 Criterios | Criterios verificables de la cadena: comandos para construir desde limpio = **1**; archivos de bundle versionados = **0**; dependencias de ejecución en el artefacto = **0**; copias manuales del bundle en el árbol = **0**; el primer build desde limpio deja el bundle **registrado como estático** (no sólo en disco); toda carpeta de PoC con markdown está en el agrupador (enumeración) | E2 ADR-00042 l.81–87; ADR-00043 l.76–81; `Rules-Examples.md` l.503–504; E1-b como forma de comprobar el criterio del manifiesto | El conocimiento «se aplicó bien» sin poder decirlo con un número |
| RV3-19 | §5 Esqueletos | Un `.csproj` generador mínimo con los cinco targets y su orden —verificar cadena, restaurar contra lock, empaquetar antes de resolver estáticos, **declarar el generado como `Content` en target**, pruebas tras `Build` con interruptor—, un `package.json` con scripts `build`/`test` y versiones exactas, y el `.gitignore`. Presentado como esqueleto de la alternativa (a) y de la (b) | E2 `<Componente>/<F1>.<Componente>.csproj` completo; `P2/...Web.csproj` l.68–91; `<Componente>/package.json` l.7–16; `<Componente>/.gitignore` | El orden de targets se transmite mal en prosa (`Rules-Base-Conocimiento.md` l.153–156 cita el precedente de `Templates/README.md`) |
| RV3-20 | §7 Anti-patrones | Paso manual previo al build; bundle versionado; copia del bundle editada a mano; `npm install` en la construcción; dos generadores del mismo bundle; verificación del sample enganchada a la construcción; versión de Node declarada pero no verificada; insumo omitido en silencio | E2 ADR-00042 l.56; `P1/SDD/Maquetas/<F1>-Web/README.md` l.159; `Rules-Devops.md` l.274–275; `Rules-Examples.md` l.327–328; E1-f | El documento repite lo que ya falló en los precedentes |
| RV3-21 | §8 Frontera | Qué de este tema **es normativo y no va acá**: orden de construcción y coordinación por arista → `Pipeline-Producto.md` (se emite con más de un proyecto de código); clase de arista y único generador → manifiesto §2.B/§3 e `Intake-Rules.md` §4; forma de entrada de los samples y su verificación → `Rules-Examples.md` §3.6; SCA, SBOM y supply chain de la cadena → `Rules-Devops.md` §4.2 y §4.6. El documento **no fija** dónde van las PoC ni cómo se llama el agrupador | E2 `Rules-Devops.md` l.80, l.162, l.199–208, l.282–284; `Master-Prompt.md` l.554, l.716, l.1586; `Rules-Base-Conocimiento.md` l.289 (conocimiento disfrazado de regla) | El documento se lee como norma y desplaza a la regla de categoría sin gobierno |

## 3. Hallazgos

| Id | Ubicación | Afirmación | Evidencia (nivel + ancla) | Severidad | Confianza | Impacto si no se corrige | Dirección propuesta |
|---|---|---|---|---|---|---|---|
| HV3-01 | DE §2.3, §4 obs. 2, §8.4 «Grafos» | El DE declara que el uso del `dist/` por la RCL «es consumo de artefacto publicado, no arista» y que hay «dos soluciones de código». El conjunto 13.17 dice lo contrario para este caso: dentro de una solución, entre proyectos de ecosistemas distintos, la única clase de arista es **insumo de construcción**, el bundle **pertenece a la solución de su consumidor**, y el orden «bundle → RCL → Web» no es un paso que alguien ejecuta sino una arista que el generador materializa en el mismo comando. El DE se escribió contra 13.15 (su cabecera lo dice) y el cambio entró el mismo día en 13.16 | E2 `Intake-Rules.md` l.127–132, l.143, l.154–155; `PRODUCT-MANIFEST-template.md` l.211–217, l.226–229; `CHANGELOG.md` l.45–63; DE l.7 («13.15 según memoria de proyecto») | S2 | 0,95 | El documento transmite un modelo de dos soluciones y de «consumo publicado» de algo que no se publica, contra la regla vigente; un agente ordena un build que ningún comando ejecuta | Reescribir DE §2.3, §4 obs. 2 y §8.4 sobre 13.17: una solución, arista de insumo de construcción, generador único, y el orden de build como consecuencia de la arista. PA-01 del DE queda parcialmente respondida por `PRODUCT-MANIFEST-template.md` §1.2 (perfil por ecosistema) |
| HV3-02 | DE §8.2 y §8.3 | El DE afirma que el framework no tiene figura para la PoC y encaja las dos PoC como samples de una unidad `library` nueva. Desde 13.16 existe la figura **«sample de un artefacto que otro proyecto carga» = anfitrión mínimo**: carga el artefacto construido desde la salida del productor, nunca una copia; invoca sólo la superficie pública; declara si abre sin servidor o servido; lleva un doble del consumidor si el artefacto invoca de vuelta. Es exactamente la PoC HTML del pedido, y fija **cómo le llega el estático**, que es lo que corresponde a esta comisión. Si esa figura resuelve además PA-04 no lo afirma esta comisión | E2 `Rules-Examples.md` l.210–223; `CHANGELOG.md` l.59–60; DE l.208–211, l.233–247 | S2 | 0,85 | El documento inventa una figura para lo que el framework ya nombra, y omite la regla «nunca una copia», que P1 incumple en su maqueta | El documento cita `Rules-Examples.md` §3.6 como la figura del anfitrión mínimo y toma de ahí la regla de carga; la decisión sobre la unidad de entrega sigue siendo del Product Owner (PA-04) |
| HV3-03 | P1: ADR-00043 §2 y §7; `Contratos-Abstractions-<F1>-<Componente>.md` §2; `Maquetas/<F1>-Web/README.md` §9 | El precedente declara **dos superficies generadas en la misma compilación** y una **copia generada** para la maqueta. El `package.json` sólo emite el módulo ES; no existe `<componente>.global.js` en la salida; la copia de la maqueta es un **prototipo a mano** fechado 2026-08-10, «forma equivalente» a una salida que la cadena no produce | E2 `<Componente>/package.json` l.8 (un solo `esbuild ... --format=esm`); ADR-00043 l.35–40, l.69–71; Contratos l.34–39; `Maquetas/.../README.md` l.163; E1-d (`wwwroot/` sólo con `<componente>.css` y `<componente>.js`) | S2 | 0,95 | Si el documento cita a P1 como evidencia de que «las dos superficies salen de la misma compilación», enseña una cadena que no existe; el agente la reproduce y la PoC HTML valida un prototipo | Presentar las dos superficies como alternativa (RV3-09) con su comando y **sin** apoyarla en P1; registrar que P1 la tiene decidida y no construida |
| HV3-04 | P1: `<F1>.<Componente>.csproj` l.25–42; ADR-00042 §7 | La versión de Node «se declara acá y no se toma la que haya en la máquina» (comentario y ADR), pero `VersionDeNodeRequerida` **no se compara con nada** y `VersionDeNodePresente` se captura y no se usa: el target sólo falla si Node está ausente. La versión `22` está escrita en **tres lugares** (csproj, Dockerfile de construcción, CI) | E1-f; E2 csproj l.31, l.34–42; `P1/herramientas/Dockerfile.construccion` l.12; `P1/.github/workflows/integracion.yml` l.40; ADR-00042 l.73 | S3 | 0,95 | El documento transmite «versión fijada» donde sólo hay «presencia verificada»; un agente con Node 18 o 24 construye un bundle distinto o no construye, con un mensaje que no nombra la versión | RV3-05: la fijación se presenta con sus alternativas reales y con el criterio «un solo lugar, verificado»; P1 se cita como declaración sin efecto, no como modelo |
| HV3-05 | P1: ADR-00042 §7 l.75 y §8 l.86; `integracion.yml`; `<F1>.<Componente>.csproj` l.50 | El ADR promete «auditoría de dependencias en el mismo gate que ya bloquea por vulnerabilidades de severidad alta» y una métrica «vulnerabilidades altas = 0, bloqueante». Ningún archivo la ejecuta: el `npm ci` corre con `--no-audit`, el flujo de CI no tiene paso de auditoría y no hay `npm audit` en `SDD/Docs/` | E1-e; E2 `integracion.yml` l.24–82 (pasos: construir, test, tres compuertas de Python, cobertura); ADR-00042 l.75, l.86 | S3 | 0,9 | El documento cita como cumplido un gate de supply chain que el precedente no tiene; el agente hereda la promesa sin el instrumento | El documento remite el SCA de la cadena JavaScript a `Rules-Devops.md` §4.2 y §4.6 (frontera, RV3-21) sin nombrar herramienta, y no cita a P1 como cumplimiento |
| HV3-06 | P1: `<F1>.<Componente>.csproj` l.54–66 (comentario «BeforeBuild solo no alcanza») | El comentario cree que generar el bundle antes de `ResolveStaticWebAssetsInputs` garantiza su registro. Medido: **la primera construcción desde limpio genera el bundle y no lo registra en el manifiesto de estáticos**; la segunda sí. P2 midió lo mismo y lo resolvió declarando el generado como `Content` dentro de un target | E1-b/E1-c; E2 `P2/...Web.csproj` l.62–66, l.83–91 | S2 | 0,9 | Es el fallo que un agente va a encontrar en su primer `dotnet build` de una PoC Blazor en clon limpio: 404 del módulo con build verde, y ni P1 ni el DE lo nombran | RV3-06 y RV3-19: síntoma, causa (evaluación de ítems antes de los targets) y las dos salidas, en §3 y §7 del documento; criterio en §6 (RV3-18) |
| HV3-07 | Encargo común, tabla de insumos, fila «Precedente real» | El encargo lista un solo precedente (P1). Las reglas 13.16 que gobiernan este mandato nacieron de **otro** destino (P2), que resolvió la cadena con decisiones distintas en al menos cinco puntos: paquete npm hermano con generador en el consumidor, modo de construcción nombrado, sello de caché por contenido, nodo inerte en el agrupador para visibilidad, y sample que carga desde la salida del productor. Con P1 solo, cada bifurcación de RV3-02, RV3-07, RV3-11, RV3-13 y RV3-17 tendría una sola alternativa anclada | E2 `CHANGELOG.md` l.47; `P2/...Web.csproj` l.54–120; `P2/visor/geometriafactory-visor.csproj` l.4–19; `P2/samples/visor/01-basico/index.html` l.31 | S3 | 0,9 | El documento presenta como única la forma de P1, que además incumple tres de sus propias decisiones (HV3-03, HV3-04, HV3-05), y dicta de hecho la estructura (R2) | Incorporar P2 como segundo precedente, citado ofuscado como P1 (R4); las filas del relevamiento de esta comisión ya lo anclan |

## 4. Lo que revisé y está bien

| # | Qué | Ancla |
|---|---|---|
| 1 | **PA-02 del DE no es una contradicción sino un silencio.** `Pipeline-Producto.md` se emite con más de un proyecto de código, y las tres fuentes coinciden; `Master-Prompt.md` §3.5 l.376 sólo dice que `Vista-Producto.md` «sí se emite» y no dice nada del pipeline. El orden de construcción del bundle tiene lugar normativo | E2 `Rules-Devops.md` l.80, l.290; `Master-Prompt.md` l.554, l.716, l.1586, l.376 |
| 2 | **Un solo comando construye, empaqueta y prueba el bundle de P1, y no deja rastro versionado.** Restauración, empaquetado condicional, 83 pruebas y `Build succeeded` en 9,5 s; `git status` idéntico antes y después. La métrica 1 de ADR-00042 §8 está cumplida y verificada, no supuesta | E1-a; ADR-00042 l.83 |
| 3 | **La incrementalidad declarada funciona como está escrita.** Con el bundle al día, `npm run build` no corre; borrado el bundle, se regenera; la batería corre en cada build porque no declara `Inputs`/`Outputs`, que es lo que `CONSTRUIR.md` promete («no se pudre») | E1-a, E1-b, E1-c; `<Componente>/<F1>.<Componente>.csproj` l.48–66, l.94–99; `P1/CONSTRUIR.md` l.96–99 |

## 5. Solicitudes de convocatoria

| Id | Rol faltante | Señal, con ubicación | Qué no puedo afirmar sin él |
|---|---|---|---|
| SC-V3-01 | AH-1, Ingeniería del bundle | HV3-03: la cadena de P1 emite sólo módulo ES aunque ADR-00043 decide dos superficies; el punto de entrada `ts/<componente>.ts` exporta funciones (`ev-03` l.113–118) y no se ve un nombre global | Si el **código** del bundle admite emitirse como global con un solo nombre sin cambios, o si la segunda superficie exige un segundo punto de entrada. De eso depende si RV3-09 se presenta como «un comando más» o como decisión de diseño del bundle |
| SC-V3-02 | V2, Interfaz / consumidor externo | RV3-08 y `Rules-Examples.md` l.220–221: el anfitrión mínimo lleva «un doble del consumidor con la lista de invocaciones que ese proyecto expone, derivada de una sola fuente» | Cuál es esa fuente única de las invocaciones de vuelta (los nombres `JSInvokable`) y cómo la PoC HTML la deriva sin copiarla; esta comisión sólo puede decir de dónde se carga el artefacto |

## 6. Preguntas al Product Owner

| Id | Pregunta cerrada | Opciones | Conjetura que la funda |
|---|---|---|---|
| PQ-V3-01 | ¿La PoC HTML tiene que abrirse **sin servidor** (doble clic sobre el archivo), o alcanza con servirla desde un servidor estático o desde la app? | (a) Sin servidor, obligatorio → el bundle emite además una superficie global; (b) Servida → alcanza el módulo ES; (c) Las dos formas, y el documento las presenta como alternativa | `C`: el pedido dice «invoca el bundle como lo haría Blazor, sin .NET» (`ev-05` l.37; DE l.198–199) y no dice cómo se abre; Blazor importa un módulo ES y un módulo ES no carga sobre `file://`. Sin la respuesta, RV3-09 no puede recomendar ni siquiera un valor por defecto |

Sigue: consolidación · presidente de mesa
