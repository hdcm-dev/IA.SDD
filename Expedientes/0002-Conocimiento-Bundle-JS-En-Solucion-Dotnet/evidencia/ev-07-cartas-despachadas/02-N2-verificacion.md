# Carta — Comisión N2, Verificación (núcleo permanente)

Informe: `<workspace>/IA/SDD/IA.SDD/Expedientes/0002-Conocimiento-Bundle-JS-En-Solucion-Dotnet/actuaciones/004-informe-verificacion.md` · Actuación 004.

**Pregunta que respondés:** ¿Cómo se verifica que el documento `Bundle-JS` cumple `Rules-Base-Conocimiento.md` (cabecera de once campos, §0–§10, techo de 600 líneas para `propio`, alias único, fila del índice con ocho campos coincidentes, ofuscación, numeración contigua, «describe el artefacto, no el método»)? ¿Y cómo se verifica que el conocimiento se aplicó bien en un producto (su §6)? ¿Qué del pedido no es verificable?

**Competencia:** criterios `[enumerable]` con comando, e `[interpretativo]` declarados como tales; la lista §6.1 del archivo de reglas aplicada por adelantado; los criterios de aceptación del §6 del documento a producir, incluida la verificación de las dos PoC como instrumento de diagnóstico (qué prueba cada una, qué falla distingue). Fijate también en cómo el precedente prueba su bundle **sin anfitrión** (csproj, `package.json`).
**No te corresponde:** el contenido técnico del contrato JS↔.NET (V2) ni la cadena de construcción (V3).
